# code3 —— 第三题 `sparse_flash_attention`（SFA，MLA-absorb）

> ## ⛔ 给"正在往这个文件里写东西的会话"：三条规则，先读完再动笔
>
> 1. **本文件只放"当前口径"，不放过程。** 它现在 **≈290 行 / ≈56 KB**（⚠️ 从 9/23 的 29 KB 一路涨上来，因为那几天把三条方向轴读翻了）—— 每一轮新对话都要整读它，你多写 5 KB 就是下个会话多付 5 KB ⇒ **规则：只加不减的那一轮不允许**；加一行前先找一行压回它（判死的、被上位读数替代的、细节已在 LOG 锚点里的），压不回去就在 PR 说明里写"净涨 X KB、因为买了 Y 这个数"。
> 2. **你要写的 `§15.x` 那种过程台账（推理、翻车、消融细节、"本轮读了什么"）一律追加到 `code 3/probes/LOG.md` 末尾**：编号**接着档案里已有的最大号**（当前到 `15.99` ⇒ 下一条 `15.100`），一条 **≤20 行**，三段式 = 改了什么 / 读数 / 判，并在那份"追加条目索引"里登一行。原始输出落 `code 3/probes/*.{txt,out}`（⚠️ `.log` 会被 `.gitignore` 吃掉）。⛔ 不要写进本文件。
> 3. 只有当新结论**改变了下面某一节里已有的断言**时，才动本文件 —— 而且**就地改那一行/那一格**（旧断言直接消失）。⛔ 不要在文末追加一节、⛔ 不要新增一条"已作废"和旧句子并存（那种共存的写法就是这份文档上次涨到 676 KB 的原因）。改完顺手把被推翻的原话留在 `LOG.md` 那张"已知为错"表里加一行。
>
> ---

> **三层文档口径（2026-09-23 起）**：读**这一份**就够，不要读档案。
>
> | 层 | 位置 | 是什么 | 什么时候读 |
> |---|---|---|---|
> | L0 | `AGENT.MD` | 三题入口 + 通用纪律 | 每次 |
> | **L1** | **本文件** | 题 3 的**全部当前口径**：状态 / 契约 / 机器事实与单价 / 架构 / 排除清单 / 下一步 | 每次，**整读**（28 KB） |
> | L2 | `code 3/probes/LOG.md` | **证据档案（冻结件）**：9/21→9/23 全部轮次的原始台账，76 节 / 700 KB | 只在要复看某条读数的**现场细节**时按锚点查 |
> | L3 | `code 3/probes/*.log`、`*.txt` | 原始计时 / 对拍输出 | 同上 |
>
> ⛔ **本文件与 LOG 冲突时一律以本文件为准**（LOG 不改、不订正，所以里面有已知错的条目）。LOG 开头有一张"⛔ 本档案中已知为错的条目"表 —— 引用任何 `LOG#…` 结论前先扫那张表。
> 锚点写法：`LOG#15.74(d)` ⇒ `grep -n '^### 15.74' "code 3/probes/LOG.md"`。
> 通用流程与坑见 [算子开发工作流.md](算子开发工作流.md)；连接参数见 [连接信息.md](连接信息.md)；**官方题面**见 [official_problem_statement.md](official_problem_statement.md#第三题b组困难题sparseflashattention-算子)。
> ⛔ 本题提交实现只在 **`code 3/code/`**，与另两题严禁跨界。其它两题：[code1.md](code1.md) · [code2.md](code2.md)。

---

## 0. 工作约束（**必须遵守**）

| # | 约束 |
|---|---|
| 1 | 每轮对话收尾：**本文件改到"当前口径"**（过期断言**就地改对**，不与"已作废"共存）+ 在 `code 3/probes/LOG.md` **末尾追加**本轮台账（一条 ≤20 行：改了什么 / 读数 / 判）。⛔ 过程叙事属于 LOG，不写进本文件 |
| 2 | 只改 `code 3/`、本文件、以及 `AGENT.MD` 的题 3 行；不动 code1 / code2 / 根目录其余 |
| 3 | 不用 `printf` / `fflush` / `cout` 调试（dump 到文件、返回值对比） |
| 4 | 改代码前先备份到 **`code 3/probes/backup/`**（⛔ 不许留在 `code/` 里） |
| 5 | 一步一步验证，每步单独确认结果，不一次性改多处 |
| 6 | **提交额度不限**（用户 2026-09-23 授权"提交到比赛平台的次数不限制"）⇒ 不再为"要不要花这一发"停下来问。⛔ 但**发次链一步不能省**（§3 那五步），且因为口径是 `latest` ⇒ **探针/判死类发次之后必须回发当前最好的那份字节**，别让榜上挂着探针 |

---

## 1. 状态板（当前口径）

| 项 | 值 |
|---|---|
| 算子 | `sparse_flash_attention`，MLA-absorb（`attentionMode=2`），D=512 + rope 64 ⇒ **576 维点积**，V 侧无 rope |
| 提交实现 | **`code 3/code/`** 四文件（§3）；工程文档 `code 3/`；真机构建区 `02aeb:~/sfa_real/`（**只放产物，源码唯一所在地是本仓库**） |
| 算力环境 | ✅ 真机使用权在手（第二账号 910B3，单卡 NPU ID=2，CANN 9.0.0 / `arch2201`，**40 AIV**，MIX `KERNEL_TYPE_MIX_AIC_1_2` = 1 AIC : 2 AIV）。构建/回归/计时全走 `code 3/npu_debug/{npu.sh,dev.sh}`。云端仿真机 `e6z6k` 降级为编译门 + 参考源码浏览器 |
| **榜上现挂** | **P105 字节第 14 遍** `[7.7, 8.32, 11.76, 13.08, 10.58, 15.32]`（Σ **66.76** = 高出池均值 66.09 只有 **1.0 %** ⇒ 按"高出中位 ≥1.5 % 才补枪"**不补**）⇒ 榜上 `21.97` / `#37`，发次 `6ab495b1694b590c3cd911a7`（P120 探针读完立刻回发）；历史最高仍是 `22.29`/`#33` 那一遍。🔴 **"追档零收益"已作废**（池撑到 12 遍、全距 **3.9 %** 之后实测抓到一次）：`ccb6da4` Σ 68.06 → 榜上 **21.7**，同一份字节立刻再发 → 65.66 → **22.04**（**两发之间代码逐字节相同**）。⇒ 新规矩：**现挂 Σ 高出池中位 ≥1.5 % 就补一枪回发**，这是白捡的分 |
| **历史最高** | **`22.29` = 同一份 P105 字节的第三遍读数**。**同字节池现已 14 遍**：Σ 均值 66.09 ms、全距 **65.50 … 68.06 = 3.9 %**（⇒ 旧的"0.6 %"是 9 遍时的欠采样），榜上真值散在 **21.7~22.3** ⇒ "最好档位"与"现挂"仍是同一份代码、差的是抽卡，但**抽卡的幅度是 ±2 %、值 ±0.4 分，不是可以忽略的量**。⚠️ **逐点比 Σ 更不稳**（§1.2 那行五遍全距的口径，`LOG#15.96` 用**相邻两遍**同字节回发复算 = `−3.1/−5.3/+2.4/−4.4/−5.2/+10.0 %`、Σ 只差 0.3 %）⇒ **单点时间带 ±5~10 % 噪声，只有六点合读数可用作参照** |
| 正确性 | ✅ **当前工作树 = 已验证 + 已计分的 P105 四枚**。全表绿凭据 = `probes/p115_gate.txt`（探针态：split=2、13 真 expect × 双 dtype 全 `超差 0` + 对**耐久** golden 逐位一致 + GATE2 14 档全 0）与 `probes/p112_gate.txt`（P105 字节态）。⚠️ `big1` 的"对 golden 非逐位"是既有档案行，判据看 `超差` 不是 `PASS`；`w3/w4/p1s*/p4s2/pm*` 那批 expect 是生成器故意填的**哑零** ⇒ 只读时间，报"超差 22 万"**不是回归**（§7.3） |
| 工作树 | ✅ `code 3/code/` = **P105 干净字节**（kernel `f815bf1e…`/68,923，与榜上同一份；P115 探针态发完后已由 `p115_reissue.sh` 还原）。探针字节只在远端副本上由 `probes/p115_split.py`（条数 ×N）/ `p114_gather_rep.py`（字节 ×2）/ `p110_score_rep.py`（计算 ×N）从 pristine 现生成，⛔ 不在本地树、⛔ 不叠补丁。**cube 线（M1d → A′ → M2d）的全部字节也只活在 `code 3/probes/backup/`**，且 `probes/LOG.md` 与 `p9*`/`p10*`/`p11*` 探针 runner **全是未跟踪**（`??`）⇒ 一次 `git clean -fd` 就把 9/23~9/24 的证据档案和 runner 一起删掉。⛔ 别在这上面跑 `checkout/clean/stash` |
| **提交包状态** | ✅ 9/24 六轮 dry-run 四枚与本地逐个吻合 = host `3a8f53050c8f15ae…`/36,758 B、kernel `f815bf1eaba0f8bc…`/68,923（P115 探针态那发 = `34c24d9e1e020390…`/70,316）、tiling.h `f2b28a86e998c194…`/4,149、tiling_key `1046b349538f3007…`/460。⛔ **文档里任何哈希都按过期处理**，判定只认当场 `sha256sum`（§7.7） |
| **设备 / 发次通道** | ✅ 探针/判死类发次**可以连发**：`LOG#15.79` 那条"一次挂死会毒化设备 ≥6 min 不自愈"归因错了（真相 = 可自愈的进程/stream 残留上下文，`LOG#15.82`）。🔴 唯一的新坑：**连发两发会撞平台 `HTTP 429`**（9/24 实测）⇒ 退避 ~4 min 重发即通，不是额度问题。✅ 实测**同一天连发 4 发全 `Pass`**（含两次回发）⇒ "发次预算"从来不是约束，约束是每次发次要占一段设备 |

### 1.1 一句话状态

**榜上现挂 = P105 字节第 14 遍**（21.97 / #37；14 份同字节 Σ 池 65.50…68.06、均值 66.09 ⇒ 榜上任何一档都是抽卡，只有落在池外 ≥1.5 % 才补一枪回发），距榜首 **3.6~4.8×**。五条轴全部读完，结论就地进 §5 排除清单，这里只留口径：

- **形状轴焊死**：`sbs` 六点全 = 1（P111，级间距 ≥15 pp 而合读数 −1.1 %）⇒ cube 线**无条件死**（#14）；`B·Q_S` 六点全 ≥ 41（P108，6~9σ）⇒ `kv_shard` 深度轴与平台分数**永久无关**（#17）。选档四轴（`nb`/`n_blk`/`ks`/run-merge）里三轴实测无收益（#4、#15、#18）。
- **段占比轴 = 镜像**（P112/P114/P116/P119 四把"工作"尺子 + P120 第一把"开销"尺子）：`ComputeScores` 翻倍本地 **+44~50 %** / 平台 **+6.1 %**，三条 `DataCopy` 翻倍本地 **+4~6 %**（下界）/ 平台 **+17.8 %** ⇒ 本地"搬运∶计算 = 1∶10"、平台"= 3∶1"；但 `SoftmaxPv` 前段翻倍**两侧同型**（本地 +2.2~2.7 / 平台 +2.4 %）⇒ "镜像"是**搬运∶计算**那根轴特有的，不是普遍规律。⇒ ① **向量线整条关死**：score / softmax 前段 / PV（P119）三段平台上合计 **≈11 %**，本地价是它的 3~10 倍（#19、#24）；② 逐点 Δ 两端差 ≥20 pp 是真的，但**中间点在 ±10 % 噪声内不可分**（#20，`LOG#15.96` 用相邻两遍同字节回发把这条噪声钉住了）；③ 能外推到平台的只有**形状轴**，不是**段占比**。⚠️ run-merge 那笔 P115 改口**已就地结掉**（#16）：卡它的反证查明是误读一根轴当另一根，天花板 ≈3~4 pp、测不出来。

**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + PV ≤3 % + 搬运 18 % = **≈29 %** ⇒ **≈71 % 无价**，3.6× 藏在这块里。🔴 **候选集只剩"开销"一类里的两条**：干活的四段全部量完且全部 ≤18 %，而**同核旗标对也价完了**（4 对/chunk ≤0.08 % of wall）⇒ 没被尺子碰过的只有**标量下标扫描**（P121）与**跨核 barrier**。9/24~9/25 那五发"去拿它"：一败 + 四读数（后两发是否证）：
- ❌ **P113（本地代理族）判废**（细节 `LOG#15.94`、判决 §5 #21）：12 档三臂无一同时复现"计算 ≈6 % ∧ 搬运 ≈18 %"，且动比值的轴是**每 token 头数 `N1`**、不是冷度/复用/形状大小 ⇒ **计算∶搬运这根轴上没有离线代理**。副产品 = 一条与冷热无关的 456~496 GB/s（≈12 GB/s/核）gather 请求吞吐墙。
- ✅ **P115（条数 vs 字节判别器）有答案**（`LOG#15.95`）：同字节、每条 gather 拆两段 ⇒ 命令数 ×2，平台六点合读 **+12.9 %**，对 P114 的 +17.8 % ⇒ **平台 gather 里"每次调用"侧 ≈72 %、"字节本身" ≈28 %**（⚠️ 72 % 是上界：拆半同时砍了每条字节数；⚠️ 且这两个读数只差 4.9 pp = 单遍合读噪声 ±2 % 下的 **1.7σ** ⇒ 只当"调用侧 ≥ 字节侧、量级 2.5∶1"用，**别当精确分解**）。⇒ "每条命令覆盖更多字节"那一侧上限 ≈2.2 分；而"每 token 少搬字节"那档架构题先被降到 ≤0.9 分、**当天直接被 P118 降到 0**（§5 #23）。⛔ 但**别立刻开工 run-merge**：`p111_ladder.txt` 等字节下命令少 128× 反而慢 2.7×，那条自相矛盾没解释之前它动的不是一个变量。
- ✅ **P118（冷暖镜像）有答案，且是否证型的**（`LOG#15.97`）：与 P114 同条数、同每条字节、只差行号（伪随机冷行）⇒ 平台 **+18.2 % vs 暖的 +17.4 %**，**冷溢价 +0.79 pp 落在噪声里**，而同一对双臂在本地是 **2.8~3.4 倍** ⇒ **"平台六点住在 HBM 那一侧"正面判死**，"少搬字节"整条架构线（跨行去重 / KV 常驻 / 粗化 over-fetch）无条件关死。（这一发把同字节池撑到 12 遍 ⇒ 口径见 §1.2）
- ✅ **P119（PV 段定价）答案是负的**（`LOG#15.98`、`p119_platform.txt`）：复制体目的端 = `kfBuf_` 末行 ⇒ 逐位惰性、**不新增 UB 分配**；本地四臂在复制份数上**严格线性** ⇒ 尺子可信，本地 PV×2 = **+18~33 %**，平台 = **Σ +2.9 %**（±2 % 带内 ⇒ 判不出非零）。🔴 **但"判不出非零"足以关闭它**（§1.2 新口径）：复制只加长度、不加真实依赖链 ⇒ Δ 小 = 这段躲在别人影子里 = 删它也省不出超过 Δ ⇒ 向量线不再发探针。
- ✅ **P120（第一把"开销"尺子）答案是否证的**（`LOG#15.99`、`p120_platform.txt`）：`FlushChunk` 尾追加 127 对**未用过**的 `SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 零内存、零 UB、零依赖 = **构造级惰性**（前六把都得论证"这块内存没人读"，这把不用）；本地每对 0.03~0.06 %、平台 127 对 = **Σ +2.42 %** ⇒ **每对 ≤0.019 %、4 对/chunk ≤0.076 %**。🔴 且这一发的 Σ 67.64 **比同字节池内最高 68.06 还低 0.62 %** ⇒ 不是"小于门"而是**被零假设整个盖住** ⇒ **减旗标对数判死**（§5 #25）。附带：**平台上旗标比本地便宜 1.3~3.2 倍**（与 P115"调用条数在平台上更贵"不矛盾 —— 每 chunk 耦合 vs 每 token 调用）。
- 🔎 **下一发 P121 = 标量下标扫描计量器**（两条剩候选里的第一条，也是 §4 里唯一还挂在"每 token 乘一次"那一侧的价：38.6 ns/条 × 每 query 约 6.1 万次标量读）。⛔ 不能用"原地重跑"—— 扫描推进迭代器状态 ⇒ **不幂等**；可行形态 = 额外读 N 条下标进丢弃值，而那在 `-O2` 下有被 DCE 的风险 ⇒ **必须先本地两臂标定看 Δ 是否随 N 线性**（P119 的做法），不线性 = 尺子根本没生效。判据按 §1.2 单向口径：带内 ⇒ 只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB。⛔ P117 继续降级。

### 1.2 分数怎么算（做决策用的唯一一把尺子）

- **`score ≈ 3.71 + 12.29 · Σᵢ(tbestᵢ / tᵢ)`**，R²=0.9787（n=41 全量榜反推）。回代自己两发：P38 预测 22.42（实 22.19）、MIX 探针 20.4（实 20.90）⇒ 本段位**这把尺子准**。榜首预测偏低（73.4 vs 80.36）⇒ `tbest` 大概率不含提交者自己。
- ⇒ **每快 1 % ≈ +0.19 分**。**噪声的实测锚点**：P105 字节五遍（榜上真值 **21.73 / 21.92 / 22.29 / ≈22.0 / 22.21**）+ P111 探针那一发（`sbs` 阶梯在六点上是级 0 ⇒ **执行路径与 P105 逐字符相同**，≈21.9）= **六份同路径样本，全距 0.56 分、均值 ≈22.0**；P38 字节两遍 = 22.19 / 21.93。同六份的 ruler = 20.82 / 21.11 / 21.35 / 21.36 / 21.52 / 21.63 ⇒ **与真值系统差 +0.5~0.7 分、单调性完好：尺子只管排序，别拿绝对值当分数读**。逐点全距（五遍）：`C1 5.5 %`、`C2 3.8 %`、`C3 5.8 %`、`C4 12.1 %`、`C5 4.9 %`、**`C6 15.6 %`** ⇒ ⛔ **单遍读数不能当参照，判收益至少夹一遍同字节取均值**；六点合分只动 ±0.3~0.6 分 ⇒ **≥8~10 % 的系统性收益在平台上是可判的**。🔴 **但这条"合分稳"本身在 9/24 被撑宽了**：池从 5 遍涨到 **14 遍**后 Σ 全距 = **3.9 %**（65.50…68.06、均值 66.09，最差一遍榜上 21.7、最好 22.2）⇒ **Σ 的噪声带按 ±2 % 用**（旧口径 ±0.6~0.9 % 是欠采样），换算：**≥4 % 的系统性收益可判、两发单遍探针之间 <5 pp 不可判**（这条直接判了 P115 那个 72∶28 的精度、也决定了 P119 必须打**大段**而不是打 2~3 % 的小段）。⚠️ 反向也成立：**判别器探针必须做到 ≥4× 噪声**才够格下结论 —— P107（签名 +17~28 %）就不够，P108（+45~71 % = 6~9σ）与 P111（级间距 15~20 pp + 六点同号）才定了案（`LOG#15.87`→`#15.91`）。🔴 **但"带内 = 判不出"只对一半**（P119 教的）：判杠杆**打开**要 Δ ≥ 6 %，判**关闭**不需要 —— 复制体只加长度不加真实依赖链 ⇒ Δ 小的两种成因都意味着"删它省不出超过 Δ"。⇒ **噪声在否定方向上单向可用**。🔴 **P120 给出这条的最强形式**：探针 Σ 若**落进同字节池的分布内部**（甚至低于池内最高）⇒ 那**不是"小于某个门"，而是被零假设整个盖住** ⇒ 一发关闭、不补枪。
- **要 40 分 ⇒ Σ = 2.95 ⇒ 六点平均每点再快 ≈2.03×**。全服最优 `tbest = [2.18, 2.30, 2.44, 3.00, 2.72, 3.68]`，榜首六点 `[2.18, 2.76, 2.64, 3.00, 2.92, 3.76]` = 80.36。我们六点 `[7.78, 8.60, 11.06, 12.30, 10.72, 13.74]` ⇒ **每点差 3.6~4.8×**，且榜首自己的散布只有 1.72×（我们 1.77×）⇒ 差距是**截距/单价**型的，不是"某一点特别慢"。
- **形态对照（当前口径）**：平台六点 = **`B·Q_S ≥ 41`（P108，6~9σ）** + **`sparse_block_size = 1`（P111，六点级 0、合读数 −1.1 %）** + fp16 / `MODE=3`（这两条来自 `LOG#15.80` 的反解，未单独复核）。`Q_N` 那条**不用再问**：它唯一的作用是决定 `CubeBlock(qN)` 进不进得去，而 `sbs` 已经把 cube 的门焊了。⛔ "decode 形 `rows`=4/8/4/16/4/32、每行几十万 token" 作废。**每行 token 数与 `S2`（KV 总长）仍未实测** ⇒ 而这正是"六点到底走不走 HBM"的分界，见 §1.1 末尾与 §6 P0。
- **性能靶 = `sbs=1 ∧ rows ≥ 41` 那一族**：现成档里 `big1`（`S1 128·N1 8·S2 8192·sbs 1`，0.67 ms）与 `p6`（`S1 32`，0.75 ms）在 `sbs` 上对得上平台，`w3/w4` 是 `sbs=2` ⇒ **只能当"同一构建两臂差分"的靶，不能当"绝对速率外推到平台"的靶**；`p4s1c*`/`p16s1c*`/`p32s1c*` 降级为 `rows≤32` 的历史靶。缺口 = `rows 64·128 ∧ sbs=1 ∧ S2 大到 KV 装不进缓存`（⇒ §6 P113 去造）。

### 1.3 本地读数（cube 线终局 + 唯一没被平台复量过的两套模型）

- **cube 终局网格**（`p103c_grid.sh`，一份 A′ 字节两臂只差 `CUBE_MIN_WAVES_PCT`）：唯一赚的形态 = **cube 单元 ≥16 ∧ `sbs ≥128`** ⇒ 比值 **0.530~0.538**；`sbs=1` 时即使单元够也只 0.947~0.952（±5 %）；对照组（门自动关）= 0.999 ⇒ 差确实只来自"走不走 cube"。`LOG#15.91`（六点全 `sbs=1`）之后**整条线无条件关死**，这些数只当历史价目。
- **AIV 吞吐模型（`LOG#15.85`）**：同构建、每行恒 4096 token、`sbs=1` ⇒ `rows` 4/8/16/32/40 = **0.3151 / 0.4646 / 0.7659 / 1.4855 / 1.4880 ms**。`rows` 32→40 时间**不涨**（核数 32→40）⇒ **聚合吞吐随核数近似线性、没撞到 aggregate 墙**；不重复搬运那族稳在**每核 ≈6.0 GB/s**，靠复用的"一头一单元"那族局部 **14 GB/s**（"L2 兜住多出来的 3 份"是推断、没量过）。⚠️ **适用边界（`LOG#15.93`）**：整套在 `S2 8192`（KV 17.8 MB = 缓存兜得住）上量的 ⇒ **不可外推到平台**（⚠️ 原括号里"六点明显在搬运/HBM 那一侧"是 P114 时代的推断、**P118 已否证掉 HBM 那一半**：搬运确实占 18 %，但它不在冷层，§1.1），可外推的只有"单价两段式 92 ns/token + 67.5 ns/头"与 `sbs`/`rows` 两条形状轴。
- **老单价仍然有效**（§4）：AIV `2.30~2.32 ns/ht`、cube `1.056~1.084 ns/ht`（对头数平坦）、碎拷贝 ≈50 ns/条与 `nValue` 无关、AIC 计算段 ≤6.6 % of wall（🔑 **AIC 的墙是 gather 调用条数不是 Mmad**）、每 chunk 固定税**饱和**（`16→32` +43~63 %、`32→64` +11.7~14.5 %、外推 `64→128` 只剩 6~9 % ⇒ 宽 chunk 判死，§5 #4）、**`SoftmaxPv` 前段（行最大 + exp + ΣP）×2 = 本地 +2.2~2.7 %（跨 45 倍形状只散 0.5 pp）/ 平台 +2.4 %** ⇒ 全平台唯一两侧同型的计量器（`LOG#15.96`）。⑤ **`gather` 的冷暖价**：同一批行换成伪随机冷行，本地 **2.84×（`p6`）~3.37×（`big1`）**、平台 **1.00~1.04×** ⇒ **"冷∶热"这个比值本身就是"在不在缓存里"的判别器**，而平台六点读出来是 1（`LOG#15.97`）。

---

## 2. 输入契约（**动手前先看这张表**）

### 2.1 张量清单

| 张量 | 必选 | 形状 | dtype |
|---|---|---|---|
| query | 必 | `(B, Q_S, Q_N, Q_D)`，`Q_D=512` | fp16 / bf16 |
| key | 必 | `(B, KV_S, KV_N, Q_D)`，`KV_N=1` | fp16 / bf16 |
| value | 必 | `(B, KV_S, KV_N, Q_D)` | fp16 / bf16 |
| sparseIndices | 必 | `(B, Q_S, KV_N, sparse_size)`，题面仅要求 `sparse_size > 0` | **int32** |
| actual_seq_lengths_query / _kv | 可选 | `(B,)`，None → 用 `Q_S` / `KV_S` | int32 |
| queryRope / keyRope | 必 | `(B, Q_S, Q_N, 64)` / `(B, KV_S, KV_N, 64)` | fp16 / bf16 |

**输出**：`attentionOut` `(B,Q_S,Q_N,512)` 与 query 同 dtype；`softmaxMaxOut` / `softmaxSumOut` = **`(B, KV_N=1, Q_S, Q_N)`、固定 float**（轴序 `(B,1,Q_S,N)`）。
**属性**：`scaleValue` = `1/√512 ≈ 0.044194`（接口传 double，内部按 fp16 精度处理）；`sparseBlockSize` ∈ `[1,128]` 且为 2 的幂；`sparseMode` `0`=不屏蔽 / `3`=rightDownCausal；`attentionMode` 仅 `2`；`returnSoftmaxLse` 默认 False。

### 2.2 官方约束（**优先信这张表，不要信自己的假设**）

| 约束 | 值 |
|---|---|
| `qk_head_dim` = `v_head_dim` | **必须 512**；`rope_head_dim` **必须 64**；`n2`（KV head 数）**必须 1** |
| `Q_N` | 950PR/950DT 支持 1~128；**Atlas A2/A3 仅枚举值 `1,2,4,8,16,32,64,128`（离散，非连续范围）** |
| layout / 数据格式 | 本题 **仅 BSND + 仅 ND** ⇒ 过滤掉 `PAGE_ATTENTION` / `FLASH_DECODE` / `TND` / `V_TEMPLATE` |
| dtype 一致性 | query、key、value **必须一致**；RoPE **必传不可为空** |
| 特殊值 | 不支持空 tensor、**不支持非连续** |
| `pre_tokens` / `next_tokens` | 仅支持默认最大值 `2^63-1` |
| 属性默认值（OpDef 实测） | `scale_value` **`0.0884`** ≠ 官方 `1/√512`（**恰好 2 倍**）。本地测不到（用例显式传值）⇒ **已知隐患，不是已排除** |
| `sparse_size` | 题面只写 `> 0` ⇒ **视为可变**（"固定 2048"那条旧记录作废，但本地语料全是 2048） |

### 2.3 ⭐ 四条最容易搞错的语义

1. **`sparseIndices` 是 KV 块号，不是 token 位置**：`blk ⇒ token ∈ [blk·sbs, blk·sbs+sbs)`，再与 threshold 取 min 截断。
2. **哨兵 `-1`，遇到即停**（不是跳过继续）；官方只判 `-1`。⚠️ 官方在 `begin >= threshold` 时用 `continue` 而非 `break` ⇒ 语义上**依赖索引有序性**。
3. **因果掩码（`sparseMode=3`）**：`threshold = (actualS2 − actualS1) + s + 1`，可见 ⟺ `t < threshold`。`sparseMode=0` ⇒ `threshold = actualS2`。⚠️ 展开 token 时按 `threshold` 截断**就已经实现了掩码**，不需要额外逐元素比较。
4. **变长语义（BSND）**：`arr[b]` per-batch；数组长度为 1 时**广播** `arr[0]`；未传用满 `Q_S`/`KV_S`。（**TND 下才是累积前缀和** —— 本题不用。）

### 2.4 LSE 口径

| 场景 | `softmaxMaxOut` | `softmaxSumOut` | `attentionOut` |
|---|---|---|---|
| 正常行 | ⭐ **未缩放**的行最大 | `Σ exp(s − max)` | 归一化加权和 |
| 全 mask 行（`s ≥ actQ` / `thr ≤ 0` / `actKV == 0`） | ⚠️ **比赛平台期望 `0.0`**（本地旧参考用 `-2e38`，`sfa_ref.py` 已同步） | `0` | **全 0** |

⭐ 必须写**未缩放**的行最大 —— 真机双对拍：同一用例 `sum` 全对、`max` 恒差 22.6274 倍（= `1/scale`）。⚠️ 官方题面 §4-3.2 与旧 `SEMANTICS.md` 都写"已乘 scale"，**已被真机推翻**；题面示例代码把索引当 **token 位置**也是错的（正式口径是 **KV 块号**）。⚠️ `refs/sfa/sfa_ref.py` 里仍留着 `lse_scaled=True` 的旧开关 = 已知隐患。

---

## 3. 提交清单 / 陷阱 / 闸门

| # | 提交路径（相对 `code/`） | 是什么 | 开发侧文件名 |
|---|---|---|---|
| 1 | `op_kernel/sparse_flash_attention.cpp` | **kernel 本体** | `sparse_flash_attention_kernel.cpp` |
| 2 | `op_kernel/sparse_flash_attention_tiling.h` | tiling 结构体 + 常量 | 同名 |
| 3 | `op_kernel/tiling_key_sparse_flash_attention.h` | tiling key | 同名 |
| 4 | `op_host/sparse_flash_attention.cpp` | **host tiling** | `sparse_flash_attention_host.cpp` |

外加三个 `CMakeLists.txt`（`code/`、`code/op_kernel/`、`code/op_host/`）。题目 ID `6a7c22d6a52e0f540a8a098d`。

**⛔ 陷阱**：① `op_host/` 与 `op_kernel/` 下**各有一个同名 `sparse_flash_attention.cpp`**，传错必挂；② ⛔ **永远不从 `code 3/submit/` 提交**（9/19 手抄快照，还留着 BF16 与 `-2e38`）；③ **文档里的哈希一律不可信**，判定"是不是验证过的那份"只认当场 `sha256sum`；④ SoC 双注册（`ascend910b` → `ascend910_93`）**只存在于远端副本**，提交源里必须 grep 命中 0。

**提交前必做（顺序固定）**：

```bash
bash "code 3/npu_debug/npu.sh" sync && bash "code 3/npu_debug/npu.sh" build
bash "code 3/probes/p32_gate.sh"         # ① 数值闸门，必须全表 超差 0（存成 probes/*.txt 当凭据）
bash "code 3/npu_debug/npu.sh" sync      # ② 所有探针跑完后【最后再】sync，此后不再 build
# ③ 核四枚当场 sha256；grep -c ascend910_93 == 0；禁用词 grep == 0
# ④ submit --dry-run（四枚必须与本地逐个吻合）→ ⑤ submit --max-wait 480
# ⑥ 若这一发是探针/判死性质 ⇒ 回发当前最好的字节（latest 计分，别让榜上挂着探针）
```

- **数值闸门 = `bash code 3/probes/p32_gate.sh`**：GATE1 13 案 × 双 dtype（`超差 0/N` + 逐位一致或与已登记末位差一致）、GATE2 14 案 × 双 dtype（`超差 0/N`）。判据是 `超差`（绝对+相对双阈值），**不是** `run.sh` 的 `PASS/FAIL`。
- ⚠️ **别把远端 `run.sh` 的 `PASS=8 FAIL=0` 当判据**：它调 legacy `./test_sfa`，判据是**纯相对误差**，于是 fp16 次正规区一个 ULP（`2^-24`）就被报成"最大相对 6e-02 / 超差 1/N ⇒ FAIL"（`r2_chunk` 至今如此）。机理见 `LOG#15.63`。
- **发次纪律（真踩过）**：探针 runner 的 `trap` 用 `npu.sh build` 还原，而 **build 会往远端 `op_host` 副本重打 SoC sed** ⇒ 还原后的远端 host **已经不是提交字节**。
- **提交链路**：真机 CLI `cannjudge_cli.py`（`/mnt/workspace/gitCode/cann/cann-learning-hub/skills/cannjudge-submit/`）。⚠️ `--ciphertext` 是**本地**用配对私钥解密再 POST ⇒ 只有公钥+密文登不进；`login()` 拒绝非交互终端 ⇒ **口令那一下永远是用户动作**（`~/sfa_login.sh` 用 `read -s`，Agent 不接触明文）。会话 `~/.cannjudge/session.json` 有效期内 submit/榜单 API 全可由 Agent 跑。CLI 自带的 `rank` 命令打的接口**服务端 404** ⇒ 看榜用 `GET /api/problems/{pid}/ranking?page=N`。

---

## 4. 机器事实与单价表（**决策只引用这张表**）

| 项 | 值 | 出处 / 备注 |
|---|---|---|
| UB | **196,352 B**（预算取 95 % ⇒ 186,534，host 实查 `GetCoreMemSize`） | `LOG#15.75(a)` |
| L1 | **524,032 B**（cube 路径现在只用 90 KB） | 同上；"L1=512 KB 未验证"那条老风险结案 |
| L0A / L0B | 各 **65,536 B** | L0B 在 `n_blk=128` 时**正好用满、零余量** |
| L0C | **131,072 B**（= 16 行 × 2048 列 fp32） | ⛔ `tiling.h` 旧注释"L0C 只有 64 列 ⇒ Mmad 的 n 最大 64"**是错的**（M2d 那轮已订正）：我们整个 cube 线只用 4 KB = **3 %**；`16×512` 的 fp32 累加器（32 KB）**装得下**。⚠️ 但**单条 Mmad 允许的 n 上限仍未测** ⇒ 别反向读成"n 可以随便抬" |
| 逐条 DMA `DataCopy`（碎拷贝） | **AIC ≈50 ns/条，与 `nValue` 无关**；AIV 侧本地实测 **8.0~12.3 ns/条**（P115：条数 ×2、字节不变，40 核均摊） | `LOG#15.70`、`#15.94`。⇒ **条数确实是成本单位**，⚠️ 但干净的条数轴只有 **P104 合并阶梯**（只改邻接 ⇒ 单调 5.2 ns/条）与 **P115**（平台 ×2 条 = +12.9 pp）。🔴 **`p111_ladder.txt` 不得当"并条不省"的证据**（`#15.98` 结案）：那条 sbs 阶梯上"游程条数"随 `sbs` **单调降**（一个 48-token chunk 跨过的表项数 `s4`→`s128` 约 12→1）而时间**不跟着降**：`s8`→`s16` 条数减半时间只动 0.6 %（0.2977/0.2959 ms），`s64`→`s128` 条数更少反倒慢 2×（0.4205/0.8212）⇒ 条数若为主因，时间该单调降；它没有 ⇒ 它动的是别的东西 |
| `Fixpipe` | ≈0.7 µs/条 | 同上 |
| 标量 **GM** 读 | ≈38.6 ns/次 | 同上；P21 只把 21 % 的预测收益藏进了向量流水 |
| 标量 **UB** 读 | ≈**97 ns/次（比 GM 还贵 2.5×）** | ⇒ "把表搬进 UB 再逐个 `GetValue`"在本机是**反模式**（P20 死在这条上，+76 %）。🔴 **`SoftmaxPv` 第 5 段每 `Axpy` 就带着一条 `p.GetValue`** ⇒ 那"64 条向量 + 1 条标量读"的混合体已由 P119 **整体**定价：平台翻倍只 **+2.9 %**（本地 +18~33 %）⇒ **含标量读在内这一段都不是杠杆** |
| `gather` 的**冷暖** | 本地冷∶热 **2.8~3.4**；平台 **1.00~1.04**（冷溢价 ≤ +0.8 pp，在 ±2 % 带内） | `LOG#15.97` ⇒ **平台侧的 18 % 里没有"访存层"这一项**；⛔ 别反读成"缓存无关紧要"，本地它是要紧的 |
| 旗标一轮（**跨核** `CrossCoreSetFlag` + `Wait`） | ≈0.42 µs/轮 | 每 chunk 固定税的成分；该税**饱和**（§1.3）。🔴 **同核 `SetFlag/WaitFlag` 一对已平台价（P120）**：127 对 ⇒ +2.42 % ⇒ **每对 ≤0.019 % of wall**、现有 **4 对/chunk ≤0.076 %** ⇒ 全删只值 +0.02 分（§5 #25）；且平台上比本地**便宜** 1.3~3.2 倍 |
| 串行标量循环里加一个分支 + 一次调用 | +8~14 µs/单元 | |
| AIV 路径单价 | **2.30~2.32 ns/ht**；其中 score 51~55 % + PV 31~34 %（= 每 ht **64 条 fp32 `Axpy`** + 12 条 exp/max/sum/环读回 ≈ 76 cycle）⚠️ **这个分解只在本地成立**，平台侧 score 段实测 ≈6 %（下一行） | `LOG#15.73(o)`、`#15.76`、`#15.92` |
| cube 路径单价 | **1.056~1.084 ns/ht**，对头数平坦；AIV 计算段占墙 ≤6.6 %（`w3`，两个大形态测不出非零） | §1.3；`LOG#15.76`（cube 臂，随该线作废只作历史） |
| **decode 单价两段式**（新原语，五点头行数 ≤4 % 咬合） | **每（单元·token）= 92 ns 固定段（搬 2 176 B）+ 67.5 ns × (头数-1)** ⇒ `nb=1` 153、`nb=2` 227、`nb=4` 362 ns/token | `LOG#15.85` ⇒ ⚠️ 口径：**50 ns/条是 AIC 的 gather**，AIV 侧实测 **5.2 ns/条**；对头数**是**敏感的（每头 +67.5 ns） |
| AIV 聚合吞吐 | 每核 **6.0 GB/s**（不重复搬的那族）⇒ 32 核 192 GB/s、40 核 240 GB/s ⇒ **随核数线性，没有 aggregate 墙**；`rows` 4~32 那几族本来就在 **32 核**上（`nb` 已把头拆开） | `LOG#15.85` + `#15.34(b)` ⇒  |
| **`kv_shard` 深度轴（P105 + 折叠税）⇒ 本地资产、平台上无落点** | 同构建扫 `(nb,ks)`：**1.35~1.42×**（`p1` 0.1602→0.1188、`pm41` 1.41×），但⛔ **全是 `rows ≤ 20` 的档** ⇒ P108 证六点全在 `rows ≥ 41` ⇒ 与平台分数**永久无关**，字节留在树里。折叠税是**首轮 11.6~17.1 µs、此后每轮边际 3.4~8 µs**（不是常数，反解见锚点；payload ×2 只多 28 % ⇒ latency 主导）。精度：27 档 × 双 dtype 全 `超差 0` + 逐位一致（`ks` 到 10、链上 9 次 fp16 往返） | `LOG#15.86`（`p105_grid.txt`、`p105_gate.txt`）+ 平台侧作废 `#15.88` |
| **AIV 的墙有两块：吞吐 + 依赖链延迟**（P110/P110b 三臂惰性消融，⚠️ 本地口径） | `ComputeScores` 整段重跑 1/2/4 遍 ⇒ **计算段 ≈51 % of wall 且线性**（w3 5.44→8.18→13.83 ms）。但"**减**工作"不是 1:1：重复的 `WidenToF32` **跳 3 遍只省 5.0~5.5 %**、**多插 1 遍要多付 7.2 %** ⇒ 一遍的吞吐份额 **W ≈ 1.85 %**，插链头付的是延迟 ⇒ 微优化方向 = **断依赖链**不是少算 | `LOG#15.90`（`p110_aivk.txt`、`p110b_widen.txt`）⇒ 提加宽出循环上限 5.5 %；⛔ 别把"线性"读成"已到 SIMD 吞吐墙"（§5 #19） |
| **墙组成 = 镜像（两个惰性计量器，本地/平台读反）** | 同一机制（原地重跑 ⇒ 逐位惰性）：`ComputeScores` 翻倍 = 本地 **+44~50 %** / 平台 **+6.1 %**；`K/kr/V` 三条 `DataCopy` 翻倍 = 本地 **w3 +3.66 / big1 +4.48 / p6 +6.14 %**（⚠️ **下界**：第二遍搬的 GM 行已在 L2 里）/ 平台 **+17.8 %** ⇒ 本地"搬运∶计算 ≈ 1∶10"、平台"≈ 3∶1" | `LOG#15.92`（`p112_platform.txt`）、`#15.93`（`p114_calib.txt`）；分解见下面两行 |
| **`nb` 选档在正确靶族（`rows ≥ 41`）上的误差** | 同构建夹 `SFA_FORCE_NB` 扫 `{1,2,4,8}` + 不夹的 `auto`：`w4` auto 9.5277 = `nb=4` 9.5273；`big1` auto 0.6700 = `nb=4` 0.6702；`w2`/`p32s1c16384` 同样逐位吻合；唯一偏差 `w3` auto(`nb=4`) 5.4435 vs `nb=2` **5.3228 = 2.2 %**。`nb=8` 在 `qN=8` 两例上**反而慢 10.7 %/6.4 %**，pick 里写着原因：`n_blk` 被 UB 从 **48 挤到 40** | `LOG#15.89`（`probes/p109_nbgrid.txt`）⇒ **选档没有收益可拿**（0.4 分以下、且不保证六点同型） |
| **平台 gather 的内部分解：每次调用侧 ≈72 %、字节本身 ≈28 %**（P115 判别器，本题最后一个"方向题"） | 设 gather = `a·条数 + b·字节` ⇒ P114（两条都 ×2）= `a·N+b·B` = **17.8 pp**、P115（同字节、条数 ×2、逐位惰性）= `a·N` = **12.9 pp**（六点合读；八遍同字节池算出来 12.95 % ⇒ 对参照池不敏感）⇒ 比值 **0.72**。⛔ "平台 gather 是带宽/字节限制"被**排除**；绝对上限：调用侧 ≈2.2 分、字节侧 ≈0.9 分（都是"整段归零"的天花板）。⚠️ 口径：拆半同时把每条字节砍半 ⇒ 72 % 是**"每次调用侧因素"的上界**（含短拷贝效率损失）、28 % 是字节侧**下界** | `LOG#15.95`（`p115_platform.txt`、`p115_gate.txt`、回发 `p115_reissue_run.txt`） |
| **N1=1 族的 gather 请求吞吐 = 结构墙 456~496 GB/s**（≈12 GB/s/核、≈175 ns/token、≈58 ns/拷贝命令当量） | 与**复用度无关**（0.5×→64× 全在同一条线上）、与**冷热无关**（KV 池 17.8 MB 热 vs 285 MB 冷）⇒ 不是 HBM miss，是**我们自己 gather 路径的上界**。⚠️ 推论（未消解）：平台若同墙，则它 7.8~14.8 ms ⇒ 3.7~7.1 GB 请求字节，榜首 2.16~3.54 ms **只能靠"少搬字节"**（跨行去重 / KV 常驻）解释 —— 但现在 P115 说字节只占 28 % ⇒ 这条推论**降级**为"至多 0.9 分的一条路" | `LOG#15.94`（`p113a/p113b_run.txt`）+ `#15.95` |
| **平台的"镜像墙"在本地造不出代理** | 12 档三臂（base / 计算 ×2 / 搬运 ×2）没有一个达到判据（Δ计算 ≤10 % ∧ Δ搬运 ≥14 %）；本地比值最低 **1.05** vs 平台 **0.34**。动比值的是**每 token 头数**（`N1` 1→2 ⇒ 1.46→3.45），**不是冷度/复用/形状大小** | `LOG#15.94` ⇒ 后果：**"改架构前先本地筛"这条纪律在 gather/搬运轴上没有可用的离线代理**，方向判定只能靠平台发次（一发 ≈ 一次判别器） |

**一句话总结这张表**：⚠️ 两侧不同型 —— **本地**的墙是向量计算段（score ≈51 %）+ 依赖链，搬运只值 4~6 %；**平台**正好反过来（计算 6 % + softmax 前段 2.4 %、搬运 ≥18 %）⇒ 平台的墙是"**搬运条数 + 标量读 + barrier 轮次 + 每 chunk 固定税**"，而那几样加起来还剩 ≈74 % 没有被任何已测机制定价。🔴 P118 + P119 之后这张"候选清单"划掉两项：**"访存层/HBM"**（冷溢价 ≈0）与**"带累加器的向量段 PV"**（翻倍只 +2.9 %）。⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销两类**：标量下标扫描（P121）/ 跨核 barrier（同核旗标对已价、≤0.08 % ⇒ 出局）。P113 已把"搬到本地来筛"这条路**试完并判废**（12 档无代理），所以剩下的定价**只能一发一发在平台上买**。⇒ "把 N 条调用并成 1 条"要按域读，且**不得当成必然有收益**（见上表碎拷贝那行的反证）。（§5 #1"可写 GM 只有本单元那一块输出行"仍然成立，只是随 cube 线作废而不再是钳子。）

---

## 5. 当前架构（形态 + 门 + 不变量 + 排除清单）

**当前提交源 = 纯 AIV 一档**（P38 字节里 `SFA_CUBE_ON`/`CubeBlock` **grep 命中 0** ⇒ cube 代码一行都不在 `code 3/code/`，只在 `probes/backup/`）。

**(a) 纯 AIV 路径**：单元 = `(b, s, 头组 nb, KV 分片 ks)`；`kv_shard` 沿 KV 轴按**下标模 ks 交错**分片，归并借**本算子自己的输出张量** + 每轮一道 `SyncAll` 串成链（分片 0 `WriteOut` 发布，分片 `r` 读回累加器与自己的 UB 部分和合并再发布，格式同构 ⇒ 深度只是多几轮，⛔ 没有 workspace 通道）。P105 起 `ks ∈ {2,3,4,5,6,8,10}` 由 `KsAllowed` 门 + kernel 侧 `coreNum == ks*total0` 复核（"一核一单元"是硬前提：分片部分和留在 UB 里），不满足就整档退回 `ks=1` ⇒ **退回路径与参考实现逐位相同**。标量下标扫描**前置进 flush 阴影**、暂存放对象不放 UB（P21）；选档 `(nb, n_blk, ks)` 由 host 代价模型（P31 的 184 格网格标定；`UB_SAFE_PCT=95`、迟滞 `0.95`、`NB_CAND={32,16,8,4,2,1}`、`NBLK_CAND={128,64,48,40,32,16,8,4,2,1}`、`SFA_STAGE_MAX=48`、`FOLD_TAX=250 等效调用`）。⚠️ 平台六点全 `rows ≥ 41` ⇒ `ks` 与 `nb` 这两根旋钮**在分数上没有落点**（§5 #17/#18），留着是因为它们在 `rows ≤ 20` 的本地档上真快 1.35~1.42×。

**(b) cube 路径 M1d + A′ —— ⛔ 已作废（`LOG#15.82`，`#15.91` 又把入场条件正面排除），字节只在 `probes/backup/`，⛔ 别当"待办"读。复活的前提"平台六点里有没有 `sbs≥128`"= **没有**。三条不变量（`mp.m=16` 分形 / READY 广播 + 单槽环 + credit / `cskip_·cthr_·cidx_` 行口径）与两道门的完整推导在 `LOG#15.71~#15.78`。

**⛔ 排除清单（别回头，每条都有实测）**

| # | 已判死 / 已作废 | 一句理由（"为什么"的细节在锚点那条 LOG 里） | 锚点 |
|---|---|---|---|
| 1 | **GM workspace 通道** | 声明 16 MB + 128 KB 真窗口照样**毒化下次 launch**（2 MB 偏移 rep0 挂 `EZ9999`），MIX 超核形态下 AIV 只拿到零长切片 ⇒ 暂存只有"自己的张量行"一个落点 | `#15.24`、`#15.70` |
| 2 | "纯 AIV 内核"口径 | 早已是 MIX 1AIC:2AIV | `#15.37` |
| 3 | "`SyncAll` 禁用（各核循环数不同会死锁）" | 作废：`SyncAll<true>()` 是纯硬件 AIV 屏障、MIX 下可用；判死的是要 workspace 的 `SoftSyncAllImpl`，从没用过 | `#15.37(c)` |
| 4 | **宽 chunk（`n_blk` → 128/256）** | 每 chunk 固定税是**饱和**曲线 ⇒ 外推只剩 6~9 % | `#15.75(g)`、`#15.82` |
| 5 | "ND2NZ 不能 gather" | 从没量过且**是错的**（`m1g` 实测 `mismatchGather=0`） | `#15.70` |
| 6 | `MulCast` / 混合类型 `WholeReduceSum` / `BlockReduceSum` 替折叠 | 当场算死：dst 只能 int8/uint8；2201 无混合实例；调用数持平 | `#15.35` |
| 7 | 平台 WA 根因 = bf16 / 形状维度 / tiling 越界 / 索引展开 / 前缀和语义 | 六条全实测排除（真因 = `actual_seq_lengths` 完全没用 + `GetStorageShape` 推断期返回 0） | `#15.8` |
| 8 | 把标量表"搬近"（搬进 UB 逐元素读） | 标量 UB 读 97 ns/次（比 GM 贵 2.5×）⇒ 扫描只能**消掉**，不能搬近、也不能靠重叠藏 | `#15.39`、`#15.40` |
| 9 | 平台 MIX 门 | 早就不是门（MIX 出过榜） | `#15.65` |
| 10 | 拿"平台两发分数相等"论证可分辨性 | 推理不成立；判收益只认同场次差分 + 多遍均值。9/24 的真锚点：同字节七遍 21.7~22.3 ⇒ **≥8~10 % 系统性收益可判**（§1.2） | `#15.52`、`#15.83`、`#15.93` |
| 11 | `code 3/submit/`、`sparse_flash_attention_code.zip`、旧 `c*.bin`、`origzip/`、`doubao/` | 全是过期快照 / 旧格式。恢复**只能**从 `probes/backup/` 与 `…_365715_pass.zip` | §8 |
| 12 | 官方题面两处口径 | `softmaxMaxOut` 写"已乘 scale"= 错；示例代码把索引当 token 位置 = 错 | §2.4 |
| 13 | 历史 `561002`（`SBS=128` UB 装不下） | 已修（host 永不拒绝 + kernel 支持稀疏块跨 chunk）⇒ 教训：**凡是"自己加上的约束"都要问"官方范围我全覆盖了吗"** | `#4.2` |
| **14** | 🔴 **整条 cube 线（M1 / A′ / M2d / PV-on-Cube）对本题分数** | 赚的条件"cube 单元 ≥16 ∧ `sbs ≥128`"（实测 0.53×）被 P111 **正面排除**（平台六点全 `sbs=1`）⇒ **无条件死**；`sbs=1` 时即使单元够也只 ±5 % | `#15.80`、`#15.82`、`#15.91` |
| 15 | "填饥饿核"（`kv_shard` 往深推、铺到 40 核）还有大收益 | 钳子是核数不是折叠税：那几族早在 32 核上 ⇒ 可填 1.25×，P105 的 1.35~1.42× 已贴上限 ⇒ **`ks` 轴到此为止**（平台上更是没有落点，见 #17） | `#15.85`→`#15.86` |
| 16 | **P104 = gather 跨块搬运聚合（run-merge）** 🔴 **两条前置结清，回到判死** | 合并能省的 = `12.9 pp × 跨表项邻接率`：基线 `CopyGm2Ub(…, run*D_)` **块内早已并成一条**、平台 `sbs=1` ⇒ 邻接率 = `1 − count/KV_S`（官方"抽样后排序"~25 %，**平台分布未知**）⇒ **天花板 ≈3~4 pp ≈ +0.6 分**，且按 §1.2 那条口径这个量**既测不出有肉也测不出没肉**。前置② 那条反证 = **误读**（sbs 阶梯不是条数轴，§4 碎拷贝行）。字节已还原，备份 `probes/backup/p104_pre_runmerge/` | `#15.84` + `#15.95` + `#15.98` |
| **17** | 🔴 "平台六点里有 `rows ≤ 40` 的点" | 探针 `rows≤40 ⇒ n_blk=8` 本地 +45~71 %、平台六点零响应 ⇒ **六点全部 `B·Q_S ≥ 41`**（6~9σ）⇒ **`kv_shard` 深度轴永久与平台分数无关** | `#15.87`→`#15.88` |
| 18 | `rows ≥ 41` 靶族上还有选档收益（`nb` 轴） | 5 例里 4 例 `auto` 逐位等于最优 forced 档，最大偏差 2.2 %（0.4 分级）⇒ **选档四条轴全部读完** | `#15.89` |
| 19 | "提 `WidenToF32` 出循环是大收益" / "计算量线性 ⇒ 已到 SIMD 吞吐墙" | 本地：上限 5.5 %、墙 = 吞吐 **+ 依赖链延迟**。🔴 P112 补刀：它瞄的 score 段在平台上只值 ≈6 % ⇒ **在平台上没有落点** | `#15.90` + `#15.92` |
| **20** | 🔴 **"六点同型"（同一个段占比模型套六点）—— 但只降级到"存在两个份额差 ≥2× 的点"** | P114 逐点 Δ `+24.1/+11.4/+35.6/+9.0/+15.6/+13.7 %` ⇒ 两端差 ≥20 pp，**超出**同字节逐点噪声（±5~10 %，§1.2）⇒ "不同型"成立；⚠️ 中间各点在噪声内**不可分**，别拿六点各自当"每点份额已知"用（`LOG#15.96` 用相邻两遍同字节回发复算这条噪声） | `#15.93` + `#15.96` |
| **21** | 🔴 **"造一个本地代理族，把平台搬到本地来筛"**（P113 的全部立意） | 12 档三臂（含 6 档新造的冷/大/低强度形状）无一达到判据，本地比值最低 1.05 vs 平台 0.34，且**扫遍冷度/复用/形状都扫不出来** ⇒ 搬运轴上**没有离线代理**，方向判定只能花在平台发次上 | `#15.94` |
| 22 | "KV 装不进缓存 ⇒ 搬运份额会涨"（把冷度当轴） | 冷池 285 MB / 热池 17.8 MB、复用 0.5×~64× 全程 Δgather 只 13.6~22.9 %，且请求吞吐钉在**与冷热无关**的 456~496 GB/s ⇒ 我们的 gather 上限不是访存层，**"换更冷的形状"不是找墙的办法**。⚠️ **口径订正（P118）**：那一族扫的是**基线自己的复用度**、计量器的冷暖从没单独变过 ⇒ 当时那句"冷度不是轴"**说过头了**：冷度**是**轴（同一条 gather 换冷行本地就 2.8~3.4 倍），只是**平台六点不在这一侧** | `#15.94` + `#15.97` |
| **23** | 🔴 **"榜首那 3.6× 只能靠少搬字节吃掉"（`#15.94` 判② 留到最后的一个机制）** | P118：与 P114 同条数、同每条字节、只差行号 ⇒ 平台冷溢价 **+0.79 pp ≤ 噪声** ⇒ 18 % 里字节那一侧总共 4.9 pp 且**已证明不是带宽、也不是层** ⇒ **跨行去重 / KV 常驻 / 粗化 over-fetch 整条线无条件死**；顺手那笔"命令减半换字节翻倍"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |
| **24** | 🔴 **"PV 段（含内层那条 97 ns 标量 UB 读）是平台上的大杠杆"** —— 最后一块没价的大 AIV 工作（本地占 AIV 31~34 %） | P119 复制 ×2 ⇒ 平台 **Σ +2.9 %**、本地同臂 **+18~33 %**（比 0.095）。带内判不出非零，但**关闭不需要过门**（§1.2）⇒ 连同 #19 把**整条向量线**结掉（三段 ≈11 % of wall） | `#15.98` |
| **25** | 🔴 **"减每 chunk 同核旗标对数是杠杆"（P120 判据里 Δ ≥ 6 % 那条岔路）** | 127 对 ⇒ 平台 **Σ +2.42 %**，而这一发比同字节池内最高（68.06）还低 0.62 % ⇒ 每对 ≤0.0191 %、现有 **4 对/chunk ≤0.076 %**（最不利读法 0.103 %）⇒ 全删 ≈ **+0.02 分**。本地每对 0.03~0.06 % ⇒ 平台/本地 ≤0.73 ⇒ **固定开销在平台上的占比比本地更小**（"每 chunk 固定税"里走旗标/管道同步的那部分从此有不利上界） | `#15.99` |

---

## 6. 下一步（按量级排，不按容易排）

| 优先级 | 待做 | 预算 / 上限 | 状态 |
|---|---|---|---|
| **P0** | ~~**P115 = 把 gather 的"字节成本"与"调用条数成本"解耦**~~ | 读完了：**同字节、条数 ×2 ⇒ +12.90 %**（P114 = +17.8 %）⇒ 调用侧 ∶ 字节侧 ≈ **72 : 28**（含"短拷贝更贵"的混淆 ⇒ 72 % 是上界）。⇒ 方向定了：**要动的是每条命令覆盖的字节数**，"每 token 少搬字节"那一档架构题降级到 ≤0.9 分 | 🔴 已读 `#15.95` |
| — | ~~**把 76 % 那块没有价格的墙拆开**~~ | 已读到 26 %：`ComputeScores` **6 %**（P112）+ gather **18 %**（P114/P118）+ softmax 前段 **2.4 %**（P116）⇒ 剩 ≈74 % 无价 | 🔴 已读 |
| — | ~~**P118 = 冷复制计量器**（第一把能读到 HBM 那一侧的尺子）~~ | 读完了，答案是**负的**：冷 **+18.2 %** vs 暖 **+17.4 %** ⇒ **冷溢价 +0.79 pp ≈ 0**（本地同双臂 2.8~3.4×）⇒ 走的是左边那条岔路："墙不是冷 miss"，而**代价比预想更狠**：它把"少搬字节"整条架构线一起带走了（§5 #23） | 🔴 已读 `#15.97` |
| — | ~~**P119 = PV 段 ×2（目的端 = `kfBuf_` 末行）**~~ —— "惰性重复"族第二口气：带累加器的段换目的端照样能复制 | 读完：本地 **+18~33 %** 且复制份数上**严格线性**（四臂 8 档）⇒ 尺子可信；平台 **Σ +2.9 %**（13 遍池 ⇒ 判不出非零）。按 §1.2 新口径这**足够关闭** ⇒ 向量线三段（≈11 %）整条结掉，含内层那条 97 ns 标量 UB 读 | 🔴 已读 `#15.98` |
| — | ~~**P120 = 给"每 chunk 同核旗标对"定价**~~ —— 第一把量**开销**不是**工作**的尺子（`FlushChunk` 尾 ×127 对未用过的 `SetFlag/WaitFlag<MTE2_V>(4)`，零内存/零 UB/零依赖 ⇒ **构造级惰性**） | 干活的四段已价完（≈29 %）⇒ 剩下 ≈71 % 只能是开销，而同核旗标是其中唯一"单价从没量过、又确定每 chunk 付 4 次"的一项（§4 那句 0.42 µs 是**跨核**）。这发也第一次直接读到平台的每 chunk 固定税，而不是靠本地那条**饱和**曲线外推。读完了：**每对 ≤0.019 % of wall ⇒ 4 对/chunk ≤0.076 %**，而 127 对那一发的 Σ 67.64 **低于同字节池内最高 68.06** ⇒ 被零假设盖住（§1.2）⇒ **减对数判死**（§5 #25）；副产品 = "每 chunk 固定税"里走旗标/管道同步的那部分**已有不利上界** | 🔴 已读 `#15.99` |
| **P0** | **P121 = 标量下标扫描计量器**（剩下的两条候选里先打 (b)）：把每 token 那条 `idxGm_.GetValue` 额外读 N 条进丢弃值 | 38.6 ns/条 是 §4 里唯一还挂在"每 token 乘一次"这一侧的价（每 query 约 6.1 万次读 ⇒ 名义上限很大、却在平台上从没价过）。⛔ 扫描推进迭代器 ⇒ **不幂等**、不能"原地重跑"，只能"额外读 + 防 DCE" ⇒ **先本地两臂标定看 Δ 是否随 N 线性**（P119 的做法），不线性 = 尺子没生效。判据（§1.2 单向）：带内 ⇒ (b) 出局、只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB | ⬜ **下一做** |
| — | ~~**P116 = 平台上给 softmax 段定价**~~ | 读完了：**+2.4 %**（本地同臂 +2.2~2.7 % ⇒ 唯一一个两侧同型的计量器）⇒ 向量可读段在平台上合计只 ≈8 %，连 softmax 侧也封死 | 🔴 已读 `#15.96` |
| — | **P117 = 本地消融阶梯**（`noDMA` 跳三条 gather / `allSkip` 再跳 `ComputeScores`+`SoftmaxPv` / `noPV`，非惰性 ⇒ `act=none` + 同构建两遍基线）给搬运份额**上界** | ⚠️ 降级排到 P118 后面：`#15.94` 已证**本地段占比不可外推到平台** ⇒ 它答的是"本地搬运值多少"，只有当 P118 也读完之后、需要给 PV 段（唯一不能被"重复"定价的那段）找价时才值得跑 | ⬜ 排队（低） |
| — | ~~§6 P1 提加宽出循环 / 组宽 48→24 断依赖链~~ / ~~P2 `Muls` 重缩放 + `RED_SLAB` 宽度~~ / ~~整条 cube 线（M1d/A′/M2d/PV-on-Cube）~~ / ~~`ks` 深度、`nb` 选档、`n_blk` 宽 chunk~~ | 全部**在平台上没有落点**：cube 线死于 `sbs=1`（P111），`ks` 死于 `rows ≥ 41`（P108），选档死于实测（P109），向量两条死于"平台 score 段只值 6 %"（P112） | 🔴 已清 |
| — | ~~**P113 = 造本地代理族把平台搬到本地筛**~~ | 12 档三臂**无一达标**（本地比值最低 1.05 vs 平台 0.34），且驱动轴是 `N1` 而非冷度/复用/形状 ⇒ **计算∶搬运这根轴上没有离线代理**。⚠️ 例外：搬运**内部**的"条数∶字节"分解本地就能对上（0.9 vs 0.72，`#15.95`）⇒ 只有这条轴可以本地筛。⚠️ **P118 给这条"例外"加了一条反向的**：冷暖比值本地 2.8~3.4 / 平台 1.0 ⇒ **本地能筛的轴，平台上未必存在** ⇒ 本地臂只当"这段值多少"的量级参考，判据一律回平台 | 🔴 已清 `#15.94` |
| — | ~~run-merge（P104）—— 曾被写成"唯一还活着的大杠杆，被一条自相矛盾卡住"~~ | **矛盾解释完，它就变小了**：① 那条反证不成立 —— sbs 阶梯上"条数"与时间不对应（条数单调降而时间不降：减半只动 0.6 %、更少反倒慢 2×），它不是条数轴；② 干净的条数轴单价 5.2 ns/条 × 平台调用侧 12.9 pp × **跨表项**邻接率（`sbs=1` ⇒ ≤~25 %）⇒ **天花板 3~4 pp ≈ +0.6 分、过不了 6 % 的判别门** | 🔴 已结 `#15.98` |

## 7. 测量纪律（**踩出来的，不是风格**）

| 条 | 规矩 | 为什么 |
|---|---|---|
| 1 | 任何 A/B **必须同场次**，且每臂跑两遍；跨场次只用于"复现同一读数" | 场次级漂移实测 **4~7 %**，而分数噪声带 ±0.6 分 ⇒ 小于 6 % 的收益换个场次就没了 |
| 2 | 远端旋钮（`NBLK_CAND`、`SFA_CUBE_ON`、sed 改常量）**必须把 `grep` 命中数当门**，改完打印确认 | 真踩过一次自欺：`sed` 基准目录混了 ⇒ "NG=1"那发其实又跑了一遍 NG=8 |
| 3 | 计时档（`w3/w4/p1s*/p4s2/p6s2` 和 **`pm*` 整族**）expect 是**哑零** ⇒ 只看时间、`act=none`，⛔ 绝不 `write` | "超差 44 万/52 万"不是回归。**`pm*` 这一族就是 P104/P105 的行阶梯靶，expect 全零由生成器自己写明**（`p104_run.py:23`）⇒ 上面那两档的网格读数里 `pm*` 的"超差 6857/8192"（连 `ks=1` 那发也一样）**是用例属性不是代码回归**。⚠️ 反之 `big1` **有真 expect**（在 `p32_gate.sh` 的 GOLD 里），它报非零超差才需要当真 |
| 4 | 消融档（删计算 / 删搬运、输出必错）**全程 `act=none`**，且以**同一构建两遍**为基线 | `cubek1/cubekg` 那轮就是这么读的；换构建做基线会把结构差读成时间差 |
| 5 | 每次真机发次前：探针全跑完 → **最后一次 `sync`（此后不再 build）** → 核 sha256 → 才 submit | §3 的 SoC sed 陷阱 |
| 6 | 本地 ms 与平台 time **不能换单位比**，只能换**倍数**（用 §1.2 的尺子） | 那条"48×"疑云的结论是形状不同，不是单位不同 |
| 7 | 文档里的 md5 / sha256 **全部视为过期**，判定只认当场 `sha256sum` | 已两次因此差点提错字节 |
| 8 | **任何网格 runner 在重建之后先撞一发探活、再扫**（会挂的档放批次最后）；某发挂死或被 `timeout` 杀掉 ⇒ **本轮剩余读数作废、重开场次**，但⛔ **不要归因成"设备被毒化"** | 前一半有用（`LOG#15.79` 那轮第一版双臂就是死在批次中间塞 `w3`）；后一条已订正 —— `LOG#15.82` 同卡跑满 cube 臂网格无一挂死 ⇒ 真相是**可自愈的进程/stream 残留上下文**。⚠️ 而且**不只 cube 臂会中**：`LOG#15.86` 那次**纯 AIV 的 `(nb,ks)` 网格**第一版整批 rc=2、`SFA_PICK` 空，拿同一份字节单发一次立刻 PASS（0.1205 ms、`超差 0`）⇒ 占住设备的上下文几分钟后自愈，**别把它读成"我的改动挂了"**。`p105_grid.sh` 已内置探活门（不通则等 60 s、重试 6 次）；`p97d_reset.sh` 从未需要跑 |
| 9 | 往远端推**代码字节**走 stdin（`cat file \| ssh host "cat > path"`）+ 事后远端 `sha256sum` 校验，⛔ 不要 base64 进 argv；远端逻辑要参数化就落成脚本再推，⛔ 不要把模板塞进本地 `printf` | 单参数上限 `MAX_ARG_STRLEN` = 128 KB：1667 行内核 base64 后 137 KB ⇒ `Argument list too long` ⇒ 那一轮"head 对照臂"跑的其实是 **p96 的字节**，结论整个反向；`printf` 那一版则把远端内层的 `%s` 一起吃掉 ⇒ 四遍读数全空 |
| 10 | **任何"某段占多少墙"的断言必须标明是在哪一侧量的**；本地靶族的段占比 ⛔ 不可外推到平台，只有**形状轴**（`sbs`、`rows`）可以 | P112/P114 两个惰性计量器读出镜像结果（本地 计算∶搬运 = 10∶1、平台 = 1∶3，`#15.93`）⇒ 拿本地消融去排平台优先级会排反。⚠️ 同一条还管"六点合读数"：P114 逐点 Δ 从 +9.0 到 +35.6 % ⇒ **六点不同型**，合读数会把结构差读成平均值 |

---

## 8. 工程材料索引

| 路径 | 是什么 |
|---|---|
| `code 3/code/{op_host,op_kernel}/…` | ⭐ **四枚提交文件**（§3） |
| `code 3/npu_debug/{npu.sh,dev.sh}` | ⭐ 真机 sync / build / run / 计时入口 |
| **`code 3/probes/LOG.md`** | ⭐ **L2 证据档案（冻结件）**：锚点目录 + "已知为错"表 + 全部原始台账（9/21→今） |
| `code 3/probes/p32_gate.sh` | ⭐ **数值闸门**（§3）。最新"全表绿"凭据 = **`p114_gate.txt`**（9/24，探针字节，26 行逐位一致 + 零非零超差）；`p112_gate.txt`/`p105_gate.txt` = 前一发与 P105 干净态 |
| `code 3/probes/p103c_grid.sh` | 形状网格 runner（§1.3 cube 那段的来源，该线已死）：一份字节两臂只差 `CUBE_MIN_WAVES_PCT`，每批先探活。⚠️ `CASE=`/`REPS=` 在 `npu.sh` 里必须当**环境变量前缀**传 |
| `code 3/probes/{p98_status,p98_hist,p111_read}.py` | 只读平台探针：榜上现态（`p98_status.py` 打 `/api/problems/{pid}/ranking`，CLI 自带 `latest`/`rank` 服务端 404）/ 历史发次 / **`p111_read.py` = 最近 N 发的六点时间 + `best_time`（一发一行，⛔ 别用 `p98_detail.py`：它把源码一起打出来 = 1.3 MB）** |
| `code 3/probes/backup/` | ⛔ **代码的唯一恢复凭据**（`p105_clean/` = 当前干净字节、`p114_pre_probe/` = 同份、`pre_p91_cubeheadblk_*` …）。备份一律放这里，不许留在 `code/` |
| `code 3/build.sh` | 提交目录里用的那份构建脚本；`code 3/{run.sh,gen_big.py}` = 真机构建 / 跑 / 大用例 |
| `code 3/cpu_debug/build_cpu.sh` | CPU 编译门脚本（云端仿真机上的真 `ccec` 交叉编译） |
| `refs/sfa/sfa_ref.py` + `refs/sfa/cases/` | ⭐ Python 独立参考实现 + 本地唯一用例语料 10 个 `.bin`（远端 164 用例**无本地副本**，别当可复现） |
| **`refs/sfa/cann_builtin_900/`** | ⭐ CANN 9.0.0 内置官方 arch22 SFA-MLA 源码（910B 版 6 文件，逐文件 md5 复验）。⛔ **只读参考、不移植**（用户 2026-09-20 裁定）；M2d 的形态来源 |
| `refs/sfa/test_sfa_real.cpp` | ⚠️ **重建版** harness（原件被误删、未经真机验证，见 `refs/README.md` §3） |
| `refs/sfa/{run.sh,build.sh,gen_case.py,bench.cpp,verify_*.py}` | 真机构建 / 对拍 / 用例生成 / 诊断 |
| `official_problem_statement.md`（根目录） | ⭐ 三题官方题面全文（§2 的权威来源；两处口径已知有错 ⇒ §2.4） |
| `code 3/sparse_flash_attention_submission_365715_pass.zip` | 6/6 通过版快照（zip md5 `ae9bcd2d…`）。⛔ `code 3/submit/`、`…_code.zip` 均为过期快照，永远不要从那里提 |
