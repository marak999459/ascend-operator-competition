# HANDOVER_DeepSeek_code1.md —— 会话交接单（题 1 `mhc_expand`）

> **这份文件写给"下一个会话"，前提是它没有任何本对话的记忆。**
> 阅读顺序：本文件 → `code1.md §1`（你那边的权威状态）→ 需要细节时再进 `DeepSeek_code1.md`。
> ⛔ 本文件**不含提交源改动**：题 1 的提交源一字未动（`4805684c` / `6c1fdacd`）。
> ⛔ 本文件**不是权威**。与 `code1.md` / `AGENT.MD` 冲突时，**以它们为准**。

**交接时间**：2026-09-21 深夜（真机不可用期间）
**上一会话状态**：目标因缺外部输入被记为**阻塞**，自动轮次已停止。

---

## 1. 交接时的工作区状态（已复核）

| 文件 | md5 | 说明 |
|---|---|---|
| `code1/op_kernel/mhc_expand.cpp` | `4805684cb99ca7bd4856209e59b584e3` (14,173B) | = 第六次提交版（R13） |
| `code1/op_host/mhc_expand.cpp` | `6c1fdacdf788e09e02f0ddb23dc33318` (11,852B) | = 第六次提交版（`core_floor=8`） |
| `code1/op_kernel/mhc_expand_tiling.h` | `f759a052a865facbb889149ea1aa37e3` | 自始未动 |
| `code1/op_kernel/tiling_key_mhc_expand.h` | `267e012564ba3d18cfabc729cc214e88` | 自始未动 |

**上一会话新增的非提交面文件（三份，全部可安全使用）**：

| 文件 | 是什么 |
|---|---|
| `DeepSeek_code1.md` | 上一会话的独立分析存档（带证据强度标注 + 已作废结论清单） |
| `code1/npu_debug/probe_floor/apply_floor.py` | **探针注入器**（替代三份已失效的旧注入器），带唯一锚点守卫 + `--revert` |
| `code1/npu_debug/msprof_digest.js` | **加固版 CSV 摘要器**（`prof_sum.js` 的替代，输出格式兼容） |

探针残留检查：`*.bak_floor` 计数 = 0（干净）。

---

## 2. 上一会话做完的事（**结论，不是计划**）

### 2.1 榜单定量（40 队，来自网页榜单第 1、2 页，用户复制提供）

- 我方队伍 **`fszqsn2200`（可爱小无棋）第 35 名，73.19 分**。榜首 90.49。
- 我方 case1/case5 = **3.68 / 4.02µs**；榜首 1.54/1.68；前 10 门槛 ≤1.86；**前 20 门槛 ≤2.14**。
- **ms 档（case3/6/7/8）全场 ±1.5% 无差别，我方在内侧**（case7 = 731.64 快过 38 队）
  ⇒ **分数差 100% 来自 case1/case5**。
- **分数 ≈ case1 的指数函数**：`score ≈ exp(-0.0663·case1 + 4.5582)`（n=34，剔除 6 条离群）
  ⇒ **每省 1µs ≈ 5~6 分**；把我方 case1 从 3.68 压到 2.0 ⇒ 约 +10 分、进前 20。
- ⚠️ 该式只在 40 个样本上拟合，**只用于排优先级，不当公式**。

### 2.2 两条被推翻的旧结论（**重要：它们在你们的文档里还是错的**）

| 旧结论 | 现状 |
|---|---|
| "平台自 2026-09-03 起对所有人停写 `score`" | ❌ **被榜单直接否证**：09-18 有 90.49、09-21 当天有 87.08/87.58/88.43/88.60。错因：`last_submission` 的 123 条很可能是"每人最新一条"而非完整提交史，而"断点后无非零分"在两种假设下**都会出现** ⇒ 该观察无判别力 |
| "小档剩余差距不在我方代码里"（`code1.md §20.8`） | ❌ **20 队都做到 case1 ≤2.14µs，而我方最优 2.5µs、提交面 3.5~3.8µs** ⇒ "我方地板"≠"真实地板" |

📌 **这两条散在 `README.md` / `code1.md` / `code3.md` / `算子开发工作流.md`，上一会话扫到 6 个文件 13 处，但未擅自改。** 新会话若有权改文档，这是**零风险、高收益**的第一件事（尤其 `README.md` 那句"残余差距已量到启动地板以下 ⇒ 不在我方代码里"，会误导下一个会话）。

### 2.3 小档瓶颈诊断（用你们自己的 51 份 `op_summary.csv`）

形状 c0（`S=64 D=256 m=2 fp16`，IO 96KB），剔首 mean，单位 µs：

| blk | Total | `aiv` | `mte2` | `mte3` | `scalar` |
|---|---|---|---|---|---|
| 1 | 12.6 | 12.1 | 6.9 | 8.3 | 8.9 |
| 4 | 4.3 | 3.7 | 1.9 | 2.1 | 2.8 |
| 8 | 3.2 | 2.3 | 1.0 | 1.0 | 1.8 |
| **12（现行）** | **3.0** | 2.0 | 0.7 | 0.7 | **1.6** |
| 16 | 2.9~3.2 | 1.9~2.0 | 0.6~0.7 | 0.5 | 1.4~1.6 |
| 40 | 4.1~4.5 | 2.4~2.6 | 1.0~1.2 | 0.2 | 1.4~1.6 |

- ✅ **`scalar` 恒占时长一半，而 `mte2`/`mte3` 各仅 0.7µs** ⇒ 小档是 **scalar-bound**。
  这解释了 `code1.md §19.1` 那个反常数（4 道 barrier 全删只值 0.45µs）⇒ §19 把 barrier 当靶子偏了一格。
- ✅ **每单元 scalar 成本 ≈0.125µs**（扣掉 `empty` 基线后），与核数无关。
- ⚠️ **但把 scalar 治到零也到不了榜首**：`aiv = max(scalar,mte2,mte3)` ⇒ 最多降 1.3µs ⇒
  我方内核下限 ≈1.7µs，**加平台侧 ~0.7µs ⇒ 平台最好 ≈2.4µs，而榜首 1.54µs** ⇒ **还缺 ~1µs 未归属**。

### 2.4 口径澄清（已被实测钉死，别再重走）

| # | 结论 | 依据 |
|---|---|---|
| 1 | **平台 `time` = 设备侧内核时长（或内核+tiling），不是 host 墙钟** | ms 档 case3=1.38ms 正好是 1.4GB 纯带宽时间，**无 20µs 偏移** |
| 2 | **harness 的 `per=` 是 host 墙钟，不能当内核时长代理** | 实测 `per=23µs` vs msprof `Task Duration=2.6µs` |
| 3 | `best_time` **不是**当前榜首，比榜首还快 1~13% | 八条里七条 `best_time` < 榜首 |
| 4 | 榜首那列（1.42 等）是 `top1_time(score 90.15)` 的**旧快照** | `submit1_analysis_20260920.log:10`；现榜首已是 90.49 |

### 2.5 工具修正（旧工装已失效，勿再用）

| 旧工装 | 失效原因 |
|---|---|
| `opt_a/apply_r11.py` | `BASE_MD5` 钉在提交 4 的 `038d65a0` ⇒ `exit 2` |
| `opt_a/apply_r11_host.py` | `BASE_MD5` 钉 `61543c1b`，**且锚点被提交 6 新增的 `core_floor` 三行切断** |
| `tiny_probe/apply_r13.py` | `BASE_MD5` 钉 `61543c1b`，**且改动本身已在现行 host 里** |

⇒ 用新的 `code1/npu_debug/probe_floor/apply_floor.py`（不钉死 md5，按锚点唯一命中判定）。

---

## 3. ⛔ 阻断条件（本会话为何停止）

| # | 条件 | 证据 |
|---|---|---|
| 1 | **真机 `02aeb` 持续不可用** | 隧道端口 `47895` 一直监听，但 SSH 在密钥交换阶段被断开：`kex_exchange_identification: Connection closed by remote host`。**只有用户能在 IDE/控制台重连** |
| 2 | **千问（Qwen）的结果始终未提供** | 上一会话被要求"评判千问的结论"，但结果从未到达 |

**上一会话试过的**（避免重复）：`devspace_tunnel.ps1 -List` / `-Role npu` 自举一次 / 等 45s 重试 / `ssh -vv` 定位 / 查本地 `~/.cannjudge` 与 CLI 副本（都没有）。
**未做**：重试轰炸、绕派活闸门（`AGENT.MD` 硬约束 3 明确禁止）。

⇒ **没有环境，`tiling_cost`/`shape_dump`/空 kernel 地板全跑不了；没有结果，评判无从做起。**

---

## 4. 🔵 环境回来后：接手顺序（**顺序不能改**）

```bash
# 0) 环境自检：拿芯片名与 CANN 版本（§11.8 曾在 910C 上白编）
bash code1/npu_debug/preflight_npu.sh

# 1) 基线快照：四文件 md5 必须 = 4805684c / 6c1fdacd / f759a052 / 267e0125

# 2) 探针前先摘干净
python3 code1/npu_debug/probe_floor/apply_floor.py status --target ~/ops_comp/code1
#    ⇒ probe_tokens 必须为 0

# 3) 隔离树：只在副本里打补丁（⚠️ 上一会话曾误打工作区，见 §6 第 3 条）
cp -a ~/ops_comp/code1 ~/ops_comp/probeF

# 4) 打两格探针（各是一个独立构建）
python3 .../apply_floor.py tiling_cost  --target ~/ops_comp/probeF   # MHC_TILING_EMPTY=1
python3 .../apply_floor.py shape_dump   --target ~/ops_comp/probeF   # FD_DUMP=1

# 5) 每格硬校验：strings pkg/custom/op_api/lib/libcust_opapi.so | grep -c 'FLOOR PROBE'
#    ⇒ 命中数必须与预期一致（防"探针没打进去/没摘掉"）

# 6) 读数一律过加固摘要器，不看裸 grep（避 §19.7.3 色码坑）
node code1/npu_debug/msprof_digest.js <op_summary.csv>

# 7) 数值门禁：每格必须 timed=1 且 mismatch=0 才采用（§17.1）

# 8) 收尾：apply_floor.py --revert --target ~/ops_comp/probeF + md5 复核 + 合规扫描
```

### 4.1 两格探针要回答的问题（**预注册判据**）

**A. `tiling_cost`（`MHC_TILING_EMPTY=1` 让 `TilingFunc` 立刻返回）**

| 读数 | 含义 | 下一步 |
|---|---|---|
| baseline 与 tiling_cost **差 ≥0.5µs** | 平台 `time` **含 tiling 执行** ⇒ 那 0.7µs 在 host 侧 | 改 `op_host`：`PlatformAscendC` 构造 + `GetCoreNumAiv()` + `GetCoreMemSize(UB)`（`op_host:21-24`）——`code1.md §14.1` 已用 msprof 钉死本机 AIV=40，**可改常量**，代价是放弃换芯片自适应 |
| 两者**一样** | 平台 `time` **不含 tiling** ⇒ 0.7µs 落在内核入口/收尾的固定 scalar | 攻序言：`empty` 的 0.9µs `scalar` 基线（§2.3） |

**B. `shape_dump`（`FD_DUMP=1` ⇒ 每次 tiling 打一行 `[FD_PROBE] S=… D=… m=… bwd=… io=… cap=… blk=… tile=… mode=…`）**

- 用途 1：**反推判分用例的真实形状**（我方至今在猜 case1 = `S=64/D=256/m=2`，从未证实）。
  做法：平台跑一次就能看到它喂了什么形状（若 stdout 不落盘，则在本地 harness 跑同形状反推）。
- 用途 2：**逐位核对 harness 里那份手写镜像 `plan_tiling()` 是否与 host 一致**
  （`§19.7.1` 那个"镜像陈旧导致日志印旧 blk"的坑，从此有机器判据）。

### 4.2 若确认要改提交源，优先级

1. **scalar 瘦身**：`NextUnit` 的四路 `splitMode` 分支改编译期特化；`FwdIn`/`FwdOut` 把 `DataCopyExtParams` 构造提到循环外；地址算术改 32 位。
   **对大档免费**（scalar 仅占 29.7/1117 = 2.7%），风险低。
2. **重测 `code1.md §14.9`**：见下。
3. **host tiling 瘦身**：取决于 `tiling_cost` 结果。

---

## 5. ⚠️ 未验证的线索（**别当结论用**）

### 5.1 `code1.md §14.9` 的"源冻结不存在"可能是**探针口径填错**

- 官方 `mhc_post_arch22.h:247` 与 `mhc_post_kernel.cpp:174` **两处**都用
  `srcStride = (d − dNum) * sizeof(T)` ⇒ **`srcStride` = "块与块之间的源侧间隔(字节)"**，不是"字节步距"。
- 我方 §14.9 探针填的是 `0` / `rb(512)` / `rb/32(16)`，**没有一个是官方那个语义**。
- 而官方 `USE_PERMANENT_X` 路径**就是**"一次多块 DMA 把 n 行读进 UB 常驻，逐帧读、内层只写不读"，**同架构 arch22**。
- ⚠️ **上一会话无法核实 `DataCopyExtParams` 字段定义**（本机没有 CANN 头文件，三处 `Ascend` 路径都不存在）
  ⇒ 整条是推断 + 未验证，**不要写成"§14.9 是错的"**。
- **建议**：把 §14.9 从"原理上不成立"降级为"探针口径可疑，待用官方形状（源常驻 UB + 多块读带 gap）重测"。
  若成立，前向"每核 18 次请求"可压到 2~3 次。

### 5.2 其他

- **case1/case5 是同一形状的正反向**（40 队里 case5 ≈ case1 + 0.2µs 近乎普遍，唯一例外 #23 = 2.22/4.74）；
  对称到 0.2µs 以内 ⇒ **暗示 m 较小（≈2）**。
- **case2 与 case4 的比值不是常数**（前 4 名 3.08~3.20、我方 2.86、#22 2.77、#23 5.05）
  ⇒ **不能假设判分用例只出自题面那三个档**。

---

## 6. ⚠️ 上一会话犯过的错（**别重复**）

| # | 错误 | 教训 |
|---|---|---|
| 1 | 由 R10 的 barrier 差值**外推**出"每道 `PIPE_ALL` ≈0.28µs"，并据此定靶子 | **把"带宽饱和态下的边际机会成本"当成"指令自身价格"去外推**。你们 §19.1 的实测（4 道全删只值 0.45µs）推翻了它；而 §18.3 早就有"c4 减半 barrier = 0"的反证在案 |
| 2 | 复述并强化了"09-03 计分停写" | **`last_submission` 的语义没验证就当成了完整提交史**；且该观察在两种竞争假设下都会出现 ⇒ 无判别力 |
| 3 | **把探针直接打进了工作区提交源**（`apply_floor.py --target` 指向工作区） | **任何会写文件的验证，第一次就该在副本里做**。已 `--revert` 并三重验证恢复，但这是纪律问题 |
| 4 | 用"我方 `empty` 地板"论证"剩余差距不在代码里" | **"我方地板"只能推出"我方这条路到顶"，推不出"这件事到顶"** |
| 5 | 提"花一次只读查询确认 1.42 是否 09-03 前" | 该结论**已由"分数非零"反推得出**，`submit1_analysis` 的 md5 就挂在 `code1.md §6.3` ⇒ **提动作前先查"是不是已经算过了"** |

**通用尺子（对任何新结论都用一遍）**：
> 对每个"干净"的观察，问 **"若反过来假设，这个观察还会不会长成这样？"**
> 上表 #1 与 #2 两次都栽在这。

---

## 7. 新会话第一件事该做什么

**如果千问的结果到了** ⇒ 按 `DeepSeek_code1.md §8` 的预注册清单评判（数字可信度 / 口径 / 结论边界 / 反假设检验），再决定是否采纳。

**如果环境交回了** ⇒ 按 §4 的顺序跑 `tiling_cost` + `shape_dump` 两格（约 1 个构建，5 分钟内出结论），它决定后面攻 host 还是攻 kernel。

**如果两样都没有** ⇒ **不要继续在没有新证据的情况下产出推断**。上一会话在第 7~11 轮已经进入"自己推翻自己上一轮"的循环，边际产出归零；`DeepSeek_code1.md` 的价值正在于**里面每条都可验证**，堆积未验证推断会稀释这一点。

---

## 8. 上一会话未能完成/未拿到的东西（诚实记账）

| 项 | 状态 |
|---|---|
| 千问（Qwen）那一路的实验结果 | **从未到达** |
| 真机 `02aeb` | 自 2026-09-21 约 19:30 起不可用；未重试轰炸 |
| 榜单第 3 页及以后 | 未取（只有第 1、2 页，40 队） |
| `DataCopyExtParams` 字段定义 | 无法本地核实（无 CANN 头文件） |
| 平台 8 条判分用例的真实形状 | 未拿到；`shape_dump` 是反推方案 |
| 改动 `code1/` 的提交源 | **一字未改**（`4805684c` / `6c1fdacd`） |
