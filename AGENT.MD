# AGENT 入口（**先读这个**）

本仓库是**昇腾 Ascend C 算子开发比赛**，共 3 道题，通过 SSH 隧道连一台 910B3 NPU 机器。

---

## 1. 阅读顺序

| 顺序 | 文件 | 内容 |
|---|---|---|
| 1 | **`AGENT.MD`**（本文件） | 入口与硬约束 |
| 2 | [算子开发工作流.md](算子开发工作流.md) | **通用**三步流程 + 通用坑 + 通用教训（跨三道题） |
| 3 | [连接信息.md](连接信息.md) | 连接参数与凭据**去哪读**（不硬编码任何值） |
| 4 | [code1.md](code1.md) | 第一题 `mhc_expand`：状态 / 已排除 / 下一步证据 |
| 5 | [code2.md](code2.md) | 第二题 `mhc_sinkhorn`：同上 |
| 6 | [code3.md](code3.md) | 第三题 `sparse_flash_attention`：同上 |

**参考资料**（按需查，不是必读）：

| 文件 | 何时看 |
|---|---|
| [official_problem_statement.md](official_problem_statement.md) | 需要**官方题面原文**时（权威口径；第一题 / 第二题 / 第三题三章） |
| [refs/README.md](refs/README.md) | 要**跑真机回归**、或想知道 `refs/` 里有什么时 |
| [refs/sfa/sfa_ref.py](refs/sfa/sfa_ref.py) | 需要**第三题参考实现 / 重新生成用例**时 |
| [refs/harness_sinkhorn/](refs/harness_sinkhorn) | 需要**第二题测试脚手架**时 |
| `ops-transformer-master/` | ⭐ 需要**官方开源对标实现 / 性能手法**时（CANN 官方算子库，三题的开源参考池）。**只读参考，不是提交源**；逐题可用性见各 `codeN.md §10`，合规条款见 `code1.md §10.0` |

> 📌 **分工**：`算子开发工作流.md` 讲"**怎么做**"，`codeN.md` 讲"**这题现在到哪了、什么已被排除、下一步要什么证据**"。
> `codeN.md` 随题目推进**持续更新**；根目录通用文档很少改动。
> 需要哪一题的细节就只读那一个 `codeN.md`，**不要全读**（省 token）。

---

## 2. ⛔ 硬约束

0. **统一术语（用户 2026-09-20 规定，全仓库文档一律照此称呼）**：
   - **仿真机** = CPU 仿真环境 = **云端仿真机**（云 devenv 容器）。⚠️ 2026-09-20 起**仿真一律在云端跑**：本地 VMware 那台不再承担仿真，改叫**通道机**（见下）
   - **通道机** = 本地 VMware Ubuntu VM，专职两件事：**第二账号的 devspace 登录位**（homedir 隔离，见 `连接信息.md` §2.3）+ **第二工位 Qoder**
   - **真机** = NPU 环境（910B3，SSH 隧道进入）
   - **比赛平台** = 代码提交/评测网站（CANNJudge）；旧文档中的"网站 / 比赛网站 / 提交平台"均指此
   - 🕘 **历史行文**：`code1.md` / `code3.md` / `README.md` 里出现的"本地仿真机"指的是那台 VMware VM 当年跑过的仿真，**不回改**；从现在起要跑仿真请按"云端仿真机"理解（口径：用户 2026-09-20 决定仿真全部上云）
1. **三道题严格分家**，绝不跨界：
   - 第一题 `mhc_expand` → 提交代码在 `code1/`
   - 第二题 `mhc_sinkhorn` → `code2/`
   - 第三题 `sparse_flash_attention` → `code 3/code/`（文档在 `code 3/`）
2. **提交里绝不能有任何调试输出**：`printf` / `fflush` / `fprintf` / `std::cout` 一律禁止，`TODO` / `FIXME` / `#if 0` / `调试` 字样也要清掉。否则比赛平台判**"提交代码中存在不合规内容"**。
3. **隧道一律先用 `devspace_tunnel.ps1` 自举，不要一上来就找用户**（2026-09-20 更新）：`.ssh/config` 的 Host 别名自带 `Match !exec` 自举，脚本封装了"拉起 + 等端口 + ssh 验证 + `bootstrap.log` 判读"。用法：**"连 cpu" → `-Role cpu`**、**"连 npu" → `-Role npu`**、只看状态 `-List`。环境表（短名 → 角色 / 可否派活 / 备注）在脚本顶部 `$EnvTable`，**加新环境只在那里补一行**；`连接信息.md` §2 是同一张表的文档口径。
   - ⭐ **派活闸门**：`-Role` 只会选中 `use='free'` 的环境；`use='reserved'`（正被别的会话当通道）和**未登记的新环境**会被跳过并打印 `[门]`。⛔ **Agent 不得为了"有环境用"而用 `-Env` 点名越过闸门** —— 被挡下就等于没环境，回去告诉用户缺什么，让用户决定（改表 / 开新环境 / 授权占用）。
   - 用户直接点名环境（"用 e6z6k"）时按 `-Env <短名>` 执行，那是用户的决定，不是越闸。
   - ⛔ 脚本报 `development environment no longer exists` ⇒ **环境本体已被回收，只有用户能在 IDE / 网页重新 Start**，此时停下来找用户；**不得重试轰炸**。真机连上之后**跑什么**仍按 §2.4/§2.5 的纪律先问用户。
4. **改代码前先说清"要改什么、为什么、怎么验"**（2026-09-21 修订，原为"不要自动改代码"）：三题均已进入"改代码 → 上机验证"阶段，Agent **可以**改提交源，但**不得静默改**——动手前给出改动点与验收判据，改完立刻按 `codeN.md` 的口径留证据（md5 基线 + 真机/仿真结果），破坏性或不可逆操作仍**必须先后请示**。各题自己的约束表（如 `code1.md §0`）优先于本条。
5. **不要连续提交**：每次提交后等用户确认；每次提交前先 `--dry-run`。
6. **密钥与私钥路径不写死在文档里**，一律引用 `连接信息.md` 的相对路径。
7. **git 主干在 Windows**（用户 2026-09-20 定）：提交/推送只在这台机器上做；**通道机（VM）里的那份是只读工位副本**，只 pull 不 push（VM 侧 `origin` 的 push URL 已被停用，恢复方法见 `连接信息.md` §2.3）。多路会话并行改文档时仍按"只 add 自己那摊"。

---

## 3. 目录导航

```
.
├─ AGENT.MD                  ← 本文件（入口）
├─ 算子开发工作流.md          ← 通用流程与教训
├─ 连接信息.md                ← 连接参数 / 凭据 的相对路径说明
├─ code1.md / code2.md / code3.md   ← 各题状态（随推进更新）
│
├─ code1/                    ← 第一题 mhc_expand（提交代码）
├─ code2/                    ← 第二题 mhc_sinkhorn（提交代码）
├─ code 3/                   ← 第三题 SFA：code/ 是提交实现，其余是工程文档
│
├─ 密钥.txt / public.pem / encrypt_password.py   ← 比赛平台凭据与加密脚本
├─ devspace_tunnel.ps1       ← ⭐ 隧道自举 + 派活闸门：-Role cpu|npu / -Env <短名> / -List / -Diag / -Watch；环境表 = 顶部 $EnvTable
│                                 角色表与环境状态见 连接信息.md §2
├─ official_problem_statement.md   ← ⭐ 三题官方题面全文（权威口径）
├─ ops-transformer-master/   ← ⭐ CANN 官方**开源**算子库（三题的开源参考池，**只读、非提交源**）
│                               逐题可用性见各 `codeN.md §10`；合规条款见 `code1.md §10.0`
│                               ⚠️ **不入库**（已 gitignore）：clone 本仓库后需自行拉 `gitcode.com/cann/ops-transformer`；本地这份 = v9.2.0，文档里的行号会随版本漂移，查不到就 grep 符号名
└─ refs/                     ← 可跑的引用材料（不是提交代码）
   ├─ README.md              ← ⭐ refs 说明 + 用法 + 重建告诫
   ├─ sfa/                   ← 第三题：参考实现 + 真机回归用例 + 诊断脚本
   │                            ⭐ sfa_ref.py（Python 参考）、run.sh（构建+对拍）
   │                            ⚠️ test_sfa_real.cpp 是**重建版**（原文件误删，见 refs/README.md §3）
   │                            ⭐ gen_case.py + cases/（用当前格式生成用例）
   └─ harness_sinkhorn/      ← 第二题 mhc_sinkhorn 的本地测试脚手架
                                ⭐ mhc_sinkhorn_scalar.cpp、sinkhorn_ref.py、run.sh
```

> ⚠️ `refs/`、`official_problem_statement.md` 与 `ops-transformer-master/` 是**参考资料**，不参与构建、**不是提交源**。
> 提交源只在 `code1/`、`code2/`、`code 3/code/`。
> `ops-transformer-master/` 受 **CANN OSL v2.0** 约束（开源、可参考可改，**不得移除版权头**）；**参考为主、不整段照抄**，只借部分细节（用户 2026-09-20 定调）。

### 3.1 ⚠️ 目录归属提醒

整理后，**各题目录已只含自己的东西**：

| 目录 | 现在含有什么 |
|---|---|
| `code1/` | 第一题提交源（`op_host/`、`op_kernel/`、`CMakeLists.txt`）+ `tools/reference.py` |
| `code2/` | 第二题提交源（`op_host/`、`op_kernel/`、`CMakeLists.txt`） |
| `code 3/` | 第三题：`code/`（提交实现）+ `submit/`（提交包）+ 构建脚本 |

> 原先混在 `code1/` 下的第二、三题材料**已归入 `refs/`**，其余历史快照与冗余副本**已删除**。

---

## 4. 构建产物噪音

`build/`、`build_out/`、`__pycache__/` 等目录下有大量构建产物，会让 `glob` / 搜索很吵 —— 搜索时**主动排除**这些目录。

`ops-transformer-master/` 同理：它是**上万文件的外部开源库**，搜"本题实现在哪"时**必须排除**，否则会淹掉 `code1/`、`code2/`、`code 3/` 的命中。只有**明确要查开源对标实现**时才定向进它（入口见各 `codeN.md §10`，别从头 grep）。

`code1/npu_debug/prof/` 同理（**已在磁盘上、但已 gitignore 不入库**）：msprof 每次运行产上百个 `.db`/`.done`/`.complete`/`.csv` 中间件，本地这份 900+ 文件 26MB。数值结论已全部摘录进 `code1.md` 的表格（§14/§18/§19），**要复现就在真机重跑 `prof_npu.sh`**，别去仓库里找原始档。

---

## 5. 当前进展（一句话版）

> 📌 **本表是"一眼看去在哪"的摘要，逐题权威口径永远在各自的 `codeN.md §1`**（本表随它们更新；两边冲突时以 `codeN.md` 为准）。**最后同步：2026-09-22 11:30（题 1：十二次提交、在榜 = 提交 12 面、核数轴封存、H3 已本地判出但 `未上提交面`）**。

| 题 | 状态 |
|---|---|
| 第一题 `mhc_expand` | ✅ **正确性完全收口**（下面第 1~2 格）；⚠️ **性能只收到"大档 + 中间档"，µs 地板档未收口**（§21/§22）真机 **7 轮 89 条 + 规则版 45×2 + 8 条 `timed=1` 全绿**（R10/R12/R13/**R14 四次**在提交树复跑同一套门禁）、前后向**位级精确**；比赛平台 **十二次提交全部 8/8 Pass、每条 `precision_ratio=1`**（首交 ~ 第六次 `6ab12d46…` ~ 第七次 `6ab152a1…`（R14 合批）~ 第八次 `6ab17147…`（合批+重定 blk，case1 反弹到 5.08）~ **第九次 `6ab17cfd…`（合批下界 4→8 ⇒ case1 拿到 2.22，本题最好面值）~ 第十次 `6ab1920c…`（反向核数律，04:18，最好名次 27 / 76.29）~ 第十一次 `6ab1d450…`（反向发起数上界，09:05，31 / 74.82，**已撤回、不在榜**）~ **第十二次 `6ab1df67…`（= 第十次那一面的逐字节重投，09:52，当前在榜 31 / 75.50）**，§13/§14/§16/§18/§19/§20/§22/**§23**；⚠️ 该次第一次尝试被 **HTTP 429** 挡回且未创建提交，等 3 分钟重试即过 ⇒ **遇 429 要重、不要改代码**）。性能：大档双向 **94.7% / 94.9% 带宽规格**（970 / 972 GB/s）；96KB 档经"少开核 + A2 + R10 + R12"累计约 **−1/3**（现值前向 2.5~2.7 / 反向 3.5~3.6µs）。⭐ **R13 量出并修掉了那条"少开核"定则的方向错误**：KB 级 IO 上它把核数钉在 1、比大 32 倍的形状还慢，`core_floor=8` 后真机 **−27%~−54%**、中大档恒等（§20.2~§20.5）；⚠️ 第六次提交当形状探针判明**判分 8 条不落在此区间** ⇒ 这批收益是"实现正确性"不是名次。⭐ ~~残余差距已量到启动地板以下 ⇒ 小档剩余差距不在我方代码里~~ **⛔ 此说已由 `code1.md §21` 判负（2026-09-21 22:38，两次独立读榜）**：1.2µs 只是**我方这条路**的地板，榜上 20/20 支队伍在同样两条用例上读到 **1.46~2.14µs**（平台 `TBest` 1.36/1.48）⇒ **case1+case5 的 2.2µs+2.5µs 在我方代码里，且已被证明可压**；上一会话从我方 51 份 `op_summary.csv` 量出的靶子是 **scalar-bound**（`blk=12` 总时长 3.0µs 里 scalar 占 1.6µs，`mte2`/`mte3` 各 0.7µs）。⛔ 九条候选路线已证伪（§14.5 / §14.9 / §17 / §18.6 / §19.1~§19.3 / §20.8，**都是"核数/barrier/DMA 合并"这三条维**）。⚠️ **这条 ⛔ 清单现在少了一项：§14.5 已在 §22 被翻案并采纳** —— 前向 ROW"每批 L 行 = 1 条连续读 + m 条跨步写"同机 A/B **−6.3%~−23.7%**（单调于每核行数），当年判死它的两条依据分别是 **`blockLen` 单位混用**和**测在 blk=40、每核 1~2 任务的无效点位**；⛔ 仍然关闭的是"**源冻结广播**"（§14.9，一支 DMA 出 m 份副本）。⭐ 由此拿到 µs 档成本模型：**每核发起次数 × ≈52~58ns ＋ 派发地板（1.2µs + 75~90ns/核）** ⇒ 合批之后"少开核"拐点**左移**，按平台 case1=3.66µs 反推它现在 `blk≈27~33` 而榜首那一档对应 `blk≈3~12`。⚠️ ~~平台 `score` 恒为 0（计分自 2026-09-03 停写）~~ **也判负**：停更的只是 `info` 里的缓存数组 `last_submission[]`，排名页 09-06~09-21 都有非零分 ⇒ **我方真实名次 = 第 35 名 / 73.19 分**（榜首 90.49），拟合上**每省 1µs ≈ 5~6 分**（`HANDOVER_DeepSeek_code1.md §2.1`，只用于排优先级）。⇒ **本题现在唯一有名额可拿的就是 µs 地板档**；第七次提交把候选形状类再切掉一块：**判分 case1 不具 `ROW ∧ 整行 ∧ tile≤6144B ∧ 每核≥2 行` 的合批资格**（同一次提交里七条恒等用例全在 ±0.9% 内、case1 只动 −0.5%），剩下三类：`S<40~50` 的前向 STREAM / `D>3072` 的整行 ROW / 每核恰好 1 行的 ROW。**下一步 `code1.md §22.7`**（H3 反向单次 gather 优先 —— 它是唯一能直接命中 case5 的一支；H1 合批态重定 `blk`；两条都能本地真机判，不花提交位）。⚠️ **R15/R16 已经打过 H1 那一支并把它结案**（合批态重定 `blk` = 提交 9 的 **case1 3.66 → 2.22，本题最好成绩**，§23.8d），同时新增两条硬口径：**换一份共跑清单等于换一台机器**（同一段代码绝对值漂 7.4%、谷底能挪一档 ⇒ A/B 只在一次 sweep 内做）、**平台 µs 档在"源码可证恒等"的面上单样本就有 ±4.5% ⇒ 预期位移 <10% 的候选不花提交位**（据此前向 floor 8→12 探针已暂缓，第十次提交的反向律读数按预登记判作废）。⇒ ⛔ **反向 µs 档的"核数轴"已由第十一次提交判死**（§23.19/§23.20：p9 下界 / p10+p12 io 抬升 / p13 m 轴 / p14 多 tile / 本次"把抬升夹住"五路全走，四次提交位、六发扫描，**净位移 0**；这条“正形律”随提交 11 面一起撤回并封存——p16~p19 四轮把整条核数轴判死（§23.24/§23.25））。⇒ ⚠️ **榜面现在挂的是第十二次 = 第十次那一面的逐字节重投**（case1 2.38 / case5 4.16，第 31 名 / 75.50；最好一次读数仍是第十次本尊的 27 名 / 76.29）。⭐ 第十二次是开赛以来第一次**真 A/A**（同一份源、独立判分）：单条 µs 用例跨提交散布 **8.3%**、总分 **±0.8 分** ⇒ **硬门槛改为“预期位移 ≥17% 才花提交位”，10~17% 必须同时有本地测量签名 + 可证恒等对照组**（§23.23）。⛔ **反向 µs 档的核数轴正式封存**（§23.21~§23.25，p16~p19 四轮 102 点）：工作分布逐字节相同、只换“启动多少个块”，同一条形状就走出 **15.5% / 27.1% 的锯齿**（锚臂跨扫描复现 ≤1.2% ⇒ 稳定但无模型）⇒ 空核税判负、v4/v5/v6 三版候选全封、**不许把判分档核数移到未测整数**（已测 10 档里 4 档有毒，而锯齿幅度 ≥ 这条轴能买到的全部收益 ≤15.6%）。⇒ **下一步只剩一支：H3 反向单次 gather**（task #23，换的是“每行几次 DMA 调用”、核数不动 ⇒ 不被锯齿污染；纸面账 m=2/4/8 分别 2.6%/7.9%/18.5%，只有 m≥8 才过门槛，而 case5 的 m 未知）⇒ 本地判符号与幅度、0 提交位。✅ **已判出（`code1.md §23.26`，真机 11:04–11:12 / 12 点同构建 A-B）：反向整行把"每行 m 次读"换成 1 次 gather，实测 −11.6% / −15.4% / −19.7%（m=2/4/8），三条同号且随 m 单调、两臂设备上报核数逐字节相同、13 条数值门全绿 ⇒ R17 第一条真增益，同时把上面那本纸面账低估的 1.6~5.0 倍暴露出来（"只有 m≥8 才过门槛"作废，m=2 已在 10~17% 带）**。**尚未上提交面**：动手前先读 §23.26-4（UB 预算那条 11:44 已按源码复核 ⇒ 从"挂死风险"降级为"覆盖率债"：预算式左边 `2·m·tile + 6·tile` 就是反向真实占用，右边 `131072 < ub_size=196608` 只会漏配不会超配），并补 `tpc=1` 与 D 轴这两条 **0 个数据点**的外推域。细节见 [code1.md](code1.md) §1、§20、**§21**、**§22** 与 **§23.16~§23.26** |
| 第二题 `mhc_sinkhorn` | ✅ **正确性与性能双双收口**。比赛平台**向量版连续三次 5/5**、每条 `precision_ratio=1`：step-5 `6ab0bc78…` → step-6b `6ab0cd43…` → **step-7 `6ab0de76…`（当前在榜版）**；更早那次 5/5 是标量版。性能（本地真机，**fp32 口径**配对 A/B/A）：`1024×8` 总时长 **step-4 116.964 → step-5 37.412 → step-6b 28.163 → step-7 28.445**，`2000×8` 66.249 → **50.737**；⚠️ 历史标量版 3048.97 是 **fp16 墙钟**，与上面跨口径**不可相除**（§11.14 明令）。⭐ 成本模型已钉住（`每趟 V 条数 = 184 + g`、`每条 = 12.8ns + 3.63ns/组`），**四条性能路线全部有结论**（V 几何 / `n<8` 步进 DMA / barrier 摊薄 / 稠密槽）⇒ 已知路线到底。⚠️ 契约缺口按 2026-09-20 裁决**只记录不动**：`normOut`/`sumOut` 未实现、`mask` 未读、`eps` 位置不同、`-inf/nan` 不传播（细节见 [code2.md](code2.md) §1 与 §4.3A） |
| 第三题 `sparse_flash_attention` | ✅ **比赛平台 6/6 全 Pass**（提交 `6aae9fad`；根因 = 空行 LSE max 平台期望 `0.0`、旧参考用 `-2e38`；判分结构靠探针反推）。本地提交源曾落后通过版 5 处，已用通过版 zip 同步并 md5 校验（§5.10.1）——⚠️ **判"是不是通过版"只认 §5.10.1 的四个 md5，`code 3/submit/` 永久作废**。剩：**真机未复验**、性能未动工（**206.9 ms** 纯标量 vs 榜首 2.16~3.54 µs）。⭐ **性能方案以 §14.5 的 P0.5→P4 表为准（取代 §11.3）**，且 §13.14 已用纯算术量出 **P0.5** 挡在一切性能改动之前（host `CalcUbNeed` 把 K/V/kr 写死 2 字节 ⇒ fp32 实例超物理 UB 17,088 B）。⚠️ 真机全量回归 PASS=164 / FAIL=9（8 个已排除 + `w_partneg` 真实 bug，**平台不判、已挂起**）。⛔ 上真机前先过云端 CPU **编译门**（§13.10），但环境 `e6z6k` **已被回收、只能由用户 Start**。细节见 [code3.md](code3.md) §1 |

> 细节与证据见各自的 `codeN.md`。
> ⚠️ **别按旧印象行动**（这三句是给人/AI 的"防呆"，2026-09-21 同步）：
> ① 第一题的真机与平台验证**已经做完**（2026-09-20/21 共**七**次提交，均 8/8），"真机从未验证"是 2026-09-20 早间的状态、**已作废**；
> ② 第二题在榜的**不是标量版**：step-5/6b/7 三个**向量版**已连续拿到 5/5，"303µs 慢 200 倍"这条无形状前缀的历史数**已作废**（`code2.md §11.2`）；
> ③ 第三题**性能方案以 `code3.md §14.5` 那张 P0.5→P4 表为准**，§11.3 已被它取代（§11.2 的 S3"单点收益最大"一并作废）。
