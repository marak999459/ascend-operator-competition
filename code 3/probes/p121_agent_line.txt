| 第三题 `sparse_flash_attention` | **平台 6/6 Pass 至今**，榜上现挂 = **P105 纯 AIV 字节第 15 遍**（同字节 15 遍 Σ 池 65.24…68.06、真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，**抽到池外 ≥1.5 % 就补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **八个平台计量器**（六把量"工作"、P120/P121 两把量"开销"）：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、冷暖溢价 **+0.8 pp ≈ 0**、PV 段 **+2.9 % ≈ 0**、**同核旗标对 127 对 = +2.42 % ⇒ 每对 ≤0.019 % of wall**、**标量下标扫描重读 7 遍 = +1.56 % ⇒ 一遍 ≈0.22 %、带上界 0.29 %**（P112~P121）⇒ ⛔ "平台住在 HBM 那一侧"否证、"少搬字节"架构线死、**整条向量线（三段 ≈11 %）死**、**"减旗标对数"死**、**"聚合掉每 token 那条标量下标读"死**（§5 #25、#26）。剩下 **≈71 % 无价**而候选只剩**一条**：**跨核 barrier** ⇒ 下一发 **P122 = 跨核 barrier 计量器**（⛔ 按**空闲硬旗标 id 成对复制** AIC↔AIV 那轮真握手 ⇒ 语义不变、只多来回，先验 0.42 µs/轮；⛔ 幅度必须先把合读 Σ 推到 ≥6 % 才够格发 —— `#15.87` 的教训。设计在 code3.md §6 P0）。✅ 口径：**Δ 落在噪声带内足以关闭一个杠杆**，P120/P121 两次给出最强形式 —— 探针 Σ **落进同字节池分布内部**（甚至低于池内最高）= **被零假设整个盖住** ⇒ 一发关闭、不补枪；⚠️ **单点越带不算信号**（同字节逐点全距实测已到 **15.6 %**，P121 的 C1 那条 +13.1 % 就是被它挡回去的）。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~15 %、六点合读 ±2 %**。全量口径见 [code3.md](code3.md)（状态板，整读） |
