#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P119 轮次对 probes/LOG.md 的就地订正：索引行重指向 + 两条「已知为错」+ 追加 `### 15.98`。
判据同 p119_docfix.py：锚点必须恰好命中 1 次，否则不写盘。"""
import sys

LOG = sys.argv[1] if len(sys.argv) > 1 else "LOG.md"
CHECK = "--check" in sys.argv

ENTRY = """### 15.98 🚫 P119：PV 段第一次被「换目的端」的复制体定价 ⇒ **平台上 +2.9 %**（本地同臂 +18~33 %）⇒ 整条向量线结掉；顺带把卡了一周的 run-merge 矛盾查明是**误读**

- **改了什么**：`probes/p119_pvrep.py N`（N=复制份数，0=pristine 直拷）把 `SoftmaxPv` 第 5 段 j 循环体整段重跑 N 遍，**复制体的目的端 = `kfBuf_` 末行**（`vf[(scGrp_-1)*rowC]`），源操作数 / 宽度 / 条数 / `p.GetValue` 的位置一字不动 ⇒ 对输出逐位惰性。⚠️ 与 `#15.97` 补② 的计划差一处：当时写「写 `sBuf_`」，实际**不能** —— 收口的 `WriteOut` 要读回 `sc`，而 `kfBuf_` 的每一个消费者都是**先覆写后读**（下一 chunk 的 `WidenToF32` / 末 chunk 的 `PackFromF32`）⇒ 只有 `kfBuf_` 这一处安全。**没有新增任何 UB/TBuf 分配**（`InitBuffer` 命中数 = pristine 的 13）⇒ 环、tiling、UB 预算三个混淆变量一个都不动。
- **惰性的证据（三层，全绿）**：① 本地**四臂**（x1/x2/x3/x5）× 8 档 × out/max/sum 对一次性 `golden_p119/` 全 `逐位一致`（含 285 MB 池的 `r64c65` 与 `w3`）；② 提交态 `p32_gate` = 13 真 expect × 双 dtype 全 `超差 0` + 对**耐久** golden 逐位一致 + GATE2 14 档全 0，闸门 `MARK=2 SK=1 REAL=1 BUFF=13 RESIDUE=0 FORBID=0 SOCEDEX=0`，dry-run 四枚 `626671b5/f2b28a86/1046b349/3a8f5305` 与本地逐个吻合；③ 平台 `Pass` 6/6、`precision_ratio` 六点全 **1**。
- **计量器本身先标定**（「惰性重复」族第一次带内建自检）：Δ 在复制份数上**严格线性** —— N2/N1 = **1.958~1.991**、N4/N1 = **3.902~3.981**（8 档全部，理想值 2.000/4.000）⇒ 无一笔固定成本、无超线性 ⇒ 加进去的那一段真的只是把同一段多跑几遍。本地价：`p6` **+30.7** / `big1` **+29.7** / `w3` **+33.4** / `g64c1` **+18.4** / `v64c8` **+20.2 %**（每多跑一遍 PV）。
- **读数（平台）** 探针 `6ab486b6694b590c3cd06246` = `[7.58, 8.26, 12.82, 13.44, 10.62, 15.26]`、Σ **67.98**、榜上 21.84；同字节池至此 **13 遍**（第 13 遍 = 本轮回发 `6ab487a6694b590c3cd0e9dd`，`Pass` 6/6、Σ **66.44**、榜上 **21.91**）⇒ 池均值 66.04 ⇒ **Δ119 = +2.94 %**（对相邻两遍基线分别 +2.32 / +3.53 %）。逐点 Δ 对第 13 遍 = `−6.9/−3.7/+4.6/+9.3/−1.3/+6.0 %`，全在 ±5~10 % 单点带内、符号都不齐 ⇒ **按 Δ ≥ 6 % 的门判不出非零**。本地/平台 = 2.9∶30.7 ≈ **0.095**。
- **判①（本轮真正拿到的是一条口径，不是一个数）**：**「判不出非零」≠「什么也判不出」** —— 惰性复制往那段**只加长度、不加真实那段的依赖链**，所以 Δ 小只有两种成因（那段本身小 / 它躲在别人的影子里），**两种都意味着删掉它也省不出比 Δ 更多的墙钟**。⇒ 噪声在「关闭一个杠杆」这件事上是**单向**的：小 Δ = 可靠的否定，只有「打开」才需要过 6 % 的门。⇒ **PV 段关闭**，连同它内层那条从没价过的 **97 ns 标量 UB 读**一起（这段是「64 条向量 + 1 条标量读」的混合体，复制体把两者都复制了 ⇒ 一次发次买两个信息，原计划兑现了，只是答案是负的）。**整条向量线就此结掉**：score 6 % + softmax 前段 2.4 % + PV ≤3 % ≈ **11 % of wall**（而它们本地的价是 3~10 倍）。
- **判②（run-merge 那条「自相矛盾」结案 = 一根轴被当成另一根）**：卡了 #16 一周的反证是「`p111_ladder.txt` 等字节下命令少 128× 反而慢 2.7×」。三条查完它不成立：① **host 选档在这族上逐位相同** —— `sparse_size = min(sparse_count·sbs, KV_S)`，而 `sparse_count = GetDim(3)` 恒 = 2048 ⇒ `s ≥ 4` 全被钳到 **8192** ⇒ 同一份 `(nb, n_blk, ks)`；② **字节也真相同** —— `gen_sbs.py` 里 `cnt = min(nblk, CN, S2/sbs)` 而 `nblk` 当初就填成 `S2/sbs` ⇒ `s ≥ 4` 每一档都覆盖**整个 KV 池**；③ 决定性的：沿阶梯往上「每 chunk 游程条数」单调**降**（`n_blk=48` 由 `p39_pickcheck.txt` 当场读出、`s ≥ 4` 各档选档相同 ⇒ 一个 chunk 跨过的表项数从 `s4` 的 ~12 降到 `s64`/`s128` 的 1~2），而时间**不跟着降**：`s8`→`s16` 条数减半、时间只动 0.6 %（0.2977→0.2959 ms），`s64`→`s128` 条数更少反倒慢 2×（0.4205→0.8212）⇒ 条数若为主因，时间该沿阶梯单调降，它没有 ⇒ **这条阶梯上「条数」与时间没有对应关系**，它动的是别的东西（候选 = 每命令宽度 / 表项数；机制**没查明、也不需要查明** —— 只要它不是条数轴，就不能拿它否证「并条省钱」）。⇒ 干净的条数轴只有 **P104 的合并阶梯**（形状全钉死、只改邻接 ⇒ 单调，**5.2 ns/条**）与 **P115**（平台 ×2 条同字节 = +12.9 pp）。**合并的平台天花板 = 12.9 pp × 跨表项邻接率**，而基线 `CopyGm2Ub(…, run*D_)` 块内早已并成一条、平台 `sbs=1` ⇒ 邻接率 = `1 − count/KV_S`（官方「抽样后排序」给 ~25 %，**平台真实分布未知**）⇒ **≈3~4 pp ≈ +0.6 分，且按判① 的口径这个量既测不出「有肉」也测不出「没肉」**（要它 ≥6 % 才谈得上开）⇒ #16 回到判死、不排队。
- **判③（候选集第一次收窄到一类）**：已价 = score 6 + softmax 前段 2.4 + PV ≤3 + gather 17.4~18.2 ⇒ **≈29 %**，而「干活的四段」已经全部量完 ⇒ 剩下的 **≈71 %** 只能是**开销**：**(a) 每 chunk 的同核 `SetFlag/WaitFlag` 对**（主循环 4 对/chunk，L965~988；§4 那条 0.42 µs 是**跨核**的价，同核这一对的单价从没量过）、**(b) 标量下标扫描**（`#15.97` 补② 曾给过它一个不利推理 —— 「读到 ≈0 什么也判不出」—— 按判① 那条推理已作废 ⇒ (b) 重新可测）、**(c) 跨核 barrier 轮次**。⇒ **下一发 P120 = (a)**：chunk 尾追加 N 对**未用过**的 event 线（`MTE2_V(4)`，本文件只用了 0/1）⇒ **零内存写、零 UB 分配、零数据依赖 ⇒ 惰性是构造级的**（前六把尺子都得论证「这块内存没人读」，这把不用）。判据：Δ(1 对/chunk) ≥ 6 % ⇒ 每 chunk 税是大头，方向 = 减对数（K/V 的 wait 合成一对、或让下一 chunk 的 wait 覆盖本 chunk 的 drain）；Δ ≈ 0 ⇒ 只剩 (b)(c)。⛔ P117（本地消融阶梯）继续排最后。

"""

E = []

E.append((
"""**`15.98`** P119（下一发：`SoftmaxPv` 第 5 段 PV 累加 ×2 打进 scratch，第一次给"带累加器的段"定价）（L4893）。 编号从 `15.99` 起。""",
"""**`15.98`** P119 ⇒ **PV 段平台价 +2.9 %**（本地同臂 +18~33 %，计量器在复制份数上严格线性）⇒ **整条向量线（三段 ≈11 % of wall）结掉**，并留下一条新口径：**"Δ 在噪声带内"足以关闭一个杠杆、只有"打开"才要过 6 % 的门**；顺带把 #16 卡了一周的 run-merge 反证查明为**误读**（sbs 阶梯不是条数轴：条数减半时间只动 0.6 %、条数更少反倒慢 2×）⇒ 合并天花板 ≈3~4 pp、不排队。下一发 **P120 = 每 chunk 同核旗标对定价**（第一把量"开销"不是"工作"的尺子）（L4895）。 编号从 `15.99` 起。"""))

E.append((
"""⇒ 带按 **±2 %** 用；**现挂落在池外 ≥1.5 % 就补一枪回发** |""",
"""⇒ 带按 **±2 %** 用；**现挂落在池外 ≥1.5 % 就补一枪回发** |
| "惰性复制读到 ≈0 什么也判不出"（`#15.97` 补② 写下，并据此把 P119 限定成"只有大 Δ 才算数"） | **方向反了**：复制体只往那段**加长度、不加真实依赖链** ⇒ Δ 小的两种成因（那段小 / 它在别人影子里）**都**意味着删它省不出超过 Δ ⇒ **小 Δ = 可靠的否定证据**，只有"打开一个杠杆"才需要 Δ ≥ 噪声门（`#15.98` 判①） |
| "`p111_ladder.txt` 等字节下命令少 128× 反而慢 2.7× ⇒ 并条不省"（`#15.95`/`#15.96` 拿来卡住 #16 的那条反证） | 那条阶梯**不是条数轴**：条数沿阶梯单调降而时间不降（`s8`→`s16` 条数减半、时间只动 0.6 %；`s64`→`s128` 条数更少反倒慢 2×）⇒ 拿它当"并条不省"的证据，等于把一根没认出来的轴读成条数轴——它既不能证有也不能证无（顺带重数：那句「少 128×」是**每条宽度**之比 `s1`∶`s128`，条数之比只有 32×）；而 host 选档（`sparse_size` 被 `min(count·sbs, S2)` 钳成 8192）与总字节在 `s ≥ 4` 各档间被证明**逐位相同 / 等量** ⇒ 它动的是别的东西。干净的条数轴只有 P104 的合并阶梯（5.2 ns/条）与 P115（`#15.98` 判②） |"""))

src = open(LOG, encoding="utf-8").read()
bad = 0
for i, (old, new) in enumerate(E, 1):
    n = src.count(old)
    if n != 1:
        print("ANCHOR FAIL #%d hits=%d: %r" % (i, n, old[:56]))
        bad += 1
print("条目=%d 锚点失败=%d  新条目=%d 字符" % (len(E), bad, len(ENTRY)))
if bad or CHECK:
    sys.exit(1 if bad else 0)
for old, new in E:
    src = src.replace(old, new, 1)
if not src.endswith("\n"):
    src += "\n"
src += ENTRY
open(LOG, "w", encoding="utf-8").write(src)
print("LOG 已写：+%d 字符" % len(ENTRY))
