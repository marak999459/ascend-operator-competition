| **28** | 🔴 **“P123 的 +22.7 % = 平台依赖链的价”（还不能用）+ 一条已成立的口径** | 那个读数被一个**从没量过的数**污染：预登记的 null 假设平台每 chunk `m ∈ [32,64]`，而这个数进过 §1.1 每一个 PV 折算（本地那份标定顺手量出 **m = 64**，平台仍是未知）。🔴 **已经成立的部分** = 同一对双臂（P119 死端 vs P123 活端、加同样条数的活）在**本地 8/8 档同价**（差 <4 pp）、在平台上差 **8×** ⇒ 这是 #21 那条“本地不能外推”的**最强一次**：不是比例失真，而是同一份机器码、同样的条数，两侧价格差一个数量级。⇒ 在 P124 把「m」与「目的端死活」分开之前，P123 只能引用成“活端一圈 ≤0.36 % of wall”这个**上界**，⛔ 不许引用成“链是钳子” | `#15.102` |
| — | ~~**P119 = PV 段 ×2（目的端 = `kfBuf_` 末行）**~~ | 本地 +18~33 %（复制份数上严格线性）／平台 **Σ +2.9 %** ⇒ 按 §1.2 的关闭口径结掉向量线三段。🔴 **但它是一把“死端尺子”，这 2.9 % 的解释权现在整体押在 P124 上** | 🔴 已读 `#15.98`，口径待 P124 |
| — | ~~**P120 = 每 chunk 同核旗标对 ×127**（未用过的 `SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 构造级惰性）~~ | **每对 ≤0.019 % ⇒ 4 对/chunk ≤0.076 %**，而探针 Σ 67.64 低于同字节池内最高 68.06 ⇒ **被零假设整个盖住** ⇒ 减对数判死（全表在 §1.1） | 🔴 已读 `#15.99` |
| — | ~~**P121 = 标量下标扫描计量器**（两条剩候选里的 (b)）~~ | 重读 7 遍 ⇒ **Σ +1.56 %** ⇒ 一遍 ≈0.22 %、**上界 0.29 % of wall** ⇒ (b) 出局。副产品：不幂等那段也能有计量器（只重读地址 + 可证不可达的 sink），代价是**必须**先本地标定证明它没被 CSE | 🔴 已读 `#15.100` |
| — | ~~**P123 = 依赖链计量器**（`SoftmaxPv` 尾 63 圈 `Muls(o_i, o_i, 1.0f, rowC)`，与 P119 只差“依赖”一个变量）~~ | 读完了，且是这一族**第一把读到非零**的尺子：平台 **Σ 81.18 = +22.7 %**、六点**全正且强分级**（C6 +58.7 / C4 +36.9 / C5 +31.8 / C3 +16.6 / C1 +12.1 / C2 +5.6 %），本地四臂 0/7/31/63 给 Δ∝N（8/8 偏差 ≤15 %）+ 8 档逐位一致 ⇒ 尺子确实在工作。🔴 **但读数不能引用成“链是钳子”** —— null 里那个“平台 m ∈ [32,64]”从没量过，与 P119 合读还差一个方程 ⇒ 解释权整体转交 P124（§5 #28） | 🔴 已读 `#15.102` |
| **P0** | **P124 = P123 的死端孪生**（同位置/同指令/同宽度/同条数/同链距，**只把目的端 `o` 换成 `kfBuf_` 的行**） | 补上第三个方程 ⇒ 一发同时解出 **m = 182.7/Δ124** 与 **p_活∶p_死 = 22.7∶Δ124**。**Δ ≤8 %** ⇒ 死活是真实的轴 ⇒ P119/P112/P116 三把押在“随后被覆写的存储”上的尺子整体降为下界、§1.1 那句“向量线合计只值 ≈11 %”作废、PV 段按活端价 ∈ [8 %, 23 %] 重开；**Δ ≥12 %** ⇒ 不是轴 ⇒ m ≤ 15 与 host 的 `NBLK_MIN=16` 正面冲突 ⇒ “chunk 填得满”这个模型错、每 chunk 固定税那一族口径全部重算。⛔ 硬前置 = 本地两臂 Δ124 显著非零（若本地就被擦掉 ⇒ 平台读数失去解释力 ⇒ 改设计，不发） | ⬜ **下一做** |
