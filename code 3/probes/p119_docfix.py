#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P119 轮次对 code3.md / AGENT.MD 的**就地**订正（判据：每条锚点在文件里必须恰好命中 1 次，
否则整脚本不写盘）。先 `--check` 再写。"""
import sys

DOC = sys.argv[1] if len(sys.argv) > 1 else "code3.md"
AGT = sys.argv[2] if len(sys.argv) > 2 else "AGENT.MD"
CHECK = "--check" in sys.argv

E = []   # (file, old, new)

# ---------- §1.1 榜上现挂 / 历史最高 ----------
E.append((DOC,
"| **榜上现挂** | **P105 字节第 12 遍** `[8.06, 8.58, 11.9, 12.1, 10.58, 14.44]`（Σ **65.66** = 池内第二低）⇒ 榜上 `22.04` / `#35`，发次 `6ab47e20694b590c3ccc4df8`；历史最高仍是 `22.29`/`#33` 那一遍。",
"""| **榜上现挂** | **P105 字节第 13 遍** `[8.14, 8.58, 12.26, 12.3, 10.76, 14.4]`（Σ **66.44** = 池内第 5 低 ⇒ 按"高出池中位 ≥1.5 % 才补枪"这条**不补**）⇒ 榜上 `21.91` / `#35`，发次 `6ab487a6694b590c3cd0e9dd`（P119 探针读完立刻回发）；历史最高仍是 `22.29`/`#33` 那一遍。"""))

E.append((DOC,
"**同字节池现已 12 遍**：Σ 均值 66.01 ms、全距 **65.50 … 68.06 = 3.9 %**",
"**同字节池现已 13 遍**：Σ 均值 66.04 ms、全距 **65.50 … 68.06 = 3.9 %**"))

# ---------- §1.1 段占比轴 ----------
E.append((DOC,
"**段占比轴 = 镜像**（P112 + P114 + P116，同一惰性机制）",
"**段占比轴 = 镜像**（P112 + P114 + P116 + P119，同一惰性机制）"))

E.append((DOC,
"""但 `SoftmaxPv` 前段翻倍**两侧同型**（本地 +2.2~2.7 / 平台 +2.4 %）⇒ \"镜像\"是**搬运∶计算**那根轴特有的，不是普遍规律。""",
"""但 `SoftmaxPv` 前段翻倍**两侧同型**（本地 +2.2~2.7 / 平台 +2.4 %）⇒ \"镜像\"不是普遍规律；🔴 **P119 又把镜像坐实在最大的那段向量上**：PV 累加翻倍本地 **+18~33 %** / 平台 **+2.9 %**，本地∶平台 = **10∶1**（`LOG#15.98`）。"""))

E.append((DOC,
"① 向量微优化（提加宽出循环 / 组宽 48→24）瞄 score 段、softmax 侧也没有落点（两段合起来平台上只 ≈8 %，#19）",
"① **向量线整体关死**：score / softmax 前段 / PV 三段在平台上合计 **≈11 %**，本地那三段的价是它的 3~10 倍（#19、#24）"))

E.append((DOC,
"""⚠️ 唯一被 P115 改口的是 run-merge：当年判死用的\"AIV 调用税只 5.2 ns/条\"是本地口径，平台上那一侧占 gather 的 72 %（#16）。""",
"⚠️ run-merge 那笔 P115 改口**已就地结掉**（#16）：卡它的那条自相矛盾查明是**误读一根轴当成了另一根**，而它的平台天花板 = 12.9 pp × 邻接率 ≤ 4 pp = 一发测不出来的量。"))

# ---------- §1.1 量级问题 ----------
E.append((DOC,
"""**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + 搬运 18 % = 26 % ⇒ **≈74 % 的墙没有任何计量器打开过**，3.6× 就藏在这块里。9/24 那三条\"去拿它\"的发次两成一败：""",
"""**剩下的量级问题（口径已换）**：计算 6 % + softmax 前段 2.4 % + PV ≤3 % + 搬运 18 % = **≈29 %** ⇒ **≈71 % 无价**，3.6× 就藏在这块里。🔴 **而这 71 % 的候选集现在只剩\"开销\"一类**：四类\"干活\"的段（score / softmax 前段 / PV / gather）全部量完且全部 ≤18 %，剩下没被任何尺子碰过的只有**每 chunk 的同核旗标对**（主循环 4 对、单价从没量过）、**标量下标扫描**、**跨核 barrier 轮次**。9/24 那几条\"去拿它\"的发次两成一败加一判零："""))

# ---------- §1.1 P119 bullet（整条替换）+ 新 P120 ----------
E.append((DOC,
"""- 🔎 **还剩 ≈74 % 无价**，而 P118 之后\"惰性重复到头了\"这句话**也过期了**：它成立只是因为前四把尺子都只会复制**幂等**的段。**新解锁**：带累加器的段照样能复制 —— 把同一批向量指令的**目的端**改指到一块 scratch（源操作数、宽度、条数一字不动）⇒ 对输出逐位惰性。⇒ 下一发 **P119 = `SoftmaxPv` 第 5 段（PV 累加）×2 打进 scratch**：它是全 kernel 最大的一块 AIV 工作（每 head·token 64 条 `Axpy`）**且每次 `Axpy` 前带一条 97 ns 的标量 UB 读**（§4，从没在这个位置上量过），也是 `#15.94`/`#15.95` 之后唯一\"既不能靠重复、也不能靠搬运解释\"的候选。⛔ **P117**（本地消融阶梯）继续降级：`#15.94` 已证本地段占比不可外推。""",
"""- ✅ **P119（给 PV 段定价）有答案，而且是负的**（`LOG#15.98`、`probes/p119_platform.txt`）：复制体目的端 = `kfBuf_` 末行（源、宽度、条数一字不动 ⇒ 逐位惰性，**不新增任何 UB 分配**）⇒ 本地四臂在复制份数上**严格线性**（N2/N1 = 1.958~1.991、N4/N1 = 3.902~3.981，无一笔固定成本）、8 档 × 三输出对 golden 逐位一致；平台 PV×2 = **Σ +2.9 %**（对 13 遍池均值；相邻两遍基线给 +2.3 / +3.5 %）⇒ **落在 ±2 % 带内、按 Δ ≥ 6 % 的门判不出非零**。⛔ 但它照样**关掉一根杠杆**，理由是新口径（§1.2）：**复制只加长度、不加真实那段的依赖链 ⇒ Δ 小 = 这段本来就躲在别人的影子里 = 删掉它也省不出比 Δ 更多的**。⇒ **整条向量线（score 6 % + softmax 前段 2.4 % + PV ≤3 %）在平台上合计 ≈11 %**，与它本地 3~10 倍的价无关 ⇒ 向量侧不再发探针。\n- 🔎 **下一发 P120 = 给\"每 chunk 同核旗标对\"定价**（第一把量**开销**而不是**工作**的尺子）：主循环每 chunk 4 对 `SetFlag/WaitFlag<V_MTE2/MTE2_V>`（L965~988），而 §4 那条 0.42 µs 是**跨核**的价、同核这一对的单价**从没量过**。做法 = 在 chunk 尾追加 N 对**未用过的 event 线**（`MTE2_V(4)`）⇒ 零内存、零 UB、零数据依赖 ⇒ 惰性是构造级的。判据：Δ(1 对) ≥ 6 %/4 ⇒ 每 chunk 税是大头，方向换成**减旗标对数**（那是唯一一处\"既不在搬运也不在计算\"的可改代码）；Δ ≈ 0 ⇒ 71 % 只剩标量扫描与跨核 barrier 两候选。⛔ P117（本地消融阶梯）继续降级。"""))

# ---------- §1.2 噪声口径 ----------
E.append((DOC,
"P108（+45~71 % = 6~9σ）与 P111（级间距 15~20 pp + 六点同号）才定了案（`LOG#15.87`→`#15.91`）。",
"""P108（+45~71 % = 6~9σ）与 P111（级间距 15~20 pp + 六点同号）才定了案（`LOG#15.87`→`#15.91`）。🔴 **但\"带内 = 什么也判不出\"只对了一半**（P119 教的）：判一个杠杆**打开**要 Δ ≥ 6 %，判它**关闭**不需要 —— 惰性复制只往那一段**加长度、不加真实依赖链**，所以 Δ 小只有两种可能（这段小 / 这段躲在别人的影子里），**两种都意味着删它省不出超过 Δ**。⇒ 噪声在这里是**单向**的：小 Δ 是可靠的否定，大 Δ 才需要复核。"""))

# ---------- §4 单价表 ----------
E.append((DOC,
"""⇒ **条数确实是成本单位**（本地冷档 gather 墙约一半在条数上），⛔ 但**不得反读成\"并条就省\"**：等字节下 `sbs` 128× 少的命令反而慢 2.7×（`p111_ladder.txt`）""",
"""⇒ **条数确实是成本单位**，⚠️ 但唯一干净的条数轴是 **P104 的合并阶梯**（形状全钉死、只改邻接 ⇒ 单调，5.2 ns/条）与 **P115**（平台 ×2 条同字节 = +12.9 pp）。🔴 **`p111_ladder.txt` 那条 sbs 阶梯不能当\"并条不省\"的证据**（`#15.98` 结案）：它等字节、host 选档也逐位相同（`sparse_size` 被 `min(count·sbs, S2)` 钳成 8192），**但每 chunk 游程数与时间根本不对应** —— `s64`/`s128` 都是 1 游程/chunk 却差 **2×**，`s8` 比 `s16` 多一倍命令反倒慢 **25 %** ⇒ 它动的是别的东西，不是条数"""))

E.append((DOC,
"""⇒ 这一段是\"64 条向量指令 + 1 条 97 ns 标量读\"的混合体，**从没单独定过价 = P119 的靶**""",
"""⇒ 那\"64 条向量 + 1 条标量读\"的混合体已由 P119 **整体**定价：平台上翻倍只 **+2.9 %**（本地同臂 +18~33 %）⇒ **含标量读在内这一段都不是杠杆**（`LOG#15.98`）"""))

E.append((DOC,
"| 旗标一轮（`CrossCoreSetFlag` + `Wait`） | ≈0.42 µs/轮 | 每 chunk 固定税的成分；该税**饱和**（§1.3） |",
"| 旗标一轮（**跨核** `CrossCoreSetFlag` + `Wait`） | ≈0.42 µs/轮 | 每 chunk 固定税的成分；该税**饱和**（§1.3）。⚠️ **同核 `SetFlag/WaitFlag`（V↔MTE2/MTE3）一轮的单价从没量过**，而主循环每 chunk 就压着 **4 对**（L965~988）⇒ **P120 的靶** |"))

E.append((DOC,
"""🔴 P118 之后这张\"候选清单\"少了一项、多了一项：**\"访存层/HBM\"已被正面划掉**（冷溢价 ≈0），而**\"带累加器的向量段（PV）\"从没被列进来过**——因为以前以为它不能惰性复制，现在知道换个目的端就能（§6 P0 = P119）。""",
"""🔴 P118 + P119 之后这张\"候选清单\"划掉两项：**\"访存层/HBM\"**（冷溢价 ≈0）与**\"带累加器的向量段 PV\"**（翻倍只 +2.9 %，§6 已读）。⇒ 干活的四段全部量完、合计 ≈29 %，**没被碰过的只剩开销**：同核旗标对（P120）/ 标量下标扫描 / 跨核 barrier。"""))

# ---------- §5 排除清单 ----------
E.append((DOC,
"""顺手那笔\"命令减半换字节翻倍\"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |\n""",
"""顺手那笔\"命令减半换字节翻倍\"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |\n| **24** | 🔴 **\"PV 段（含其内层那条 97 ns 标量 UB 读）是平台上的大杠杆\"**（`#15.96`/`#15.97` 时代它是最后一块没价的大 AIV 工作，本地占 AIV 31~34 %） | P119 惰性复制 ×2（目的端 = `kfBuf_` 末行，不新增 UB）⇒ 平台 **Σ +2.9 %**，本地同臂 **+18~33 %** ⇒ 比值 0.095。按 ±2 % 带它\"判不出非零\"，但**关闭不需要过门**（§1.2 新口径：只加长度不加依赖 ⇒ Δ 小 = 这段在影子里 = 删它也省不出更多）⇒ 连同 #19 把**整条向量线**结掉：三段合计 ≈11 % of wall | `#15.98` |\n"""))

E.append((DOC,
"""| 16 | **P104 = gather 跨块搬运聚合（run-merge）** ⚠️ **判死已降级为\"未在平台定价\"** | 当年判死的定价用的是**本地**口径（\"满连续只值 ≤4 %\"\"AIV 调用税 5.2 ns/条 ⇒ 那面墙不存在\"），而 P115 在平台量到\"每次调用\"侧占 gather 的 **≈72 % = 全墙的 12.9 pp（绝对上限 ≈2.2 分）** ⇒ 那句\"墙不存在\"在平台上**没被验证过**。两条未清的前置：① 形状结论仍在（`sbs=1` + 逐 token 随机索引 ⇒ 可合并的相邻段本就稀少，`rows=4` 那族实测收益恰好为零）；② **反证未解释**：等字节下命令少 **128×** 反而慢 **2.7×**（`p111_ladder.txt`）⇒ 那个实验里\"条数\"不是唯一自变量。字节已还原，备份 `probes/backup/p104_pre_runmerge/` | `#15.84` + `#15.93` + `#15.95` |""",
"""| 16 | **P104 = gather 跨块搬运聚合（run-merge）** 🔴 **两条前置全部结清，回到判死** | 当年判死用本地口径（\"满连续只值 ≤4 %\"、AIV **5.2 ns/条**），P115 改口说平台调用侧占 gather 的 ≈72 % ⇒ 重开过一次。**现在按算术读**：合并能省的 = `12.9 pp × 可合并邻接率`，而基线的 `CopyGm2Ub(kb[done*D_], …, run*D_)` **块内早已并成一条** ⇒ 只剩\"跨相邻表项\"那一块；平台 `sbs=1`（P111）⇒ 邻接率 = `1 − count/KV_S`，官方生成器\"抽样后排序\"给 ~25 %（**平台真实分布未知**）⇒ 天花板 **≈3~4 pp ≈ +0.6 分**。前置②那条反证 = **误读**：`p111_ladder.txt` 的 `s64`/`s128` 每 chunk 游程数**相同**却差 2×、`s8` 命令多于 `s16` 反倒慢 ⇒ 它不是条数轴（细节 `#15.98`）。⇒ 一个 ≤4 pp、且**按新口径连测都测不出来**（判关闭不需要过门，判有肉需要 ≥6 %）的改动不排队。字节已还原，备份 `probes/backup/p104_pre_runmerge/` | `#15.84` + `#15.93` + `#15.95` + `#15.98` |"""))

# ---------- §6 ----------
E.append((DOC,
"""| **P0** | **P119 = 给 PV 段定价（`SoftmaxPv` 第 5 段 ×2，目的端改指 scratch）** —— \"惰性重复\"族的**第二口气**：以前只会复制幂等的段，带累加器的段用 **scratch 目的端**一样能复制（源、宽度、条数一字不动 ⇒ 对输出逐位惰性） | 一发 ≈1 个信息单位。为什么是它：① 它是**全 kernel 最大的一块 AIV 工作**（每 head·token 64 条 `Axpy`）且本地价里 PV 占 AIV 31~34 %，而**平台侧从没量过**；② 每条 `Axpy` 前跟一条 **97 ns 标量 UB 读**（§4）⇒ 若平台也贵，这一段的构成里\"标量读\"可能比向量本身还大，那是**唯一一条既不属于搬运、也不属于 SIMD 吞吐、且可改**的候选；③ 判据按新的 ±2 % 带：Δ ≥ 6 % 才算非零。⚠️ **已知局限**：复制出来的 `Axpy` 目的端不同 ⇒ `o` 的读写流被复制、`p` 的标量读被复制，唯一不复制的是 `o` 的**依赖链**（真实那段是串在 `o` 上的）⇒ Δ 是**下界**，读到大 Δ 才算数 | 🔴 **下一做** |""",
"""| — | ~~**P119 = 给 PV 段定价（`SoftmaxPv` 第 5 段 ×2，目的端 = `kfBuf_` 末行）**~~ —— \"惰性重复\"族的第二口气：带累加器的段换目的端照样能复制 | 读完了：本地 **+18~33 %** 且**在复制份数上严格线性**（四臂 8 档，比值 1.96~1.99 / 3.90~3.98）⇒ 计量器可信；平台 PV×2 = **Σ +2.9 %**（13 遍池、±2 % 带 ⇒ 判不出非零）。**按 §1.2 新口径这足够关闭**：只加长度不加依赖 ⇒ 删它省不出超过 Δ ⇒ **向量线（score 6 + softmax 前段 2.4 + PV ≤3 ≈11 %）整条结掉**，其中内层那条 97 ns 标量 UB 读也一并定价 | 🔴 已读 `#15.98` |\n| **P0** | **P120 = 给\"每 chunk 同核旗标对\"定价** —— 第一把量**开销**而不是**工作**的尺子：chunk 尾追加 N 对**未用过**的 `SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 零内存、零 UB、零数据依赖 ⇒ **构造级惰性**（前六把尺子都得靠\"这块内存没人读\"来论证，这把不用） | 已价四段合计 ≈29 % ⇒ **剩下的 ≈71 % 只能是开销**，而同核旗标是其中唯一\"单价从没量过、又确定每 chunk 付 4 次\"的一项（§4 那句 0.42 µs 是**跨核**）。本地价：每 chunk 税**饱和** ⇒ 这一发也顺带第一次直接读到\"平台的每 chunk 固定税\"而不是靠形状外推。判据：Δ(1 对/chunk) ≥ 6 % ⇒ 税是大头，方向 = **减对数**（把 K 与 V 的 wait 合成一对、或干脆让下一 chunk 的 wait 覆盖本 chunk 的 drain）；Δ ≈ 0 ⇒ 只剩标量扫描 + 跨核 barrier 两候选 | ⬜ **下一做** |"""))

E.append((DOC,
"""| — | run-merge（P104，把碎 gather 并成长拷贝）—— **是本题唯一还活着的大杠杆，但被一条自相矛盾卡住** | P115 的 72 % 说\"每条覆盖更多字节\"有肉（上限 2.2 分），可 `p111_ladder.txt` 那条等字节阶梯上 `sbs` 少 **128×** 条命令反而慢 **2.7×** ⇒ \"并条\"的收益在同一份代码里被自己否掉过一次。**开工前必须先解释那条矛盾**（候选：合并路径改了 UB 暂存/环占用，或 `pendRuns` 变长把 stage 环挤瘦 ⇒ 动的不是条数一个变量） | ⬜ 先解释矛盾，再决定 |""",
"""| — | ~~run-merge（P104，把碎 gather 并成长拷贝）—— 曾被写成\"唯一还活着的大杠杆，被一条自相矛盾卡住\"~~ | **矛盾已解释、且解释完它就变小了**：① 那条反证（等字节少 128× 命令反倒慢 2.7×）**不成立** —— sbs 阶梯上\"每 chunk 游程数\"与时间不对应（`s64`=`s128`=1 游程却差 2×；`s8` 命令多于 `s16` 反倒慢），它动的不是条数；② 干净的条数轴（P104 合并阶梯）给的单价是 **5.2 ns/条**，乘平台调用侧的 12.9 pp 与**跨表项**邻接率（`sbs=1` ⇒ 至多 ~25 %，且平台分布未知）⇒ **天花板 ≈3~4 pp ≈ +0.6 分、且过不了 6 % 的判别门**。⇒ 降级为\"不值得单独一发\" | 🔴 已结 `#15.98` |"""))

# ---------- AGENT.MD ----------
E.append((AGT,
"""榜上现挂 = **P105 纯 AIV 字节**（同字节 12 遍真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，但**全距 3.9 % 时\"抽到池外\"就该补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **五个**惰性计量器：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、**gather 的冷暖溢价 +0.8 pp ≈ 0**（P112/P114/P115/P116/P118）⇒ ⛔ **\"平台住在 HBM 那一侧\"已正面否证**，\"少搬字节\"整条架构线（跨行去重 / KV 常驻 / 粗化 over-fetch）无条件死。剩下 **≈74 % 无价**，那才是 3.6× 的藏身处。✅ \"惰性重复\"**没到头**：带累加器的段换个目的端照样能复制 ⇒ 下一发 **P119 = `SoftmaxPv` 第 5 段（PV）×2 打进 scratch**（设计在 code3.md §6 P0）。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~10 %、六点合读 ±2 % ⇒ 两发单遍探针之间 <5 pp 不可判**。""",
"""榜上现挂 = **P105 纯 AIV 字节**（同字节 13 遍真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，但**全距 3.9 % 时\"抽到池外\"就该补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **六个**惰性计量器：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、冷暖溢价 **+0.8 pp ≈ 0**、**PV 段 +2.9 % ≈ 0**（P112/P114/P115/P116/P118/P119）⇒ ⛔ **\"平台住在 HBM 那一侧\"正面否证**、\"少搬字节\"整条架构线死、**整条向量线（三段合计 ≈11 %）也死**。剩下 **≈71 % 无价**，而候选集已收窄到**开销**一项 ⇒ 下一发 **P120 = 每 chunk 同核 `SetFlag/WaitFlag` 对定价**（第一把量开销的尺子，设计在 code3.md §6 P0）。✅ 两条新口径：**Δ 在噪声带内足以关闭一个杠杆**（复制只加长度不加依赖 ⇒ 删它省不出超过 Δ），以及卡了一周的 run-merge 反证是**误读**（sbs 阶梯不是条数轴）⇒ #16 结掉。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~10 %、六点合读 ±2 %**。"""))

src = {DOC: open(DOC, encoding="utf-8").read(), AGT: open(AGT, encoding="utf-8").read()}
bad = 0
for i, (f, old, new) in enumerate(E, 1):
    n = src[f].count(old)
    if n != 1:
        print("ANCHOR FAIL #%d hits=%d file=%s: %r" % (i, n, f, old[:60]))
        bad += 1
print("条目=%d  锚点失败=%d" % (len(E), bad))
if bad or CHECK:
    sys.exit(1 if bad else 0)
for f in (DOC, AGT):
    for _, old, new in [e for e in E if e[0] == f]:
        src[f] = src[f].replace(old, new, 1)
    before = len(open(f, encoding="utf-8").read())
    open(f, "w", encoding="utf-8").write(src[f])
    print("%-9s bytes %d → %+d" % (f, before, len(src[f]) - before))
