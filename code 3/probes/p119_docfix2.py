#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P119 轮次对 code3.md / AGENT.MD 的**就地**订正（第二轮，压缩版：p119_docfix.py 的条目太长，
本轮纪律是"状态板只换口径、证据一律指 LOG#15.98"）。判据：每条锚点恰好命中 1 次，否则不写盘。"""
import sys

DOC = sys.argv[1] if len(sys.argv) > 1 else "code3.md"
AGT = sys.argv[2] if len(sys.argv) > 2 else "AGENT.MD"
CHECK = "--check" in sys.argv

E = []

# ---------- §1.1 榜上现挂 / 历史最高 ----------
E.append((DOC,
"""| **榜上现挂** | **P105 字节第 12 遍** `[8.06, 8.58, 11.9, 12.1, 10.58, 14.44]`（Σ **65.66** = 池内第二低）⇒ 榜上 `22.04` / `#35`，发次 `6ab47e20694b590c3ccc4df8`；历史最高仍是 `22.29`/`#33` 那一遍。""",
"""| **榜上现挂** | **P105 字节第 13 遍** `[8.14, 8.58, 12.26, 12.3, 10.76, 14.4]`（Σ **66.44** = 池内第 5 低 ⇒ 按"高出池中位 ≥1.5 % 才补枪"**不补**）⇒ 榜上 `21.91` / `#35`，发次 `6ab487a6694b590c3cd0e9dd`（P119 探针读完立刻回发）；历史最高仍是 `22.29`/`#33` 那一遍。"""))

E.append((DOC,
"""**同字节池现已 12 遍**：Σ 均值 66.01 ms、全距 **65.50 … 68.06 = 3.9 %**""",
"""**同字节池现已 13 遍**：Σ 均值 66.04 ms、全距 **65.50 … 68.06 = 3.9 %**"""))

# ---------- §1.1 段占比轴 ----------
E.append((DOC,
"""**段占比轴 = 镜像**（P112 + P114 + P116，同一惰性机制）""",
"""**段占比轴 = 镜像**（P112 + P114 + P116 + P119，同一惰性机制）"""))

E.append((DOC,
"""⇒ ① 向量微优化（提加宽出循环 / 组宽 48→24）瞄 score 段、softmax 侧也没有落点（两段合起来平台上只 ≈8 %，#19）""",
"""⇒ ① **向量线整条关死**：score / softmax 前段 / PV（P119）三段平台上合计 **≈11 %**，本地价是它的 3~10 倍（#19、#24）"""))

E.append((DOC,
"""⚠️ 唯一被 P115 改口的是 run-merge：当年判死用的"AIV 调用税只 5.2 ns/条"是本地口径，平台上那一侧占 gather 的 72 %（#16）。""",
"""⚠️ run-merge 那笔 P115 改口**已就地结掉**（#16）：卡它的反证查明是误读一根轴当另一根，天花板 ≈3~4 pp、测不出来。"""))

# ---------- §1.1 量级问题 + P119/P120 两条 bullet ----------
E.append((DOC,
"""**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + 搬运 18 % = 26 % ⇒ **≈74 % 的墙没有任何计量器打开过**，3.6× 就藏在这块里。9/24 那三条"去拿它"的发次两成一败：""",
"""**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + PV ≤3 % + 搬运 18 % = **≈29 %** ⇒ **≈71 % 无价**，3.6× 藏在这块里。🔴 **候选集现在只剩"开销"一类**：干活的四段全部量完且全部 ≤18 %，没被尺子碰过的只有每 chunk 的**同核旗标对**（4 对/chunk、单价从没量过）、**标量下标扫描**、**跨核 barrier**。9/24 那几条"去拿它"的发次两成一败加一判零："""))

E.append((DOC,
"""- 🔎 **还剩 ≈74 % 无价**，而 P118 之后"惰性重复到头了"这句话**也过期了**：它成立只是因为前四把尺子都只会复制**幂等**的段。**新解锁**：带累加器的段照样能复制 —— 把同一批向量指令的**目的端**改指到一块 scratch（源操作数、宽度、条数一字不动）⇒ 对输出逐位惰性。⇒ 下一发 **P119 = `SoftmaxPv` 第 5 段（PV 累加）×2 打进 scratch**：它是全 kernel 最大的一块 AIV 工作（每 head·token 64 条 `Axpy`）**且每次 `Axpy` 前带一条 97 ns 的标量 UB 读**（§4，从没在这个位置上量过），也是 `#15.94`/`#15.95` 之后唯一"既不能靠重复、也不能靠搬运解释"的候选。⛔ **P117**（本地消融阶梯）继续降级：`#15.94` 已证本地段占比不可外推。""",
"""- ✅ **P119（PV 段定价）答案是负的**（`LOG#15.98`、`p119_platform.txt`）：复制体目的端 = `kfBuf_` 末行 ⇒ 逐位惰性、**不新增 UB 分配**；本地四臂在复制份数上**严格线性**（比值 1.96~1.99 / 3.90~3.98）⇒ 尺子可信，本地 PV×2 = **+18~33 %**，平台 = **Σ +2.9 %**（±2 % 带内 ⇒ 判不出非零）。🔴 **但"判不出非零"足以关闭它**（§1.2 新口径）：复制只加长度、不加真实依赖链 ⇒ Δ 小 = 这段躲在别人影子里 = 删它也省不出超过 Δ ⇒ 向量线不再发探针。
- 🔎 **下一发 P120 = 给"每 chunk 同核旗标对"定价**（第一把量**开销**的尺子）：chunk 尾追加 N 对**未用过**的 event 线（`MTE2_V(4)`）⇒ 零内存、零 UB、零数据依赖 ⇒ **构造级惰性**（前六把都得论证"这块内存没人读"，这把不用）。判据：Δ(1 对/chunk) ≥ 6 % ⇒ 方向换成**减旗标对数**（唯一一处既不在搬运也不在计算的可改代码）；Δ ≈ 0 ⇒ 只剩扫描与跨核 barrier。⛔ P117 继续降级。"""))

# ---------- §1.2 噪声口径 ----------
E.append((DOC,
"""P108（+45~71 % = 6~9σ）与 P111（级间距 15~20 pp + 六点同号）才定了案（`LOG#15.87`→`#15.91`）。""",
"""P108（+45~71 % = 6~9σ）与 P111（级间距 15~20 pp + 六点同号）才定了案（`LOG#15.87`→`#15.91`）。🔴 **但"带内 = 判不出"只对一半**（P119 教的）：判杠杆**打开**要 Δ ≥ 6 %，判**关闭**不需要 —— 复制体只加长度不加真实依赖链 ⇒ Δ 小的两种成因都意味着"删它省不出超过 Δ"。⇒ **噪声在否定方向上单向可用**。"""))

# ---------- §4 单价表 ----------
E.append((DOC,
"""⇒ **条数确实是成本单位**（本地冷档 gather 墙约一半在条数上），⛔ 但**不得反读成"并条就省"**：等字节下 `sbs` 128× 少的命令反而慢 2.7×（`p111_ladder.txt`）""",
"""⇒ **条数确实是成本单位**，⚠️ 但干净的条数轴只有 **P104 合并阶梯**（只改邻接 ⇒ 单调 5.2 ns/条）与 **P115**（平台 ×2 条 = +12.9 pp）。🔴 **`p111_ladder.txt` 不得当"并条不省"的证据**（`#15.98` 结案）：那条 sbs 阶梯上"每 chunk 游程数"单调下降而时间非单调（`s128` 命令最少反倒慢 2.7×、`s8` 多于 `s16` 反倒慢 25 %）⇒ 它动的是别的东西"""))

E.append((DOC,
"""⇒ 这一段是"64 条向量指令 + 1 条 97 ns 标量读"的混合体，**从没单独定过价 = P119 的靶**""",
"""⇒ 那"64 条向量 + 1 条标量读"的混合体已由 P119 **整体**定价：平台翻倍只 **+2.9 %**（本地 +18~33 %）⇒ **含标量读在内这一段都不是杠杆**"""))

E.append((DOC,
"""| 旗标一轮（`CrossCoreSetFlag` + `Wait`） | ≈0.42 µs/轮 | 每 chunk 固定税的成分；该税**饱和**（§1.3） |""",
"""| 旗标一轮（**跨核** `CrossCoreSetFlag` + `Wait`） | ≈0.42 µs/轮 | 每 chunk 固定税的成分；该税**饱和**（§1.3）。⚠️ **同核 `SetFlag/WaitFlag` 一轮的单价从没量过**，而主循环每 chunk 压着 **4 对**（L965~988）⇒ **P120 的靶** |"""))

E.append((DOC,
"""🔴 P118 之后这张"候选清单"少了一项、多了一项：**"访存层/HBM"已被正面划掉**（冷溢价 ≈0），而**"带累加器的向量段（PV）"从没被列进来过**——因为以前以为它不能惰性复制，现在知道换个目的端就能（§6 P0 = P119）。""",
"""🔴 P118 + P119 之后这张"候选清单"划掉两项：**"访存层/HBM"**（冷溢价 ≈0）与**"带累加器的向量段 PV"**（翻倍只 +2.9 %）。⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销**：同核旗标对（P120）/ 标量扫描 / 跨核 barrier。"""))

# ---------- §5 排除清单 ----------
E.append((DOC,
"""顺手那笔"命令减半换字节翻倍"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |
""",
"""顺手那笔"命令减半换字节翻倍"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |
| **24** | 🔴 **"PV 段（含内层那条 97 ns 标量 UB 读）是平台上的大杠杆"** —— 最后一块没价的大 AIV 工作（本地占 AIV 31~34 %） | P119 复制 ×2 ⇒ 平台 **Σ +2.9 %**、本地同臂 **+18~33 %**（比 0.095）。带内判不出非零，但**关闭不需要过门**（§1.2）⇒ 连同 #19 把**整条向量线**结掉（三段 ≈11 % of wall） | `#15.98` |
"""))

E.append((DOC,
"""| 16 | **P104 = gather 跨块搬运聚合（run-merge）** ⚠️ **判死已降级为"未在平台定价"** | 当年判死的定价用的是**本地**口径（"满连续只值 ≤4 %""AIV 调用税 5.2 ns/条 ⇒ 那面墙不存在"），而 P115 在平台量到"每次调用"侧占 gather 的 **≈72 % = 全墙的 12.9 pp（绝对上限 ≈2.2 分）** ⇒ 那句"墙不存在"在平台上**没被验证过**。两条未清的前置：① 形状结论仍在（`sbs=1` + 逐 token 随机索引 ⇒ 可合并的相邻段本就稀少，`rows=4` 那族实测收益恰好为零）；② **反证未解释**：等字节下命令少 **128×** 反而慢 **2.7×**（`p111_ladder.txt`）⇒ 那个实验里"条数"不是唯一自变量。字节已还原，备份 `probes/backup/p104_pre_runmerge/` | `#15.84` + `#15.93` + `#15.95` |""",
"""| 16 | **P104 = gather 跨块搬运聚合（run-merge）** 🔴 **两条前置结清，回到判死** | 合并能省的 = `12.9 pp × 跨表项邻接率`：基线 `CopyGm2Ub(…, run*D_)` **块内早已并成一条**、平台 `sbs=1` ⇒ 邻接率 = `1 − count/KV_S`（官方"抽样后排序"~25 %，**平台分布未知**）⇒ **天花板 ≈3~4 pp ≈ +0.6 分**，且按 §1.2 那条口径这个量**既测不出有肉也测不出没肉**。前置② 那条反证 = **误读**（sbs 阶梯不是条数轴，§4 碎拷贝行）。字节已还原，备份 `probes/backup/p104_pre_runmerge/` | `#15.84` + `#15.95` + `#15.98` |"""))

# ---------- §6 ----------
E.append((DOC,
"""| **P0** | **P119 = 给 PV 段定价（`SoftmaxPv` 第 5 段 ×2，目的端改指 scratch）** —— "惰性重复"族的**第二口气**：以前只会复制幂等的段，带累加器的段用 **scratch 目的端**一样能复制（源、宽度、条数一字不动 ⇒ 对输出逐位惰性） | 一发 ≈1 个信息单位。为什么是它：① 它是**全 kernel 最大的一块 AIV 工作**（每 head·token 64 条 `Axpy`）且本地价里 PV 占 AIV 31~34 %，而**平台侧从没量过**；② 每条 `Axpy` 前跟一条 **97 ns 标量 UB 读**（§4）⇒ 若平台也贵，这一段的构成里"标量读"可能比向量本身还大，那是**唯一一条既不属于搬运、也不属于 SIMD 吞吐、且可改**的候选；③ 判据按新的 ±2 % 带：Δ ≥ 6 % 才算非零。⚠️ **已知局限**：复制出来的 `Axpy` 目的端不同 ⇒ `o` 的读写流被复制、`p` 的标量读被复制，唯一不复制的是 `o` 的**依赖链**（真实那段是串在 `o` 上的）⇒ Δ 是**下界**，读到大 Δ 才算数 | 🔴 **下一做** |""",
"""| — | ~~**P119 = PV 段 ×2（目的端 = `kfBuf_` 末行）**~~ —— "惰性重复"族第二口气：带累加器的段换目的端照样能复制 | 读完：本地 **+18~33 %** 且复制份数上**严格线性**（四臂 8 档）⇒ 尺子可信；平台 **Σ +2.9 %**（13 遍池 ⇒ 判不出非零）。按 §1.2 新口径这**足够关闭** ⇒ 向量线三段（≈11 %）整条结掉，含内层那条 97 ns 标量 UB 读 | 🔴 已读 `#15.98` |
| **P0** | **P120 = 给"每 chunk 同核旗标对"定价** —— 第一把量**开销**不是**工作**的尺子：chunk 尾追加 N 对**未用过**的 `SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 零内存、零 UB、零依赖 ⇒ **构造级惰性** | 干活的四段已价完（≈29 %）⇒ 剩下 ≈71 % 只能是开销，而同核旗标是其中唯一"单价从没量过、又确定每 chunk 付 4 次"的一项（§4 那句 0.42 µs 是**跨核**）。这发也第一次直接读到平台的每 chunk 固定税，而不是靠本地那条**饱和**曲线外推。判据：Δ ≥ 6 % ⇒ 方向 = **减对数**（K/V 的 wait 合一、或让下一 chunk 的 wait 覆盖本 chunk 的 drain）；Δ ≈ 0 ⇒ 只剩标量扫描 + 跨核 barrier | ⬜ **下一做** |"""))

E.append((DOC,
"""| — | run-merge（P104，把碎 gather 并成长拷贝）—— **是本题唯一还活着的大杠杆，但被一条自相矛盾卡住** | P115 的 72 % 说"每条覆盖更多字节"有肉（上限 2.2 分），可 `p111_ladder.txt` 那条等字节阶梯上 `sbs` 少 **128×** 条命令反而慢 **2.7×** ⇒ "并条"的收益在同一份代码里被自己否掉过一次。**开工前必须先解释那条矛盾**（候选：合并路径改了 UB 暂存/环占用，或 `pendRuns` 变长把 stage 环挤瘦 ⇒ 动的不是条数一个变量） | ⬜ 先解释矛盾，再决定 |""",
"""| — | ~~run-merge（P104）—— 曾被写成"唯一还活着的大杠杆，被一条自相矛盾卡住"~~ | **矛盾解释完，它就变小了**：① 那条反证不成立 —— sbs 阶梯上"每 chunk 游程数"与时间不对应（`s8` 命令多于 `s16` 反倒慢 25 %），它不是条数轴；② 干净的条数轴单价 5.2 ns/条 × 平台调用侧 12.9 pp × **跨表项**邻接率（`sbs=1` ⇒ ≤~25 %）⇒ **天花板 3~4 pp ≈ +0.6 分、过不了 6 % 的判别门** | 🔴 已结 `#15.98` |"""))

# ---------- AGENT.MD ----------
E.append((AGT,
"""榜上现挂 = **P105 纯 AIV 字节**（同字节 12 遍真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，但**全距 3.9 % 时"抽到池外"就该补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **五个**惰性计量器：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、**gather 的冷暖溢价 +0.8 pp ≈ 0**（P112/P114/P115/P116/P118）⇒ ⛔ **"平台住在 HBM 那一侧"已正面否证**，"少搬字节"整条架构线（跨行去重 / KV 常驻 / 粗化 over-fetch）无条件死。剩下 **≈74 % 无价**，那才是 3.6× 的藏身处。✅ "惰性重复"**没到头**：带累加器的段换个目的端照样能复制 ⇒ 下一发 **P119 = `SoftmaxPv` 第 5 段（PV）×2 打进 scratch**（设计在 code3.md §6 P0）。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~10 %、六点合读 ±2 % ⇒ 两发单遍探针之间 <5 pp 不可判**。""",
"""榜上现挂 = **P105 纯 AIV 字节**（同字节 13 遍真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，**抽到池外 ≥1.5 % 就补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **七个**惰性计量器：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、冷暖溢价 **+0.8 pp ≈ 0**、**PV 段 +2.9 % ≈ 0**（P112~P119）⇒ ⛔ "平台住在 HBM 那一侧"否证、"少搬字节"架构线死、**整条向量线（三段 ≈11 %）也死**。剩下 **≈71 %** 而候选已收窄到**开销** ⇒ 下一发 **P120 = 每 chunk 同核 `SetFlag/WaitFlag` 对定价**（设计在 code3.md §6 P0）。✅ 新口径：**Δ 落在噪声带内足以关闭一个杠杆**（复制只加长度不加依赖 ⇒ 删它省不出超过 Δ），卡了一周的 run-merge 反证是误读 ⇒ #16 结掉。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~10 %、六点合读 ±2 %**。"""))

src = {DOC: open(DOC, encoding="utf-8").read(), AGT: open(AGT, encoding="utf-8").read()}
orig = {f: len(v) for f, v in src.items()}
bad = 0
for i, (f, old, new) in enumerate(E, 1):
    n = src[f].count(old)
    if n != 1:
        print("ANCHOR FAIL #%d hits=%d file=%s: %r" % (i, n, f, old[:56]))
        bad += 1
print("条目=%d  锚点失败=%d" % (len(E), bad))
if bad:
    sys.exit(1)
d = {}
for f, old, new in E:
    d[f] = d.get(f, 0) + len(new) - len(old)
print("净增长（字符数，UTF-8 下中文 3 B）：" + "  ".join("%s=%+d" % kv for kv in d.items()))
if CHECK:
    sys.exit(0)
for f in (DOC, AGT):
    for _, old, new in [e for e in E if e[0] == f]:
        src[f] = src[f].replace(old, new, 1)
    open(f, "w", encoding="utf-8").write(src[f])
    print("%-9s bytes %d → %d (%+d)  行数 %d" % (f, orig[f], len(src[f]), len(src[f]) - orig[f],
                                                 src[f].count("\n") + 1))
