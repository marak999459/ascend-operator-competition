P83 回滚点 = P81 结案态（kernel 1ac3545b46213c500710734d5e7f7c16 / host 6bc30b1d / tiling 0826c562）
P83 待做：CUBE_KSLICE 128->256 ⇒ AIC 每片 5 条 k 切片降到 3 条

结果（2026-09-23，同场次三臂 A/B）：**判死，已回滚到本目录的状态**。
  · 3 片@256 在 AIC 受限形态上慢 2.5 %（w3/p6/w2 各 +2.5~2.6 %），AIV 受限的 w4 持平。
  · 两臂输出逐位相同（超差计数一字不差）⇒ 纯成本差，不是精度/布局问题。
  · 回滚后同场次复测与首测基线差 ≤0.15 % ⇒ 场次无漂移，那 2.5 % 是可复现的。
提交源与本目录的 diff 只剩 `CUBE_KSLICE` 上方那段注释（代码逐字回到 128u / j<5u / cbu_+=5u）。
口径见 code3.md §15.73(m)。
