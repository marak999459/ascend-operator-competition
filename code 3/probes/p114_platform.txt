P114 —— 与 `sbs` 无关的"gather 搬运量翻倍"探针的平台读数（2026-09-24，sub `6ab458f5694b590c3cbff67b`，Pass 6/6、`precision_ratio` 全 1）

字节 = P105 kernel + 7 行（`ProcessToken` 里 K/kr 两条 `CopyGm2Ub` 原地再发一遍 + 3 行注释，`FlushChunk` 里 V 那条再发一遍）。
本地/远端/dry-run 三侧 kernel sha = `d467fef68798c4f3…`/69,467，其余三枚 `f2b28a86…`/4,149、`1046b349…`/460、`3a8f5305…`/36,758；
`SOCEDEX=0`（远端源码无 SoC 双注册）、`MARK_P114=2 RESIDUE=0`、禁用词 0/0、`HITS_KB=2 HITS_KR=2 HITS_VB=2`（= 原条 + 探针条）。
闸门 = `p114_gate.txt`：`构建 OK` + GATE1 13 真 expect × 双 dtype **全 `超差 0` + 对 golden 逐位一致**（26 行逐位、零非零超差）+ GATE2 14 档全 `超差 0` + `P32_GATE_DONE`。
⇒ 惰性的**正向**证明：探针在 `sbs=1` 的 `p6`/`big1` 上真的执行了两遍搬运，输出仍逐位不变。

标定 = `p114_calib.txt`（同一构建两臂，`p114_gather_rep.py` 从 pristine 生成，`HITS_P114=0` 基线 / `=3` 探针；两臂各自的 `p6`/`big1`/`r2_chunk` 都 `超差 0` + 逐位一致）：
  基线  w3 5.4396 / w4 9.5253 / w2 1.3806 / big1 0.6693 / p6 0.7492 ms
  探针  w3 5.6389 / w4 9.8680 / w2 1.4373 / big1 0.6993 / p6 0.7952 ms
  Δ     **+3.66 / +3.60 / +4.11 / +4.48 / +6.14 %** ⇒ **本地 gather 段 ≤4~6 %**（时间加权合读 +3.80 %）

平台六点 vs **同字节六遍均值**（P105 五遍 + P111 那发级 0，`LOG#15.92` 的池子）：
  参照均值 [7.867, 8.493, 11.163, 12.607, 10.887, 14.773]
  读数     [9.76,  9.46,  15.14,  13.74,  12.58,  16.80 ]
  Δ        +24.1 % +11.4 % +35.6 % +9.0 % +15.6 % +13.7 %  ⇒ **六点求和 +17.8 %**（六点合分 ruler 18.67）
  ⚠️ 逐点 Δ 本身散布 9.0~35.6 % = 4 倍 ⇒ 六点**不在同一种形状上**（各自 gather 份额差 4 倍），这是第一次能看见六点"内部结构"的读数。

⇒ **读数是"P112 的镜像"：平台的墙里搬运比计算贵，本地正相反。**
   同一惰性机制、同一份字节：
     | 翻倍的对象 | 本地 | 平台 |
     | `ComputeScores`（V 计算） | **+44~50 %** | **+6.1 %** |
     | `K/kr/V` 三条 `DataCopy`（MTE2） | **+4~6 %** | **+17.8 %** |
   ⇒ 本地"搬运 ∶ 计算 ≈ 1 ∶ 10"，平台"≈ 3 ∶ 1"。**两个方向的比例都是反的**，所以这不是"平台慢一点"，而是**不同的型**。

⇒ 三条后果：
   ① §6 的向量微优化线（P1/P2）**关闭**（P112 已判），**搬运线开**：平台上每翻倍 1 份 gather 值 +17.8 % ⇒ gather 段 ≥ **18 %** of wall，
      而且这是**下界** —— 第二遍搬的是同一批 GM 行，它们已被第一遍拉进 L2 ⇒ 探针买不到一次 miss 的价钱。真实份额可能是 2~3 倍（36~50 %）。
   ② `#15.84` 的 run-merge 判死口径**要按域重读**：它当时"满连续只值 ≤4 %"是在本地"搬运只占 4 %"的靶上量的。
      ⛔ 但**不要**据此复活 run-merge：平台 `sbs=1` + 逐 token 随机索引 ⇒ 可合并的相邻段本就稀少，`#15.84` 那条"收益恰好为零"的**形状**结论仍然成立；
      被推翻的只是"聚合这件事在平台上不重要"这个**定价**。
   ③ `§1.1` 那条"3.6~4.8× 分差在已测单价里凑不出来"第一次有了方向性的解释候选：**平台在 HBM 侧、本地在缓存侧**。
      本地 `S2 8192` 那份 KV 只有 17.8 MB ⇒ 每核反复搬的其实是 L2 命中的行（每核 6.0 GB/s 那条模型因此从来没碰到过带宽墙）。

⇒ **下一发拆成两条，一条免费一条一发：**
   **P113（本地、零提交，最高杠杆）**：造一个能**同时复现两个平台份额**（计算翻倍 ≈ +6 %、搬运翻倍 ≈ +18 %）的本地形状族
     （`sbs=1 ∧ rows ≥ 41 ∧ S2 大到 KV 装不进缓存`，现成候选 `r64c65`/`p32s1c16384`/`b32n8s1`，`S2 131072` ⇒ KV 151 MB）。
     一旦对上，我们就有了**平台的离线代理** ⇒ 之后所有优化先在本地过，不必每发都花钱。
   **P115（一发判别器）**：把"字节"和"调用条数"这两个成本**解耦** —— 每条 gather 原地拆成**两条半长**（同 src 区域、同总字节、调用数 ×2 ⇒ 仍逐位惰性），
     与 P114（字节 ×2、调用 ×2）对照 ⇒ 读"平台那 17.8 % 里有多少是每次调用的固定成本、多少是字节本身"。
     若拆半就显著 ⇒ 值钱的是"**每次搬得更长/更少条**"；若几乎不涨 ⇒ 值钱的是"**每 token 搬更少字节**"（跨头/跨行复用 ⇒ 那是架构题）。

回发：`6ab4552d694b590c3cbeeae2` 之前榜上挂的是 P112 探针（21.9 档），P114 读完后回发 P105（本次记录见 `p114_reissue.sh` 输出）。
