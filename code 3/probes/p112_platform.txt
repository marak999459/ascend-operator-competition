P112 —— 与 `sbs` 无关的"score 计算段翻倍"探针的平台读数
（2026-09-23 23:07:2xZ，sub `6ab4546e694b590c3cbea7b2`，Pass 6/6、`precision_ratio` 全 1）

字节 = P105 kernel + 5 行（`FlushChunk` 里把 `ComputeScores(q, kb, kr, sc, nbCur, m)` **原地无条件**
再跑一遍）。本地 kernel sha 提交前 = `705094aa…`；远端四枚与本地逐个吻合、`ascend910_93` 源码侧 0 命中、
禁词 0、`ComputeScores(q, kb, kr,` 命中 2（= 原句 + 探针句）。
闸门 = `p111/p112_gate.txt`：**GATE1 13 真 expect × 双 dtype 全 `超差 0` + 对 golden 逐位一致**
（⚠️ 这一发的惰性是**正向**证明，因为 `p6`/`big1` 是 `sbs=1` 档、探针在它们身上真的执行了 ——
P111 那发的"逐位一致"里阶梯根本没跑。），GATE2 14 档 × 双 dtype 全 `超差 0`。

标定 = **同一形态在本地早就量过**（`LOG#15.90` 的 `rep2` 臂 = 基线 + 每个 chunk 多跑一遍）：
  w3 +50.2 % / w4 +50.2 % / w2 +50.0 % / big1 +44.3 % / p6 +44.8 %
⇒ 这个臂的 Δ% 就是"score 计算段占整面墙的比例"（P110 已证"加工作 1:1 涨"）。

平台六点 vs **同路径五遍均值**（P105 四遍 + P111 探针那一发 = 级 0 时执行路径逐字符相同）：
  参照均值 [7.836, 8.532, 11.288, 12.572, 10.848, 14.940]  全距 [5.5, 3.8, 5.8, 12.1, 4.9, 15.6] %
  读数     [8.42,  8.38,  11.66,  12.88,  11.18,  17.54 ]
  Δ        +7.5 % −1.7 % +3.3 % +2.4 % +3.1 % +17.4 %   ⇒ **六点求和 +6.1 %**
  六点合分 ruler 1.4159（≈ 同字节池子中段，别单独读）

⇒ **读数：平台六点里 score 计算段只占墙的 ≈6 %，不是本地的 ≈50 %。** 落差 7.5 倍 = 39 pp，
   而同字节合分噪声只有 ±3 % ⇒ 这是一个 13σ 的"不符"，不是抽卡。
⇒ 三条直接后果：
   ① §6 P1（把 `WidenToF32` 提出头循环 / 组宽 48→24 断依赖链）**在平台上没有落点** —— 它整笔
      瞄准的就是 score 段，而 score 段在那里只值 5.5 %/8 ≈ **0.7 %**。
   ② 本地 `w2/w3/w4` 那族在"墙由什么组成"这件事上**与平台不同型**（它们 score 段 = 50 %），
      所以 P110/P110b 那两条"计算侧"教训不能往平台外推 —— 能外推的只有 `sbs` 与 `rows` 两条形状轴。
   ③ 剩下 94 % 的候选只剩三类：`K/kr/V` 的 gather（MTE2）、`SoftmaxPv` 的向量段、每 chunk 固定税。
⇒ 机制假设（下一个可判死的问题，不要当结论用）：平台很可能是 **`q_N` 很小（1~2）**。
   本地靶族 `N1=4/8` ⇒ host 选 `nb=4` ⇒ 每搬 1 个 token 要做 4 份 576 维点积；若 `q_N=1` 则
   每 token 只有 1 份 ⇒ **计算/字节比整块小 4 倍**，墙自然滑到 gather 侧。这条假设同时解释
   P112 的 6 %、`#15.81` 的"单价对头数不敏感"，以及 P99 那一发为什么读不出 `q_N`。
⇒ 下一发（P114）= **把每 chunk 那三条 `DataCopy` 原地再发一遍**（同 `src` 同 `dst` ⇒ 逐位惰性，
   与 P112 同一种机制）⇒ 直接读"平台六点的 gather 段占比"。若它读到 ≈80 % ⇒ 墙 = MTE2，
   那么唯一有意义的方向是"每 token 搬更少字节 / 每次搬更长"（`#15.84` 的 run-merge 判死口径
   是在计算占 50 % 的靶族上量的，届时必须重开），而 P1/P2 那两条向量微优化全部作废。
