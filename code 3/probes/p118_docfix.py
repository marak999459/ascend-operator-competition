#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P118 读数落地：把 code3.md 里所有被这一发否证/撑宽的断言**就地改对**（不留"已作废"共存）。
每处替换都先断言锚点唯一命中；末尾打印字节净增长（本轮规则：要么压缩、要么明说）。"""
import io
import sys

P = "/home/fszqsn/ops_comp/ascend-operator-competition/code3.md"
src = io.open(P, encoding="utf-8").read()
n0 = len(src.encode("utf-8"))

E = []

# ---------- §1.1 榜上现挂 / 历史最高 ----------
E.append((
"""| **榜上现挂** | **P105 字节第 9 遍** `[7.58, 8.16, 11.26, 12.04, 10.86, 15.62]`（Σ 65.52 = 池内最低之一），发次 `6ab47205694b590c3cc74c81`（P116 探针读完立刻回发）；历史最高仍是 `22.29`/`#33` 那一遍（`Pass` 6/6、六点 `precision_ratio` 全 1）。⚠️ **同一份字节没有"最好档"可追**：九遍同字节 Σ 全距 ±0.6 %、真值 21.7~22.3、均值 ≈22.0 ⇒ 榜上任何一档都是抽卡，追档零收益 |""",
"""| **榜上现挂** | **P105 字节第 12 遍** `[8.06, 8.58, 11.9, 12.1, 10.58, 14.44]`（Σ **65.66** = 池内第二低）⇒ 榜上 `22.04` / `#35`，发次 `6ab47e20694b590c3ccc4df8`；历史最高仍是 `22.29`/`#33` 那一遍。🔴 **订正一条决断口径**："`追档零收益`"是池只有 9 遍、全距 0.6 % 时写的 ⇒ 同一天池撑到 12 遍、全距 **3.9 %**，且实测抓到一次：`ccb6da4` 抽出 Σ 68.06 → 榜上 **21.7**，同一份字节立刻再发 → 65.66 → **22.04**（**两发之间代码逐字节相同**）。⇒ 新规矩：**现挂 Σ 高出池中位 ≥1.5 % 就补一枪回发**，这是白捡的分 |""",))

E.append((
"""| **历史最高** | **`22.29` = 同一份 P105 字节的第三遍读数**（九遍同字节：六点合读 Σ 均值 65.75 ms，两个可复算的极端遍 = 65.50 与 65.90 ⇒ **0.6 %**；真值散在 21.7~22.3）⇒ "最好档位"与"现挂"是同一份代码，差的那 0.6 分是抽卡不是回归。⚠️ **Σ 稳但逐点不稳**""",
"""| **历史最高** | **`22.29` = 同一份 P105 字节的第三遍读数**。**同字节池现已 12 遍**：Σ 均值 66.01 ms、全距 **65.50 … 68.06 = 3.9 %**（⇒ 旧的"0.6 %"是 9 遍时的欠采样），榜上真值散在 **21.7~22.3** ⇒ "最好档位"与"现挂"仍是同一份代码、差的是抽卡，但**抽卡的幅度是 ±2 %、值 ±0.4 分，不是可以忽略的量**。⚠️ **逐点比 Σ 更不稳**""",))

# ---------- §1.1 量级问题三行 ----------
E.append((
"""**剩下的量级问题**：计算 6 % + 搬运 18 % = 24 % ⇒ **≈76 % 的墙没有任何计量器打开过**，3.6× 就藏在这块里。9/24 那两条"去拿它"的发次一成一败：""",
"""**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + 搬运 18 % = 26 % ⇒ **≈74 % 的墙没有任何计量器打开过**，3.6× 就藏在这块里。9/24 那三条"去拿它"的发次两成一败：""",))

E.append((
"""平台六点合读 **+12.9 %**，对 P114 的 +17.8 % ⇒ **平台 gather 里"每次调用"侧 ≈72 %、"字节本身" ≈28 %**（⚠️ 72 % 是上界：拆半同时砍了每条字节数）。⇒ "每 token 少搬字节"那档架构题**降级到 ≤0.9 分**，"每条命令覆盖更多字节"那一侧上限 ≈2.2 分。⛔ 但**别立刻开工 run-merge**：""",
"""平台六点合读 **+12.9 %**，对 P114 的 +17.8 % ⇒ **平台 gather 里"每次调用"侧 ≈72 %、"字节本身" ≈28 %**（⚠️ 72 % 是上界：拆半同时砍了每条字节数；⚠️ 且这两个读数只差 4.9 pp = 单遍合读噪声 ±2 % 下的 **1.7σ** ⇒ 只当"调用侧 ≥ 字节侧、量级 2.5∶1"用，**别当精确分解**）。⇒ "每条命令覆盖更多字节"那一侧上限 ≈2.2 分；而"每 token 少搬字节"那档架构题先被降到 ≤0.9 分、**当天直接被 P118 降到 0**（§5 #23）。⛔ 但**别立刻开工 run-merge**：""",))

E.append((
"""- 🔎 **还剩 ≈74 % 的墙没有计量器打开过**（计算 6 % + softmax 前段 2.4 % + gather 18 % = 26 %）⇒ 这是 3.6× 的藏身处。**"惰性重复"这一族已经到头**：P114 的暖复制从原理上看不见冷 miss ⇒ 下一发**改机制**（设计在 §6 P0 那一行，只写一次）。⛔ **P117**（本地消融阶梯）降级：`#15.94` 已证本地段占比不可外推。""",
"""- ✅ **P118（冷暖镜像）有答案，且是否证型的**（`LOG#15.97`）：与 P114 同条数、同每条字节、只差行号（伪随机冷行）⇒ 平台 **+18.2 % vs 暖的 +17.4 %**，**冷溢价 +0.79 pp 落在噪声里**，而同一对双臂在本地是 **2.8~3.4 倍** ⇒ **"平台六点住在 HBM 那一侧"正面判死**，"少搬字节"整条架构线（跨行去重 / KV 常驻 / 粗化 over-fetch）无条件关死。⚠️ 这一发同时把**同字节池从 9 遍撑到 12 遍、Σ 噪声带从 ±0.6 % 撑到 ±2 %** ⇒ 新判别口径：**两发单遍探针之间 <5 pp 不可判**（§1.2）。
- 🔎 **还剩 ≈74 % 无价**，而 P118 之后"惰性重复到头了"这句话**也过期了**：它成立只是因为前四把尺子都只会复制**幂等**的段。**新解锁**：带累加器的段照样能复制 —— 把同一批向量指令的**目的端**改指到一块 scratch（源操作数、宽度、条数一字不动）⇒ 对输出逐位惰性。⇒ 下一发 **P119 = `SoftmaxPv` 第 5 段（PV 累加）×2 打进 scratch**：它是全 kernel 最大的一块 AIV 工作（每 head·token 64 条 `Axpy`）**且每次 `Axpy` 前带一条 97 ns 的标量 UB 读**（§4，从没在这个位置上量过），也是 `#15.94`/`#15.95` 之后唯一"既不能靠重复、也不能靠搬运解释"的候选。⛔ **P117**（本地消融阶梯）继续降级：`#15.94` 已证本地段占比不可外推。""",))

# ---------- §1.2 噪声锚点 ----------
E.append((
"""六点合分只动 ±0.3~0.6 分 ⇒ **≥8~10 % 的系统性收益在平台上是可判的**。⚠️ 反向也成立：**判别器探针必须做到 ≥4× 噪声**才够格下结论""",
"""六点合分只动 ±0.3~0.6 分 ⇒ **≥8~10 % 的系统性收益在平台上是可判的**。🔴 **但这条"合分稳"本身在 9/24 被撑宽了**：池从 5 遍涨到 **12 遍**后 Σ 全距 = **3.9 %**（65.50…68.06，最差一遍榜上 21.7、最好 22.2）⇒ **Σ 的噪声带按 ±2 % 用**（旧口径 ±0.6~0.9 % 是欠采样），换算：**≥4 % 的系统性收益可判、两发单遍探针之间 <5 pp 不可判**（这条直接判了 P115 那个 72∶28 的精度、也决定了 P119 必须打**大段**而不是打 2~3 % 的小段）。⚠️ 反向也成立：**判别器探针必须做到 ≥4× 噪声**才够格下结论""",))

# ---------- §1.3 ----------
E.append((
"""⇒ **不可外推到平台**（六点明显在搬运/HBM 那一侧，§1.1）""",
"""⇒ **不可外推到平台**（⚠️ 原括号里"六点明显在搬运/HBM 那一侧"是 P114 时代的推断、**P118 已否证掉 HBM 那一半**：搬运确实占 18 %，但它不在冷层，§1.1）""",))

E.append((
"""** ⇒ 全平台唯一两侧同型的计量器（`LOG#15.96`）。""",
"""** ⇒ 全平台唯一两侧同型的计量器（`LOG#15.96`）。⑤ **`gather` 的冷暖价**：同一批行换成伪随机冷行，本地 **2.84×（`p6`）~3.37×（`big1`）**、平台 **1.00~1.04×** ⇒ **"冷∶热"这个比值本身就是"在不在缓存里"的判别器**，而平台六点读出来是 1（`LOG#15.97`）。""",))

# ---------- §4 单价表 ----------
E.append((
"""| 标量 **UB** 读 | ≈**97 ns/次（比 GM 还贵 2.5×）** | ⇒ "把表搬进 UB 再逐个 `GetValue`"在本机是**反模式**（P20 死在这条上，+76 %） |""",
"""| 标量 **UB** 读 | ≈**97 ns/次（比 GM 还贵 2.5×）** | ⇒ "把表搬进 UB 再逐个 `GetValue`"在本机是**反模式**（P20 死在这条上，+76 %）。🔴 **`SoftmaxPv` 第 5 段每 `Axpy` 就带着一条 `p.GetValue`** ⇒ 这一段是"64 条向量指令 + 1 条 97 ns 标量读"的混合体，**从没单独定过价 = P119 的靶** |
| `gather` 的**冷暖** | 本地冷∶热 **2.8~3.4**；平台 **1.00~1.04**（冷溢价 ≤ +0.8 pp，在 ±2 % 带内） | `LOG#15.97` ⇒ **平台侧的 18 % 里没有"访存层"这一项**；⛔ 别反读成"缓存无关紧要"，本地它是要紧的 |""",))

# ---------- §5 老账 ----------
E.append((
"""⇒ 我们的 gather 上限不是访存层，**"换更冷的形状"不是找墙的办法** | `#15.94` |""",
"""⇒ 我们的 gather 上限不是访存层，**"换更冷的形状"不是找墙的办法**。⚠️ **口径订正（P118）**：那一族扫的是**基线自己的复用度**、计量器的冷暖从没单独变过 ⇒ 当时那句"冷度不是轴"**说过头了**：冷度**是**轴（同一条 gather 换冷行本地就 2.8~3.4 倍），只是**平台六点不在这一侧** | `#15.94` + `#15.97` |
| **23** | 🔴 **"榜首那 3.6× 只能靠少搬字节吃掉"（`#15.94` 判② 留到最后的一个机制）** | P118：与 P114 同条数、同每条字节、只差行号 ⇒ 平台冷溢价 **+0.79 pp ≤ 噪声** ⇒ 18 % 里字节那一侧总共 4.9 pp 且**已证明不是带宽、也不是层** ⇒ **跨行去重 / KV 常驻 / 粗化 over-fetch 整条线无条件死**；顺手那笔"命令减半换字节翻倍"的账（净 −1.6 pp）前提今天一起没了 | `#15.97` |""",))

# ---------- §6 下一步 ----------
E.append((
"""| **P0** | ~~**把 76 % 那块没有价格的墙拆开**~~ 已读到 26 %：`ComputeScores` **6 %**（P112）+ gather **18 %**（P114，下界）+ softmax 前段 **2.4 %**（P116）⇒ 剩 ≈74 % 无价。⛔ **"惰性重复"这一族到此为止**：P114 那把尺子从原理上看不见冷 miss（第二遍已在 L2 里）⇒ 下一发**换机制**：**P118 = 冷复制计量器** —— 每 chunk 额外发 `m` 条 1024 B `DataCopy`，src = 本 batch `value` 池内 xorshift 伪随机行、dst = `kfBuf_` 里下一段必被 `WidenToF32` 整块覆写的死区 ⇒ 逐位惰性；与 P114 **同条数、同每条字节、只差冷暖** ⇒ `Δ118 − 17.8 pp` = **冷溢价**，第一把能读到 HBM 那一侧的尺子 | 一发 ≈1 个信息单位；**若 Δ118 ≈ 17.8 ⇒ 墙不是冷 miss，那 74 % 只能在"标量 GM 读 + barrier 轮次 + 每 chunk 固定税"里**（下一发改问这三样）；**若 Δ118 ≫ 17.8 ⇒ 平台在 HBM 侧，而我们的字节与条数都已到算法下界** ⇒ 只剩"跨 query 行共享 KV 遍历"这一档架构题，值得为它花几天 | 🔴 **下一做** |""",
"""| — | ~~**把 76 % 那块没有价格的墙拆开**~~ | 已读到 26 %：`ComputeScores` **6 %**（P112）+ gather **18 %**（P114/P118）+ softmax 前段 **2.4 %**（P116）⇒ 剩 ≈74 % 无价 | 🔴 已读 |
| — | ~~**P118 = 冷复制计量器**（第一把能读到 HBM 那一侧的尺子）~~ | 读完了，答案是**负的**：冷 **+18.2 %** vs 暖 **+17.4 %** ⇒ **冷溢价 +0.79 pp ≈ 0**（本地同双臂 2.8~3.4×）⇒ 走的是左边那条岔路："墙不是冷 miss"，而**代价比预想更狠**：它把"少搬字节"整条架构线一起带走了（§5 #23） | 🔴 已读 `#15.97` |
| **P0** | **P119 = 给 PV 段定价（`SoftmaxPv` 第 5 段 ×2，目的端改指 scratch）** —— "惰性重复"族的**第二口气**：以前只会复制幂等的段，带累加器的段用 **scratch 目的端**一样能复制（源、宽度、条数一字不动 ⇒ 对输出逐位惰性） | 一发 ≈1 个信息单位。为什么是它：① 它是**全 kernel 最大的一块 AIV 工作**（每 head·token 64 条 `Axpy`）且本地价里 PV 占 AIV 31~34 %，而**平台侧从没量过**；② 每条 `Axpy` 前跟一条 **97 ns 标量 UB 读**（§4）⇒ 若平台也贵，这一段的构成里"标量读"可能比向量本身还大，那是**唯一一条既不属于搬运、也不属于 SIMD 吞吐、且可改**的候选；③ 判据按新的 ±2 % 带：Δ ≥ 6 % 才算非零。⚠️ **已知局限**：复制出来的 `Axpy` 目的端不同 ⇒ `o` 的读写流被复制、`p` 的标量读被复制，唯一不复制的是 `o` 的**依赖链**（真实那段是串在 `o` 上的）⇒ Δ 是**下界**，读到大 Δ 才算数 | 🔴 **下一做** |""",))

E.append((
"""⚠️ 例外：搬运**内部**的"条数∶字节"分解本地就能对上（0.9 vs 0.72，`#15.95`）⇒ 只有这条轴可以本地筛 | 🔴 已清 `#15.94` |""",
"""⚠️ 例外：搬运**内部**的"条数∶字节"分解本地就能对上（0.9 vs 0.72，`#15.95`）⇒ 只有这条轴可以本地筛。⚠️ **P118 给这条"例外"加了一条反向的**：冷暖比值本地 2.8~3.4 / 平台 1.0 ⇒ **本地能筛的轴，平台上未必存在** ⇒ 本地臂只当"这段值多少"的量级参考，判据一律回平台 | 🔴 已清 `#15.94` |""",))

# ---------- §4 表尾那句"平台的墙由什么组成" ----------
E.append((
"""⇒ 平台的墙是"**搬运条数与字节 + 标量 GM 读 + barrier 轮次 + 每 chunk 固定税**"，而那几样加起来还剩 ≈74 % 没有被任何已测机制定价。P113 已把"搬到本地来筛"这条路**试完并判废**（12 档无代理），所以剩下的定价**只能一发一发在平台上买**（下一发 = §6 P0 那一行）。""",
"""⇒ 平台的墙是"**搬运条数 + 标量读 + barrier 轮次 + 每 chunk 固定税**"，而那几样加起来还剩 ≈74 % 没有被任何已测机制定价。🔴 P118 之后这张"候选清单"少了一项、多了一项：**"访存层/HBM"已被正面划掉**（冷溢价 ≈0），而**"带累加器的向量段（PV）"从没被列进来过**——因为以前以为它不能惰性复制，现在知道换个目的端就能（§6 P0 = P119）。P113 已把"搬到本地来筛"这条路**试完并判废**（12 档无代理），所以剩下的定价**只能一发一发在平台上买**。""",))

# ---------- 压缩：§1.1 的 P113 长条与 §5 #21 / §6 "已清"行三处重复 ----------
E.append((
"""- ❌ **P113（本地代理族）判废**（`LOG#15.94`）：12 档三臂（含 6 档新造的冷/大/低强度形状）无一同时复现"计算 ≈+6 % ∧ 搬运 ≈+18 %"，本地比值最低 1.05 vs 平台 0.34；而且动比值的轴是**每 token 头数 `N1`**、不是冷度/复用/形状大小 ⇒ **计算∶搬运这根轴上没有离线代理**，"改架构前先本地筛"不可用，方向判定只能一发一发在平台上买。副产品是一条**结构墙**：`N1=1` 族的 gather 请求吞吐钉在 456~496 GB/s（≈12 GB/s/核），与冷热、与复用度全都无关。""",
"""- ❌ **P113（本地代理族）判废**（细节 `LOG#15.94`、判决 §5 #21）：12 档三臂无一同时复现"计算 ≈6 % ∧ 搬运 ≈18 %，且动比值的轴是**每 token 头数 `N1`**、不是冷度/复用/形状大小 ⇒ **计算∶搬运这根轴上没有离线代理**。副产品 = 一条与冷热无关的 456~496 GB/s（≈12 GB/s/核）gather 请求吞吐墙。""",))

for i, (old, new) in enumerate(E):
    c = src.count(old)
    if c != 1:
        sys.exit("ANCHOR FAIL #%d count=%d :: %s" % (i, c, old[:70]))
    src = src.replace(old, new, 1)

# §6 表尾与 ## 7 之间缺一个空行
tail_anchor = "动的不是一个变量 |\n## 7."
if src.count(tail_anchor) == 1:
    src = src.replace(tail_anchor, "动的不是一个变量 |\n\n## 7.", 1)
    print("blank line inserted")
else:
    print("blank-line anchor count=%d (skip)" % src.count(tail_anchor))

io.open(P, "w", encoding="utf-8").write(src)
n1 = len(src.encode("utf-8"))
print("edits=%d  bytes %d -> %d  (net %+d)" % (len(E), n0, n1, n1 - n0))
