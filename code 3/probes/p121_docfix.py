#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P121 状态板订正：code3.md 就地改（规则 3：只改被读数改变了的断言，旧句消失、不新增"已作废"）。
   每个锚点必须恰好命中一次；任一失败 ⇒ 整份不落盘。`--check` 只数不改。
   规则 1 的"只加不减不允许" ⇒ 同一批里带了两处压缩（L47 与 L48 重复的抽卡叙事、L72 已被 15 遍池取代的五遍枚举），并把 L72 的"逐点全距"标成**下界** —— P121 的 C1 判读正是拿它当包络用的。"""
import sys

DOC = "/home/fszqsn/ops_comp/ascend-operator-competition/code3.md"
CHECK = "--check" in sys.argv

E = []
add = lambda o, n: E.append((o, n))

# ---- 文首自指数字 + LOG 编号水位 ----
add("它现在 **≈290 行 / ≈56 KB**", "它现在 **≈295 行 / ≈58 KB**")
add("（当前到 `15.99` ⇒ 下一条 `15.100`）", "（当前到 `15.100` ⇒ 下一条 `15.101`）")

# ---- 顶部状态行：14 遍 → 15 遍（新池内最低 65.24 ⇒ 榜上 22.13） ----
add('**P105 字节第 14 遍** `[7.7, 8.32, 11.76, 13.08, 10.58, 15.32]`（Σ **66.76** = 高出池均值 66.09 只有 **1.0 %** ⇒ 按"高出中位 ≥1.5 % 才补枪"**不补**）⇒ 榜上 `21.97` / `#37`，发次 `6ab495b1694b590c3cd911a7`（P120 探针读完立刻回发）',
    '**P105 字节第 15 遍** `[7.92, 8.6, 11.8, 12.48, 10.16, 14.28]`（Σ **65.24 = 池内新低** ⇒ 不补枪）⇒ 榜上 `22.13` / `#37`，发次 `6ab4a30b694b590c3ce10748`（P121 探针读完立刻回发）')
add('**同字节池现已 14 遍**：Σ 均值 66.09 ms、全距 **65.50 … 68.06 = 3.9 %**',
    '**同字节池现已 15 遍**：Σ 均值 66.03 ms、全距 **65.24 … 68.06 = 4.3 %**')

# ---- §1.1 一句话状态 ----
add('**榜上现挂 = P105 字节第 14 遍**（21.97 / #37；14 份同字节 Σ 池 65.50…68.06、均值 66.09 ⇒',
    '**榜上现挂 = P105 字节第 15 遍**（22.13 / #37；15 份同字节 Σ 池 65.24…68.06、均值 66.03 ⇒')

# ---- §1.1 计量器家族：P121 是第八把、第二把"开销"尺子 ----
add('（P112/P114/P116/P119 四把"工作"尺子 + P120 第一把"开销"尺子）',
    '（P112/P114/P116/P119 四把"工作"尺子 + P120/P121 两把"开销"尺子）')

# ---- §1.1 剩下的量级问题：候选从两条收成一条 ----
add('**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + PV ≤3 % + 搬运 18 % = **≈29 %** ⇒ **≈71 % 无价**，3.6× 藏在这块里。🔴 **候选集只剩"开销"一类里的两条**：干活的四段全部量完且全部 ≤18 %，而**同核旗标对也价完了**（4 对/chunk ≤0.08 % of wall）⇒ 没被尺子碰过的只有**标量下标扫描**（P121）与**跨核 barrier**。9/24~9/25 那五发"去拿它"：一败 + 四读数（后两发是否证）：',
    '**剩下的量级问题**：计算 6 % + softmax 前段 2.4 % + PV ≤3 % + 搬运 18 % + 标量下标读 ≈0.2 % = **≈29 %** ⇒ **≈71 % 无价**，3.6× 藏在这块里。🔴 **候选集只剩"开销"一类里的一条**：干活的四段全部量完且全部 ≤18 %，而"开销"这一类里**同核旗标对**（4 对/chunk ≤0.08 %）与**标量下标扫描**（重读 7 遍 ⇒ s̄ ≤0.29 %）也价完了 ⇒ 没被尺子碰过的只剩**跨核 barrier**。9/24~9/25 那六发"去拿它"：一败 + 五读数（后三发是否证）：')

# ---- 给增量"付账"的两处压缩：L47 的抽卡叙事、L72 的五遍枚举 ----
add('🔴 **"追档零收益"已作废**（池撑到 12 遍、全距 **3.9 %** 之后实测抓到一次）：`ccb6da4` Σ 68.06 → 榜上 **21.7**，同一份字节立刻再发 → 65.66 → **22.04**（**两发之间代码逐字节相同**）。⇒ 新规矩：**现挂 Σ 高出池中位 ≥1.5 % 就补一枪回发**，这是白捡的分 |',
    '🔴 **"追档零收益"已作废**（实测过同字节两发差 **0.34 分**：`ccb6da4` Σ 68.06 → 21.7，立刻再发 65.66 → 22.04）⇒ 规矩：**现挂 Σ 高出池中位 ≥1.5 % 就补一枪回发** |')
add('**噪声的实测锚点**：P105 字节五遍（榜上真值 **21.73 / 21.92 / 22.29 / ≈22.0 / 22.21**）+ P111 探针那一发（`sbs` 阶梯在六点上是级 0 ⇒ **执行路径与 P105 逐字符相同**，≈21.9）= **六份同路径样本，全距 0.56 分、均值 ≈22.0**；P38 字节两遍 = 22.19 / 21.93。同六份的 ruler = 20.82 / 21.11 / 21.35 / 21.36 / 21.52 / 21.63 ⇒ **与真值系统差 +0.5~0.7 分、单调性完好：尺子只管排序，别拿绝对值当分数读**。逐点全距（五遍）：',
    '**噪声的实测锚点**：头六份**同路径**样本（含 P111 那发 —— `sbs` 阶梯在六点上是级 0 ⇒ 执行路径与 P105 逐字符相同）榜上真值全距 **0.56 分**、均值 ≈22.0。⚠️ **尺子只管排序、别拿绝对值当分数读**：同六份的预测与真值系统差 **+0.5~0.7 分**、单调性完好。逐点全距（头五遍同字节；此后只会增 ⇒ 当**下界**用）：')
add('池从 5 遍涨到 **14 遍**后 Σ 全距 = **3.9 %**（65.50…68.06、均值 66.09',
    '池从 5 遍涨到 **15 遍**后 Σ 全距 = **4.3 %**（65.24…68.06、均值 66.03')

# ---- §1.1 尾巴：🔎 P121 → ✅ P121 + 🔎 P122 ----
add('- 🔎 **下一发 P121 = 标量下标扫描计量器**（两条剩候选里的第一条，也是 §4 里唯一还挂在"每表项乘一次"那一侧的价：38.6 ns/条 × 每单元扫过的表项数（`#15.39` 旧形状上 1024 条 = 39.5 µs）× 每 query 的头块数）。⛔ 不能用"原地重跑"—— 扫描推进迭代器状态 ⇒ **不幂等**；可行形态 = 额外读 N 条下标进丢弃值，而那在 `-O2` 下有被 DCE 的风险 ⇒ **必须先本地两臂标定看 Δ 是否随 N 线性**（P119 的做法），不线性 = 尺子根本没生效。判据按 §1.2 单向口径：带内 ⇒ 只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB。⛔ P117 继续降级。',
    '- ✅ **P121（第二把"开销"尺子 = 标量下标扫描）答案也是否证的**（`LOG#15.100`、`p121_platform.txt`）：`NextTokenBlock` 推进迭代器 ⇒ **不幂等**、不能"原地重跑"，尺子改成"按同一条 `tokStep` 把本 chunk **刚扫过的那段地址重读 7 遍**、只累加进一个 uint64"（同基址/同步进/**同条数** ⇒ Δ/N 就是"一遍的价"），sink 挂在"acc < 2^40 vs 哨兵 `UINT64_MAX`"这道**可证明永不成立**的分支上 ⇒ 连 scratch 归属都不用论证。⚠️ 防 CSE 靠地址里带一项 `acc >> 63`（运行时恒 0、编译器证不出）—— 没它 N=3 与 N=1 是同一份机器码 ⇒ **本地标定是这一族的必经步骤**：3/1 = 2.77~3.08、7/1 = 7.27~8.12（八档逐位一致）⇒ 尺子确实在工作。平台 **Σ +1.56 %**（紧邻同字节读法 +2.79 %、对池内最高 68.06 还是**负的**）⇒ 一遍 ≈0.22 %、带边上界 **≤0.29 % of wall**（§5 #26）。⚠️ 唯一点越出"同字节三遍经验带"的是 C1（+13.1 %），但 §1.2 那行五遍**逐点全距**已量到 `C4 12.1 % / C6 15.6 %` ⇒ 13.1 % 在"什么都没改"的单点包络内；且最不利读法（只 C1 命中、s=1.9 %）全删也只值 **+0.03~0.04 分 < 我们自己两发的回差** ⇒ 分辨真假本身不值一发。\n'
    '- 🔎 **下一发 P122 = 跨核 barrier 计量器**（候选集最后一条）：把 AIC↔AIV 那轮真握手按**空闲硬旗标 id 成对复制** N 轮（Set/Wait 一起复制 ⇒ 语义不变、只多来回），先验 = `#15.55` 的 0.42 µs/轮。⛔ 判据必须按 `#15.87` 的教训**把幅度做到逐点噪声够不着**（先本地标定"一轮 %"、再把 N 推到 Σ ≥6 %），否则这一发不如不发；⛔ P117 继续降级。')

# ---- §4 一句话总结：候选清单再划两项 ----
add('⇒ 平台的墙是"**搬运条数 + 标量读 + barrier 轮次 + 每 chunk 固定税**"，而那几样加起来还剩 ≈74 % 没有被任何已测机制定价。🔴 P118 + P119 之后这张"候选清单"划掉两项：**"访存层/HBM"**（冷溢价 ≈0）与**"带累加器的向量段 PV"**（翻倍只 +2.9 %）。⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销两类**：标量下标扫描（P121）/ 跨核 barrier（同核旗标对已价、≤0.08 % ⇒ 出局）。',
    '⇒ 平台的墙是"**搬运条数 + barrier 轮次 + 每 chunk 固定税**"，而那几样加起来还剩 ≈71 % 没有被任何已测机制定价。🔴 P118~P121 之后这张"候选清单"划掉四项：**"访存层/HBM"**（冷溢价 ≈0）、**"带累加器的向量段 PV"**（翻倍只 +2.9 %）、**"每 chunk 同核旗标对"**（≤0.08 %）与**"每 token 一条标量下标读"**（重读 7 遍只 +1.6 % ⇒ ≤0.29 %）。⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销一类里的一条**：跨核 barrier（P122）。')

# ---- §5 排除清单：#25 之后加 #26 ----
add('| **25** | 🔴 **"减每 chunk 同核旗标对数是杠杆"（P120 判据里 Δ ≥ 6 % 那条岔路）** | 127 对 ⇒ 平台 **Σ +2.42 %**，而这一发比同字节池内最高（68.06）还低 0.62 % ⇒ 每对 ≤0.0191 %、现有 **4 对/chunk ≤0.076 %**（最不利读法 0.103 %）⇒ 全删 ≈ **+0.02 分**。本地每对 0.03~0.06 % ⇒ 平台/本地 ≤0.73 ⇒ **固定开销在平台上的占比比本地更小**（"每 chunk 固定税"里走旗标/管道同步的那部分从此有不利上界） | `#15.99` |',
    '| **25** | 🔴 **"减每 chunk 同核旗标对数是杠杆"（P120 判据里 Δ ≥ 6 % 那条岔路）** | 127 对 ⇒ 平台 **Σ +2.42 %**，而这一发比同字节池内最高（68.06）还低 0.62 % ⇒ 每对 ≤0.0191 %、现有 **4 对/chunk ≤0.076 %**（最不利读法 0.103 %）⇒ 全删 ≈ **+0.02 分**。本地每对 0.03~0.06 % ⇒ 平台/本地 ≤0.73 ⇒ **固定开销在平台上的占比比本地更小**（"每 chunk 固定税"里走旗标/管道同步的那部分从此有不利上界） | `#15.99` |\n'
    '| **26** | 🔴 **"把每 token 那条标量下标读聚合掉是杠杆"（含 P121 判据里"一条 `DataCopy` 整段拉进 UB"那条岔路）** | 重读 7 遍（同基址/同步进/同条数、逐位惰性、本地 Δ∝N）⇒ 平台 **Σ +1.56 %** ⇒ 一遍 ≈0.22 %、带边上界 **0.29 %**；探针 Σ 67.06 仍低于池内最高 68.06 ⇒ 同 P120 的"被零假设盖住"。⚠️ 唯一越点的 C1(+13.1 %) 在**五遍同字节逐点全距**（`C4 12.1 / C6 15.6 %`）之内；即便按命中读法，全删也只值 +0.03~0.04 分。⇒ 那条岔路本身还反向：**UB 标量读 97 ns > GM 标量读 38.6 ns** | `#15.100` |')

# ---- §6 优先表：P121 行 → 已读，新 P0 = P122 ----
add('| **P0** | **P121 = 标量下标扫描计量器**（剩下的两条候选里先打 (b)）：把每 token 那条 `idxGm_.GetValue` 额外读 N 条进丢弃值 | 38.6 ns/条 是 §4 里唯一还挂在"每表项乘一次"这一侧的价（`#15.39` 在旧形状上量到每单元 1024 条 = 39.5 µs，每 query 还要乘头块数 ⇒ 名义上限不小，却在平台上从没价过）。⛔ 扫描推进迭代器 ⇒ **不幂等**、不能"原地重跑"，只能"额外读 + 防 DCE" ⇒ **先本地两臂标定看 Δ 是否随 N 线性**（P119 的做法），不线性 = 尺子没生效。判据（§1.2 单向）：带内 ⇒ (b) 出局、只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB | ⬜ **下一做** |',
    '| — | ~~**P121 = 标量下标扫描计量器**（两条剩候选里的 (b)）~~ | 读完了：重读 7 遍 ⇒ 平台 **Σ +1.56 %**（紧邻同字节 +2.79 %、对池内最高为负），本地四臂 Δ∝N ⇒ 一遍 ≈0.22 %、**上界 0.29 % of wall** ⇒ (b) 出局（§5 #26）。⚠️ 方法学副产品：**不幂等的那段也能有计量器** —— "只重读地址 + 可证明不可达的 sink"，代价是**必须**先本地标定证明它没被 CSE | 🔴 已读 `#15.100` |\n'
    '| **P0** | **P122 = 跨核 barrier 计量器**（候选集最后一条 (c)）：把 AIC↔AIV 那轮真握手按**空闲硬旗标 id 成对复制** N 轮（生产端 Set / 消费端 Wait 一起复制 ⇒ 语义不变、只多来回） | 先验 = `#15.55` 的 **0.42 µs/轮** × 每单元轮数；这是"开销"一类里最后一个具名候选，也是 §1.1 那 ≈71 % 唯一还挂着名字的落点。⛔ **幅度必须做到逐点噪声够不着**（`#15.87` 的教训：先本地标定"一轮 %"，再把 N 推到 Σ ≥6 % 那一档），否则这一发不如不发；⛔ 复制体不得新增生产者/消费者、不得改依赖结构 | ⬜ **下一做** |')


def main():
    src = open(DOC, encoding="utf-8").read()
    bad = 0
    for i, (o, n) in enumerate(E, 1):
        c = src.count(o)
        if c != 1:
            print(f"[{i}] 命中 {c} 次（应 1）⇒ {o[:70]}")
            bad += 1
    if "P122 = 跨核 barrier 计量器" in src:
        print("已存在 P122 ⇒ 不动")
        bad += 1
    if bad:
        return 1
    if CHECK:
        print(f"锚点全部命中（条目={len(E)}），未改文件")
        return 0
    out = src
    for o, n in E:
        out = out.replace(o, n, 1)
    open(DOC, "w", encoding="utf-8").write(out)
    print(f"条目={len(E)} 锚点失败=0  净增长（字符数）={len(out) - len(src)}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
