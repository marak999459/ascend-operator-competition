#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""P120 状态板订正：code3.md 就地改（规则 3：只改被读数改变了的断言，旧句消失、不新增"已作废"）。
   每个锚点必须恰好命中一次；任一失败 ⇒ 整份不落盘。`--check` 只数不改。
   规则 1 的"只加不减不允许" ⇒ 同一批里带了三处压缩（P118/P119 与 §1.2 重复的口径、榜上现挂那行的旧叙事）。"""
import sys

DOC = "/home/fszqsn/ops_comp/ascend-operator-competition/code3.md"
CHECK = "--check" in sys.argv

E = []
add = lambda o, n: E.append((o, n))

# ---- 文首三条规则里的两个自指数字（每轮整读，过期即误导） ----
add("它现在 **273 行 / 48 KB**", "它现在 **≈290 行 / ≈56 KB**")
add("（当前到 `15.92` ⇒ 下一条 `15.93`）", "（当前到 `15.99` ⇒ 下一条 `15.100`）")

# ---- 顶部状态行：榜上现挂 13 遍 → 14 遍（顺手把旧叙事压掉） ----
add('**P105 字节第 13 遍** `[8.14, 8.58, 12.26, 12.3, 10.76, 14.4]`（Σ **66.44** = 高出池均值 66.04 只有 **0.6 %**',
    '**P105 字节第 14 遍** `[7.7, 8.32, 11.76, 13.08, 10.58, 15.32]`（Σ **66.76** = 高出池均值 66.09 只有 **1.0 %**')
add('榜上 `21.91` / `#35`，发次 `6ab487a6694b590c3cd0e9dd`（P119 探针读完立刻回发）',
    '榜上 `21.97` / `#37`，发次 `6ab495b1694b590c3cd911a7`（P120 探针读完立刻回发）')
add('🔴 **订正一条决断口径**："`追档零收益`"是池只有 9 遍、全距 0.6 % 时写的 ⇒ 同一天池撑到 12 遍、全距 **3.9 %**，'
    '且实测抓到一次：`ccb6da4` 抽出 Σ 68.06 → 榜上 **21.7**，同一份字节立刻再发 → 65.66 → **22.04**（**两发之间代码逐字节相同**）。',
    '🔴 **"追档零收益"已作废**（池撑到 12 遍、全距 **3.9 %** 之后实测抓到一次）：`ccb6da4` Σ 68.06 → 榜上 **21.7**，'
    '同一份字节立刻再发 → 65.66 → **22.04**（**两发之间代码逐字节相同**）。')
add('**同字节池现已 13 遍**：Σ 均值 66.04 ms', '**同字节池现已 14 遍**：Σ 均值 66.09 ms')

# ---- §1.1 一句话状态 ----
add('**榜上现挂 = P105 字节**（八遍同字节真值散在 21.7~22.3、均值 ≈22.0 ⇒ 榜上任何一档都是抽卡，追档零收益），距榜首 **3.6~4.8×**',
    '**榜上现挂 = P105 字节第 14 遍**（21.97 / #37；14 份同字节 Σ 池 65.50…68.06、均值 66.09 ⇒ 榜上任何一档都是抽卡，'
    '只有落在池外 ≥1.5 % 才补一枪回发），距榜首 **3.6~4.8×**')
add('**段占比轴 = 镜像**（P112 + P114 + P116 + P119，同一惰性机制）',
    '**段占比轴 = 镜像**（P112/P114/P116/P119 四把"工作"尺子 + P120 第一把"开销"尺子）')
add('🔴 **候选集现在只剩"开销"一类**：干活的四段全部量完且全部 ≤18 %，没被尺子碰过的只有每 chunk 的**同核旗标对**'
    '（4 对/chunk、单价从没量过）、**标量下标扫描**、**跨核 barrier**。9/24 那几条"去拿它"的发次两成一败加一判零：',
    '🔴 **候选集只剩"开销"一类里的两条**：干活的四段全部量完且全部 ≤18 %，而**同核旗标对也价完了**（4 对/chunk ≤0.08 % of wall）'
    '⇒ 没被尺子碰过的只有**标量下标扫描**（P121）与**跨核 barrier**。9/24~9/25 那五发"去拿它"：一败 + 四读数（后两发是否证）：')

# ---- 压缩：P118/P119 两条里与 §1.2 重复的口径 ----
add('。⚠️ 这一发同时把**同字节池从 9 遍撑到 12 遍、Σ 噪声带从 ±0.6 % 撑到 ±2 %** ⇒ 新判别口径：'
    '**两发单遍探针之间 <5 pp 不可判**（§1.2）。', '。（这一发把同字节池撑到 12 遍 ⇒ 口径见 §1.2）')
add('本地四臂在复制份数上**严格线性**（比值 1.96~1.99 / 3.90~3.98）⇒ 尺子可信，',
    '本地四臂在复制份数上**严格线性** ⇒ 尺子可信，')

# ---- P120 那行预告 → 读数，并写下 P121 ----
add('- 🔎 **下一发 P120 = 给"每 chunk 同核旗标对"定价**（第一把量**开销**的尺子）：chunk 尾追加 N 对**未用过**的 event 线'
    '（`MTE2_V(4)`）⇒ 零内存、零 UB、零数据依赖 ⇒ **构造级惰性**（前六把都得论证"这块内存没人读"，这把不用）。'
    '判据：Δ(1 对/chunk) ≥ 6 % ⇒ 方向换成**减旗标对数**（唯一一处既不在搬运也不在计算的可改代码）；Δ ≈ 0 ⇒ 只剩扫描与跨核 barrier。'
    '⛔ P117 继续降级。',
    '- ✅ **P120（第一把"开销"尺子）答案是否证的**（`LOG#15.99`、`p120_platform.txt`）：`FlushChunk` 尾追加 127 对**未用过**的 '
    '`SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 零内存、零 UB、零依赖 = **构造级惰性**（前七把都得论证"这块内存没人读"，这把不用）；'
    '本地每对 0.03~0.06 %、平台 127 对 = **Σ +2.42 %** ⇒ **每对 ≤0.019 %、4 对/chunk ≤0.076 %**。🔴 且这一发的 Σ 67.64 '
    '**比同字节池内最高 68.06 还低 0.62 %** ⇒ 不是"小于门"而是**被零假设整个盖住** ⇒ **减旗标对数判死**（§5 #25）。'
    '附带：**平台上旗标比本地便宜 1.3~3.2 倍**（与 P115"调用条数在平台上更贵"不矛盾 —— 每 chunk 耦合 vs 每 token 调用）。\n'
    '- 🔎 **下一发 P121 = 标量下标扫描计量器**（两条剩候选里的第一条，也是 §4 里唯一还挂在"每 token 乘一次"那一侧的价：'
    '38.6 ns/条 × 每 query 约 6.1 万次标量读）。⛔ 不能用"原地重跑"—— 扫描推进迭代器状态 ⇒ **不幂等**；'
    '可行形态 = 额外读 N 条下标进丢弃值，而那在 `-O2` 下有被 DCE 的风险 ⇒ **必须先本地两臂标定看 Δ 是否随 N 线性**'
    '（P119 的做法），不线性 = 尺子根本没生效。判据按 §1.2 单向口径：带内 ⇒ 只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB。'
    '⛔ P117 继续降级。')

# ---- §1.2 噪声口径：池 12→14 遍 + "被零假设盖住"这种读法 ----
add('池从 5 遍涨到 **12 遍**后 Σ 全距 = **3.9 %**（65.50…68.06，最差一遍榜上 21.7、最好 22.2）',
    '池从 5 遍涨到 **14 遍**后 Σ 全距 = **3.9 %**（65.50…68.06、均值 66.09，最差一遍榜上 21.7、最好 22.2）')
add('⇒ **噪声在否定方向上单向可用**。',
    '⇒ **噪声在否定方向上单向可用**。🔴 **P120 给出这条的最强形式**：探针 Σ 若**落进同字节池的分布内部**（甚至低于池内最高）'
    '⇒ 那**不是"小于某个门"，而是被零假设整个盖住** ⇒ 一发关闭、不补枪。')

# ---- §4 单价表：同核旗标对从"从没量过"变成有价 ----
add('⚠️ **同核 `SetFlag/WaitFlag` 一轮的单价从没量过**，而主循环每 chunk 压着 **4 对**（L965~988）⇒ **P120 的靶** |',
    '🔴 **同核 `SetFlag/WaitFlag` 一对已平台价（P120）**：127 对 ⇒ +2.42 % ⇒ **每对 ≤0.019 % of wall**、'
    '现有 **4 对/chunk ≤0.076 %** ⇒ 全删只值 +0.02 分（§5 #25）；且平台上比本地**便宜** 1.3~3.2 倍 |')

# ---- §4 总结行：划掉旗标 ----
add('⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销**：同核旗标对（P120）/ 标量扫描 / 跨核 barrier。',
    '⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销两类**：标量下标扫描（P121）/ 跨核 barrier'
    '（同核旗标对已价、≤0.08 % ⇒ 出局）。')

# ---- §5 排除清单 + #25 ----
add('| P119 复制 ×2 ⇒ 平台 **Σ +2.9 %**、本地同臂 **+18~33 %**（比 0.095）。带内判不出非零，但**关闭不需要过门**（§1.2）'
    '⇒ 连同 #19 把**整条向量线**结掉（三段 ≈11 % of wall） | `#15.98` |',
    '| P119 复制 ×2 ⇒ 平台 **Σ +2.9 %**、本地同臂 **+18~33 %**（比 0.095）。带内判不出非零，但**关闭不需要过门**（§1.2）'
    '⇒ 连同 #19 把**整条向量线**结掉（三段 ≈11 % of wall） | `#15.98` |\n'
    '| **25** | 🔴 **"减每 chunk 同核旗标对数是杠杆"（P120 判据里 Δ ≥ 6 % 那条岔路）** '
    '| 127 对 ⇒ 平台 **Σ +2.42 %**，而这一发比同字节池内最高（68.06）还低 0.62 % ⇒ 每对 ≤0.0191 %、'
    '现有 **4 对/chunk ≤0.076 %**（最不利读法 0.103 %）⇒ 全删 ≈ **+0.02 分**。本地每对 0.03~0.06 % ⇒ 平台/本地 ≤0.73 '
    '⇒ **固定开销在平台上的占比比本地更小**（"每 chunk 固定税"里走旗标/管道同步的那部分从此有不利上界） | `#15.99` |')

# ---- §6：P120 行改成已读 + 插 P121 ----
add('| **P0** | **P120 = 给"每 chunk 同核旗标对"定价** —— 第一把量**开销**不是**工作**的尺子：chunk 尾追加 N 对**未用过**的 '
    '`SetFlag/WaitFlag<MTE2_V>(4)` ⇒ 零内存、零 UB、零依赖 ⇒ **构造级惰性** |',
    '| — | ~~**P120 = 给"每 chunk 同核旗标对"定价**~~ —— 第一把量**开销**不是**工作**的尺子（`FlushChunk` 尾 ×127 对'
    '未用过的 `SetFlag/WaitFlag<MTE2_V>(4)`，零内存/零 UB/零依赖 ⇒ **构造级惰性**） |')
add('判据：Δ ≥ 6 % ⇒ 方向 = **减对数**（K/V 的 wait 合一、或让下一 chunk 的 wait 覆盖本 chunk 的 drain）；'
    'Δ ≈ 0 ⇒ 只剩标量扫描 + 跨核 barrier | ⬜ **下一做** |',
    '读完了：**每对 ≤0.019 % of wall ⇒ 4 对/chunk ≤0.076 %**，而 127 对那一发的 Σ 67.64 **低于同字节池内最高 68.06** '
    '⇒ 被零假设盖住（§1.2）⇒ **减对数判死**（§5 #25）；副产品 = "每 chunk 固定税"里走旗标/管道同步的那部分**已有不利上界** '
    '| 🔴 已读 `#15.99` |\n'
    '| **P0** | **P121 = 标量下标扫描计量器**（剩下的两条候选里先打 (b)）：把每 token 那条 `idxGm_.GetValue` 额外读 N 条进丢弃值 '
    '| 38.6 ns/条 是 §4 里唯一还挂在"每 token 乘一次"这一侧的价（每 query 约 6.1 万次读 ⇒ 名义上限很大、却在平台上从没价过）。'
    '⛔ 扫描推进迭代器 ⇒ **不幂等**、不能"原地重跑"，只能"额外读 + 防 DCE" ⇒ **先本地两臂标定看 Δ 是否随 N 线性**（P119 的做法），'
    '不线性 = 尺子没生效。判据（§1.2 单向）：带内 ⇒ (b) 出局、只剩跨核 barrier；≥6 % ⇒ 一条 `DataCopy` 把整段 `idx` 拉进 UB '
    '| ⬜ **下一做** |')


def main():
    src = open(DOC, encoding="utf-8").read()
    bad = 0
    for i, (o, n) in enumerate(E, 1):
        c = src.count(o)
        if c != 1:
            print(f"锚点失败 #{i} count={c}  «{o[:60]}…»")
            bad += 1
    if "P121 = 标量下标扫描计量器" in src:
        print("已存在 P121 ⇒ 不动")
        bad += 1
    if bad:
        return 1
    if CHECK:
        print(f"锚点全部命中（条目={len(E)}），未改文件")
        return 0
    out = src
    for o, n in E:
        out = out.replace(o, n, 1)
    open(DOC, "w", encoding="utf-8").write(out)
    print(f"条目={len(E)} 锚点失败=0  净增长（字符数，UTF-8 下中文 3 B）={len(out) - len(src)}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
