| 第三题 `sparse_flash_attention` | **平台 6/6 Pass 至今**，榜上现挂 = **P105 纯 AIV 字节第 14 遍**（同字节 14 遍 Σ 池 65.50…68.06、真值 21.7~22.3、历史最高 `22.29` ⇒ 榜上档位是抽卡，**抽到池外 ≥1.5 % 就补一枪回发**），距榜首 **3.6~4.8×**。⛔ **cube 线（M1/A′/M2d/PV-on-Cube）已无条件判死**（P111：平台六点全 `sbs=1`）。性能线现状 = **七个平台计量器**（六把量"工作"、P120 第一把量"开销"）：计算段 **+6 %**、gather **+18 %**（调用侧∶字节侧 ≈2.5∶1）、softmax 前段 **+2.4 %**、冷暖溢价 **+0.8 pp ≈ 0**、PV 段 **+2.9 % ≈ 0**、**同核旗标对 127 对 = +2.42 % ⇒ 每对 ≤0.019 % of wall、现有 4 对/chunk ≤0.076 %**（P112~P120）⇒ ⛔ "平台住在 HBM 那一侧"否证、"少搬字节"架构线死、**整条向量线（三段 ≈11 %）死**、**"减旗标对数"死**（§5 #25）。剩下 **≈71 % 无价**而候选只剩两条：**标量下标扫描**、**跨核 barrier** ⇒ 下一发 **P121 = 标量扫描计量器**（⛔ 扫描推进迭代器 ⇒ 不幂等、只能"额外读 + 防 DCE" ⇒ 必须先本地标定验证 Δ 随 N 线性；设计在 code3.md §6 P0）。✅ 口径升级：**Δ 落在噪声带内足以关闭一个杠杆**，P120 还给出最强形式 —— 探针 Σ **落进同字节池分布内部**（甚至低于池内最高）= **被零假设整个盖住** ⇒ 一发关闭、不补枪。⚠️ 本地代理已判废（P113）⇒ 方向判定只能一发一发给平台；⚠️ **噪声口径：逐点 ±5~10 %、六点合读 ±2 %**。全量口径见 [code3.md](code3.md)（状态板，整读） |
