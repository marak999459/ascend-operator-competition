#!/usr/bin/env python3
# P122 静态关闭 ⇒ code3.md 就地订正（锚点校验；--check 只验锚点不写盘）
# 事实依据（全部当场从 code 3/code/op_kernel/sparse_flash_attention.cpp 读出）：
#   1) 36 道 Set/WaitFlag 全是同核 V<->MTE2/MTE3（MTE2_V 6 / V_MTE2 6 / MTE3_V 12 / V_MTE3 12）
#      ⇒ 本核函数里**没有任何跨核旗标**；AIC 在 L174-177 提前 return。
#   2) 全文件唯一的跨核结构 SyncAll() 在 L268，外面套 `if (ks_ < 2u) { return; }`(L260)；
#      ks_>=2 要同时满足 lseOn_（L171，平台不给 LSE 输出）与 coreNum == ksh*total0，
#      而 total0 = B*S1*nHeadBlk >= rows >= 41 > 40（P108，6~9σ）⇒ 两道独立充分条件。
#   3) 另两处 PipeBarrier(L768 PIPE_ALL / L789 PIPE_V) 在 MergeToken(L729) 内 ⇒ 同样 ks=1 死；
#      热路径上只剩 L1030 一道 PipeBarrier<PIPE_V>（SoftmaxPv 第 3 段，每 chunk 1 次）。
import sys, io

P = "code3.md"
src = io.open(P, encoding="utf-8").read()
edits = []
def add(old, new):
    edits.append((old, new))

# ---- §1.1 候选句：两条 -> 零条，并写下"为什么零条不等于没事可做" ----
add('🔴 **候选集只剩"开销"一类里的一条**：干活的四段全部量完且全部 ≤18 %，而"开销"这一类里**同核旗标对**（4 对/chunk ≤0.08 %）与**标量下标扫描**（重读 7 遍 ⇒ s̄ ≤0.29 %）也价完了 ⇒ 没被尺子碰过的只剩**跨核 barrier**。',
    '🔴 **"开销"这一类已清空、且清空的方式是静态的**：同核旗标对（≤0.08 %）与标量下标扫描（≤0.29 %）价完之后，最后挂名的**跨核 barrier 根本不在执行路径上**（§5 #27，`#15.101`）⇒ **没有一个具名机制还挂着**。⇒ 那 71 % 只剩**依赖链本身**，而"复制一份同样的活"这一族计量器在构造上读不到它：复制体可以躲在别人的影子里（P119/P120/P121 那三个 ≈0 全是这个形状），**链不能**。')

# ---- §1.1 P121 那条 bullet 的尾巴：与 §5 #26 重复的 C1 叙事压回（本轮净涨的付款） ----
add('⚠️ 唯一点越出"同字节三遍经验带"的是 C1（+13.1 %），但 §1.2 那行五遍**逐点全距**已量到 `C4 12.1 % / C6 15.6 %` ⇒ 13.1 % 在"什么都没改"的单点包络内；且最不利读法（只 C1 命中、s=1.9 %）全删也只值 **+0.03~0.04 分 < 我们自己两发的回差** ⇒ 分辨真假本身不值一发。',
    '⚠️ 唯一点越带的 C1(+13.1 %) 落在五遍同字节**逐点全距**（`C4 12.1 / C6 15.6 %`）内，且最不利读法全删只值 +0.03~0.04 分 < 我们自己两发的回差 ⇒ 不补枪（账在 §5 #26）。')

# ---- §1.1 下一发 bullet：P122 不发 -> (c) 静态关闭 + P123 ----
add('- 🔎 **下一发 P122 = 跨核 barrier 计量器**（候选集最后一条）：把 AIC↔AIV 那轮真握手按**空闲硬旗标 id 成对复制** N 轮（Set/Wait 一起复制 ⇒ 语义不变、只多来回），先验 = `#15.55` 的 0.42 µs/轮。⛔ 判据必须按 `#15.87` 的教训**把幅度做到逐点噪声够不着**（先本地标定"一轮 %"、再把 N 推到 Σ ≥6 %），否则这一发不如不发；⛔ P117 继续降级。',
    '- ✅ **(c) 跨核 barrier 静态关闭，`#15.101` 一发没花**：全文件 36 道旗标**全是同核** V↔MTE（6/6/12/12）⇒ 本算子没有任何跨核旗标；唯一的跨核结构 `SyncAll` 在 L268、外面套 `if (ks_ < 2u) return`，而 `ks_ ≥ 2` 要同时过 `lseOn_`（平台不给 LSE 输出）和 `coreNum == ksh·total0`（`total0 ≥ rows ≥ 41 > 40`，P108）⇒ **两道独立充分条件都把它杀掉**。另两处 `PipeBarrier` 与它同在 `MergeToken` 里 ⇒ 一起死；热路径上只剩每 chunk 一道 `PipeBarrier<PIPE_V>`（L1030）。\n'
    '- 🔎 **下一发 P123 = 依赖链计量器**（唯一没被碰过的一类，且与 P119 只差一个变量）：`SoftmaxPv` 第 5 段之后追加 N 遍 `Muls(o_i, o_i, 1.0f, rowC)`（i<nbCur）⇒ **指令/宽度/目的端与第 4 段那道真重缩放逐字同型**（`x*1.0f` 逐位恒等是本仓库自己 L347 在用的），但 `dst == src` ⇒ **每 chunk 自依赖深度 N**，不像 P119 那样能重叠。按吞吐外推它的"无依赖"价 ≈ PV 的 1/m ⇒ **≈0.06 %**，所以：Δ(P123) ≈ 0.06 % ⇒ 链不是钳子、71 % 彻底没有具名落点；Δ(P123) ≫ 之 ⇒ **链是钳子**，而链的解法是**双累加器交错**（拆自依赖、不动指令数 —— 第一次有理由动的真实改动）。P117 继续排最后。')

# ---- §4 一句话总结尾巴 ----
add('⇒ 干活的四段量完 ≈29 %，**没被碰过的只剩开销一类里的一条**：跨核 barrier（P122）。',
    '⇒ 干活的四段量完 ≈29 %，**"开销"一类清空**（(c) 静态死、#27）⇒ 没被碰过的只剩**依赖链**（P123，与 P119 只差"依赖"这一个变量）。')

# ---- §5 排除清单：#27 ----
add('⇒ 那条岔路本身还反向：**UB 标量读 97 ns > GM 标量读 38.6 ns** | `#15.100` |',
    '⇒ 那条岔路本身还反向：**UB 标量读 97 ns > GM 标量读 38.6 ns** | `#15.100` |\n'
    '| **27** | 🔴 **"跨核 barrier 轮次是 71 % 那块墙的落点"（P122 的立意）** | **不用发货，代码路径上不存在**：36 道旗标全是同核 V↔MTE ⇒ 零跨核旗标；唯一的 `SyncAll`(L268) 在 `ks_ ≥ 2` 分支内，而 `ks_ ≥ 2` 需同时满足 `lseOn_`（平台无 LSE 输出）与 `coreNum == ksh·total0`（`total0 ≥ rows ≥ 41 > 40`，P108）⇒ 两道独立充分条件各杀一次；`MergeToken` 里那两处 `PipeBarrier` 同一条路径一起死。⚠️ **方法学（比这条本身值钱）**：即便它在，**复制族计量器也读不到 barrier** —— 空流水上多一道旗标是免费的（P120 的 ≈0 就是这么来的），barrier 的价只在**删掉它**时显形，而删除从不逐位惰性 ⇒ 开销类里只有"有发射成本的那部分"（旗标对、标量读）可价 | `#15.101` |')

# ---- §6 优先级表：P122 行 -> 静态关闭，另开 P123 P0 ----
add('| **P0** | **P122 = 跨核 barrier 计量器**（候选集最后一条 (c)）：把 AIC↔AIV 那轮真握手按**空闲硬旗标 id 成对复制** N 轮（生产端 Set / 消费端 Wait 一起复制 ⇒ 语义不变、只多来回） | 先验 = `#15.55` 的 **0.42 µs/轮** × 每单元轮数；这是"开销"一类里最后一个具名候选，也是 §1.1 那 ≈71 % 唯一还挂着名字的落点。⛔ **幅度必须做到逐点噪声够不着**（`#15.87` 的教训：先本地标定"一轮 %"，再把 N 推到 Σ ≥6 % 那一档），否则这一发不如不发；⛔ 复制体不得新增生产者/消费者、不得改依赖结构 | ⬜ **下一做** |',
    '| — | ~~**P122 = 跨核 barrier 计量器**（候选 (c)）~~ | **静态关闭、一发没花**：平台路径上**没有跨核同步**（36 道旗标全同核；`SyncAll` 与 `MergeToken` 的两道 `PipeBarrier` 都在 `ks_ ≥ 2` 里，而 `ks = 1` 由 `lseOn_` 与 `coreNum == ksh·total0` 两道独立条件各自证死）⇒ §5 #27。附带口径：**复制族量不了 barrier**（空流水上的旗标免费） | 🔴 已结 `#15.101` |\n'
    '| **P0** | **P123 = 依赖链计量器**：`SoftmaxPv` 尾追加 N 遍 `Muls(o_i, o_i, 1.0f, rowC)`（i<nbCur、dst==src ⇒ 自依赖）+ 本地四臂标定 | 与 P119 **只差"依赖"这一个变量**：同指令类、同宽度(512 fp32)、同目的端(真累加器)、位置就在真重缩放旁边，唯一区别是 P119 写 scratch 可自由重叠、这一发写回自己不能。按吞吐账它的"无依赖价"≈ PV ÷ m ⇒ **≈0.06 %** ⇒ **读数只要 ≥6 % 就只能是链**。判死/判活都直接接下一步（活 ⇒ **双累加器交错**；死 ⇒ 71 % 无具名落点、转"停止定价、只读结构"） | ⬜ **下一做** |')

# ---- 头部自指：LOG 编号 ----
add('当前到 `15.100` ⇒ 下一条 `15.101`', '当前到 `15.101` ⇒ 下一条 `15.102`')

n = 0
bad = []
for old, new in edits:
    c = src.count(old)
    if c != 1:
        bad.append((c, old[:48]))
        continue
    src = src.replace(old, new)
    n += 1
print("anchors ok %d/%d" % (n, len(edits)))
for c, s in bad:
    print("  BAD count=%d  %s" % (c, s))
if bad:
    sys.exit(1)
if "--apply" in sys.argv:
    io.open(P, "w", encoding="utf-8").write(src)
    print("written")
