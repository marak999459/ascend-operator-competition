
### 15.102 ✅ P123：九把「惰性复制」尺子里**第一把读到非零**的 —— 活端 63 圈 ⇒ 平台 **Σ 81.18 = +22.8 %**、六点全正且强分级；⚠️ 但这个数**现在还不能引用成"链的价"**，它被一个从没量过的 `m` 污染 ⇒ 当场立 P124（死端孪生）补第三个方程

- **改了什么**：`probes/p123_chainrep.py N`（0 = pristine 直拷，本次 N=63）在 `SoftmaxPv` 第 5 段（PV 累加 j0 循环）收尾之后追加 N 圈 × `nbCur` 条 `Muls(o[i*rowC], o[i*rowC], 1.0f, rowC)`。与 P119 的**唯一结构差别 = 目的端是真累加器 `o`** ⇒ `dst==src` 让 N 份互相串行（P119 写 `kfBuf_` 末行、可自由重叠）。惰性的论证用本仓库自己 L339 那句"IEEE 下 `x*1.0f` 对**所有**浮点值逐位恒等"（L347/L360 已拿它做透传），写的就是活累加器 ⇒ 不需要 sink、也不可 DCE。当场门：`MARK=1 LOOP=1 INNER=1 MULS=+1 FLAG=36 BUFF=13`、行数增量恒 **+10**（与 N 无关）、68923 → 69706/69709 B。
- **本地四臂先标定**（两场 `p123_calib_low/hi.txt`，REPS=5、8 档、同一份 pristine 现生成）：Δ 对 N 近线性 —— 以 Δ7 为基准，`g64c1` +2.0 / +10.2 / +19.8 %、`w3` +4.3 / +17.1 / +34.0 %（N=7/31/63；八档对理想比例的偏差 ≤15 %）；N=1 那一圈已经在噪声里（−0.6 ~ +0.5 %）⇒ **幅度只能靠 N 堆**。8 档 × out/max/sum 对一次性 `golden_p123/` 全 `逐位一致` ⇒ 尺子在本地确实在工作。
- **平台读数**（探针 Σ **81.18**、榜上 19.67 / #42）：对 **16 遍**同字节池均值 66.12 = **+22.8 %**、对池内最高 68.06 仍 **+19.3 %** ⇒ 这一族**第一次把"零假设"整个甩开**（P119/P120/P121 的探针 Σ 全落在池内、符号都不齐）。逐点 Δ（对 3 遍池均）`C1 +12.1 / C2 +5.6 / C3 +16.6 / C4 +36.9 / C5 +31.8 / C6 +58.7 %` ⇒ **六个点全正、跨度 10×**，与 P114（+9~+35.6 %）同型 —— "六点同型"这条老口径在链轴上第 N 次被否证。
- ⚠️ **预登记的判据没有直接兑现**（当时写"Δ ≥12 % ⇒ 链型 ⇒ 双累加器交错"）：那条 null 把"每 chunk 的 token 数 `m ∈ [32,64]`"当既成事实，而 `m`（生产端 = `pend`；host 侧只有上界 `toks`，且 `CalcBlocking` 注释明说它会**高估**）**从没在平台量过**。本地那两场标定顺手把它量了：P119 的本地臂 = 每多跑一遍 PV（加 `m·nbCur` 条）= +18.4~+33.4 %，与 P123 的 N=63（加 `63·nbCur` 条）在 **8/8 档**上差 ≤4.3 pp（其余七档 ≤1.4 pp）⇒ 两把加的条数只可能相等 ⇒ **本地 `m` = 64**（这些档 host 全选 `n_blk=64`，与选档一致）。
- **于是三未知、两方程**（p_活、p_死、平台 `m`）：平台上 P119（死端 `m·nbCur` 条，+2.9 %）与 P123（活端 `63·nbCur` 条，+22.8 %）那 **7.8×** 缺口有三个互斥解释，每个都要推翻一句现话 —— **(甲) 链**（本地链不暴露、平台暴露 ⇒ 双累加器交错有戏）；**(乙) 平台编译器擦掉死写**（⇒ P119/P112/P116 三把押在"随后被覆写的存储"上的尺子整体降为下界、§1.1 那句"向量线合计只值 ≈11 %"作废、PV 段按活端价重开）；**(丙) 平台 `m` ≈ 8**（纯吞吐账完全自洽：`63/8 × 2.9 = 22.9 ≈ 22.8` —— 但 `NBLK_MIN=16`，且 §1.1 每一处 PV 折算都建在"chunk 填得满"上）。
- 🔴 **方法学（本轮真正拿走的一条）**：**"本地同价、平台差一个数量级"第一次被抓到实例** —— 同一份指令形态、同样的条数、同样的宽度，两侧价格 7.8×。这是 §5 #21（"本地段占比不可外推到平台"）的最强一次佐证，也是这一族计量器**必须逐发本地双臂标定**的全部理由：没有那两场标定，今晚就会把 +22.8 % 直接读成"链是钳子"，然后去付一次双累加器重构。⇒ §5 新开 #28 把这条**排除**写进必查清单（P123 只能引用成"活端一圈 ≤0.36 % of wall"这个上界）。
- ⇒ **下一发 P124 = P123 的死端孪生**（`p124_deadrep.py` + `p124_design.txt`，判据已预登记）：同位置/同指令/同宽度/**同条数**/同链距，只把目的端换成 `kfBuf_` 的行（内圈 `min(nbCur, scGrp_)`：`NB_CAND` 上限 32、`NBLK_MIN` 16 ⇒ 除 `n_blk=16 ∧ nb=32` 外与 P123 严格同基）。⇒ **`m` = 182.7/Δ124**、**p_活∶p_死 = 22.8∶Δ124**，(甲)(乙)(丙) 一发裁掉。⛔ 硬前置（本轮已绿）：本地两臂 Δ124 显著非零 + 8 档逐位一致 —— 实测 Δ124 = +19.6~+34.1 %，与 Δ123 **逐档对齐 ≤0.14 pp**（八档全表），⇒ 本地完全读不到"死活"这个轴，这一发的信息量全在平台上。
- **回发（latest 计分）**：回发 = P105 字节第 **16** 遍（榜上确认 `sub=3cec18f5`），Σ **67.54** ⇒ 榜上 **21.89 / #38**；池均值 66.03 → **66.12**、全距 65.24…68.06（4.3 %）不变 ⇒ 这一遍在池内、不重抽。⚠️ 两笔操作教训：① 探针发次后**立刻**回发会撞 **HTTP 429**（第一次回发静默死在"提交代码到题目…"那行，榜上仍是探针 `3ce970b1` 才发现）⇒ 回发**必须看榜确认**，不能看日志尾巴；② `nohup TMO=… bash` 不成立（nohup 不吃 env 前缀），要 `nohup env TMO=… bash`。
- **文档**：§1.1 榜上口径与池计数、§1.2 的 🔎P123 → ✅ 读数条 + 新 🔎P124 条、§5 开 #28（排除 + 方法学）、§6 P119/P120/P121 三行压缩 + P123 划掉 + P124 开 P0；⚠️ 顺带把 §5 #24 那句改写成"成立条件挂在 P124 上"。四批合计净 **+1778 B / +3 行**（新增量 = §5 #28 + §1.2 两条 + §6 一行 P0，压缩抵掉 §5 四行与 §6 三行的重复 586 B）。
