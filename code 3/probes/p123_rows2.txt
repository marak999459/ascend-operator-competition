| **24** | 🔴 **"PV 段（含内层那条 97 ns 标量 UB 读）是平台上的大杠杆"** —— 最后一块没价的大 AIV 工作（本地占 AIV 31~34 %） | P119 复制 ×2 ⇒ 平台 **Σ +2.9 %**、本地同臂 +18~33 % ⇒ 连同 #19 把**整条向量线**结掉（三段 ≈11 % of wall）。🔴 **本条的成立条件现在挂在 P124**：那 2.9 % 是"**死端**"价；若"目的端死活"是一个真实的轴，本条要按活端价重开 | `#15.98` + 待 P124 |
| **25** | 🔴 **"减每 chunk 同核旗标对数是杠杆"（P120 判据里 Δ ≥ 6 % 那条岔路）** | 127 对 ⇒ **Σ +2.42 %**，而这一发比同字节池内最高（68.06）还低 0.62 % ⇒ 每对 ≤0.019 %、现有 **4 对/chunk ≤0.076 %** ⇒ 全删 ≈ +0.02 分。副产品：平台/本地 ≤0.73 ⇒ 固定开销在平台上的占比比本地**更小** | `#15.99` |
| **26** | 🔴 **"把每 token 那条标量下标读聚合掉是杠杆"（含 P121 那条"一条 `DataCopy` 整段拉进 UB"岔路）** | 重读 7 遍 ⇒ **Σ +1.56 %** ⇒ 一遍 ≈0.22 %、带边上界 **0.29 % of wall**；探针 Σ 67.06 仍在池内 ⇒ 与 P120 同属"被零假设盖住"。唯一点越带的 C1(+13.1 %) 在五遍逐点全距（`C4 12.1 / C6 15.6 %`）内，最不利读法也只值 +0.03~0.04 分。⚠️ 那条岔路还**反向**：UB 标量读 97 ns > GM 标量读 38.6 ns | `#15.100` |
| **27** | 🔴 **"跨核 barrier 轮次是 71 % 那块墙的落点"（P122 的立意）** | **不用发货，代码路径上不存在**：36 道旗标全是同核 V↔MTE、零跨核；唯一 `SyncAll` 与 `MergeToken` 的两道 `PipeBarrier` 都在 `ks_ ≥ 2` 分支内，而 `ks_ ≥ 2` 要同时满足 `lseOn_`（平台无 LSE 输出）与 `coreNum == ksh·total0`（`ksh ≥ 2 ⇒ ksh·total0 ≥ 2·41 = 82 > 40 ≥ coreNum`）⇒ 两道独立充分条件各杀一次 ⇒ 热路径只剩每 chunk 一道 `PipeBarrier<PIPE_V>`（逐条现场数法在 `#15.101`）。⚠️ **方法学（比这条本身值钱）**：即便它在，**复制族计量器也量不了它** —— 空流水上的重复旗标/障碍免费（P119/P120/P121 那三个 ≈0 全是这个形状）⇒ **barrier 的价只在"删除"那一侧读得到，而删除不逐位惰性** | `#15.101` |
