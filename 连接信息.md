# 连接信息（NPU / VM / 密钥）

> **本文件是"从哪里读连接参数"的唯一说明处。**
> ⛔ 这里的**具体值一律不写死**：端口、私钥路径会变，每次都要向用户确认或从现场读取。
> 文档正文（含 `AGENT.MD`、`算子开发工作流.md`、`code1~code3.md`）都只引用本文件，不再硬编码任何地址。


**相关文档**：[AGENT.MD](AGENT.MD)（入口） · [算子开发工作流.md](算子开发工作流.md)（通用流程） · [code1.md](code1.md) · [code2.md](code2.md) · [code3.md](code3.md)
---

## 1. 凭据文件（相对项目根目录）

| 相对路径 | 用途 | 归属 |
|---|---|---|
| `密钥.txt` | 已加密的 CANNJudge 登录密文（提供给 CANNBot） | 提交/登录 |
| `public.pem` | RSA **公钥**，用于本地加密 CANNJudge 密码 | 提交/登录 |
| `encrypt_password.py` | 加密脚本；默认读同目录的 `public.pem` | 提交/登录 |

**读取方式（不要向用户反复索要）**：

```bash
# 公钥：直接用，脚本默认就是同目录
python3 encrypt_password.py

# 若公钥不在当前工作目录，显式指定
python3 encrypt_password.py --public-key <项目根>/public.pem
```

- 依赖：`pip install pycryptodome`
- ⛔ **不要打印/记录密文**：`密钥.txt` 的内容只用于 API 调用，不写进任何日志或文档。
- ⛔ 这些文件**绝不能进提交**（提交只含算子源码）。

---

## 2. NPU 真机连接（隧道需**人工**建立）

| 项 | 取值来源 |
|---|---|
| 主机 | `127.0.0.1`（回环，走本地端口转发） |
| 端口 | **每次向用户确认**；该端口**会变**（本项目历史上见过两个不同值） |
| 用户名 | `developer` |
| 私钥路径 | **每次向用户确认**，或从现场读取（位于用户目录下的 `.ssh` 凭据目录内） |
| CANN 路径 | `~/Ascend/cann-9.0.0/set_env.sh` |

**调用形式（占位符）**：

```bash
ssh -p <PORT> -i <KEY_PATH> -o BatchMode=yes developer@127.0.0.1 "<命令>"
```

### 2.1 ⚠️ 隧道行为（已实测，务必照做）

| 现象 | 说明 | 做法 |
|---|---|---|
| `Connection refused` | 隧道是 **VS Code Remote-SSH 扩展拉的 WebSocket 转发**，**不是常驻监听** | ⛔ **不要反复重试**，直接问用户"隧道断了吗，能不能连一次" |
| `Test-NetConnection <PORT>` 探不到 | 该端口不是常规监听端口，探测手段不适用 | ⛔ 不要用它判断连通性 |
| 需要用户先动手 | 必须先由用户用 VS Code 通过 SSH 连上 NPU 环境 | ⛔ **用户确认连接成功之前，不要执行任何 NPU 操作** |

### 2.2 提交平台专用入口（另有其法）

`cannjudge-submit` 技能的认证走 `密钥.txt` 的密文 + `public.pem`，**与上面这条 SSH 隧道是两套东西**，不要混用。详见 `算子开发工作流.md` §5。

---

## 3. 本地 VMware 虚拟机（CPU 调试环境）

| 项 | 取值来源 |
|---|---|
| 用途 | **本地 CPU 逻辑验证**（不是 WSL / 不是 Docker，是 VMware 里的 Ubuntu + 直接部署的 CANN） |
| 主机 / 用户名 | **现场读取**（VMware 设置或虚拟机内 `ifconfig` / `ip addr`） |
| CANN 路径 | `/usr/local/Ascend/cann-9.0.0/set_env.sh` ⚠️ **与 NPU 的路径不同，不要照抄** |

> ⚠️ **两台环境的环境变量必须分别确认**，混用会直接报 `libascend_hal.so: cannot open shared object file`。

---

## 4. 通用上机注意事项

| 坑 | 现象 | 做法 |
|---|---|---|
| `scp` 卡死 | 长时间无输出后超时（隧道本身正常，`ssh` 秒回） | 改用 `cmd /c type` 管道传输，**传完必须 `md5sum` 复验** |
| PowerShell 管道改行尾 | 远端文件比本地**多 2 字节**（LF → CRLF），MD5 对不上 | 同上；字节敏感的场合尤其注意 |
| 跑测试前忘记 source | `libascend_hal.so: cannot open` | 每次跑测试前先 `source <对应 CANN 路径>/set_env.sh` |
