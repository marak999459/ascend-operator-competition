# 连接信息（NPU 真机 / 密钥）

> **本文件是"从哪里读连接参数"的唯一说明处。**
> ⛔ 这里的**具体值一律不写死**：端口、私钥路径会变，每次都要向用户确认或从现场读取。
> 文档正文（含 `AGENT.MD`、`算子开发工作流.md`、`code1~code3.md`）都只引用本文件，不再硬编码任何地址。


**相关文档**：[AGENT.MD](AGENT.MD)（入口） · [算子开发工作流.md](算子开发工作流.md)（通用流程） · [code1.md](code1.md) · [code2.md](code2.md) · [code3.md](code3.md)
---

## 1. 凭据文件（相对项目根目录）

| 相对路径 | 用途 | 归属 |
|---|---|---|
| `密钥.txt` | 已加密的 CANNJudge 登录密文（提供给 CANNBot） | 提交/登录 |
| `public.pem` | RSA **公钥**，用于本地加密 CANNJudge 密码 | 提交/登录 |
| `encrypt_password.py` | 加密脚本；默认读同目录的 `public.pem` | 提交/登录 |

**读取方式（不要向用户反复索要）**：

```bash
# 公钥：直接用，脚本默认就是同目录
python3 encrypt_password.py

# 若公钥不在当前工作目录，显式指定
python3 encrypt_password.py --public-key <项目根>/public.pem
```

- 依赖：`pip install pycryptodome`
- ⛔ **不要打印/记录密文**：`密钥.txt` 的内容只用于 API 调用，不写进任何日志或文档。
- ⛔ 这些文件**绝不能进提交**（提交只含算子源码）。

---

## 2. NPU 真机 / 云端 CPU 环境连接（隧道可用脚本自举）

**环境角色表（2026-09-20 用户逐一口述确认；别名短名 = `devenvc_<短名>.<envId>` 的前半段）**

> ⭐ **唯一权威 = `devspace_tunnel.ps1` 顶部的 `$EnvTable`**（`role` / `use` / `note` 三个字段一行写完）。下表是它的文档镜像；**加新环境（比如后面要加的第二个 NPU）只需在 `$EnvTable` 补一行**，脚本下次运行就会认出。

| 短名 | 角色 | `use` | 状态与约定 |
|---|---|---|---|
| `02aeb` | **NPU**（真机） | `free` | 隧道可由脚本自举；**上机跑东西前仍先问用户** |
| （第二台 NPU） | **NPU**（真机，**第二个账号**） | — | ⚠️ **2026-09-20 方案改为方案①：它不出现在 Windows 的 `.ssh/config`**，而是出现在**通道机（VMware VM）内**的 `.ssh/config`（homedir 隔离，见 §2.3）。驱动走 `ssh -A … "<二跳>"`，**不进** Windows 侧 `$EnvTable` 的 `-Role npu` 候选 |
| `bna7c` | 已删除 | `no` | 容器已销毁，`.ssh/config` 里只是残留条目 → **不要用** |

**派活闸门（脚本内建，2026-09-20 加）**：`-Role npu` 只走 `use='free'` 的环境（CPU 仿真环境已停用）；`reserved` / 未登记的新环境会被跳过并打印 `[门] 跳过 <短名> -> 要用它必须 -Env <短名>`。⛔ Agent **不得**为了"有环境用"而自行 `-Env` 越过闸门——被挡下即视为"没有可用环境"，回去向用户报告缺什么。**用户口头指定某个环境**时才算显式授权。

> 端口与完整别名**不写进本文档**（每次新建环境都会变）。现场读取：
> `powershell -NoProfile -ExecutionPolicy Bypass -File devspace_tunnel.ps1 -List`
> 日常用法：**"连真机" → `-Role npu`**（CPU 仿真环境已停用），指定单个再加 `-Env <短名>`；脚本负责按需拉起隧道 + 验证 + 失败判读。`-Watch` 可循环保温（默认前台跑 20 轮 × 60s）。


| 项 | 取值来源 |
|---|---|
| 主机 | `127.0.0.1`（回环，走本地端口转发） |
| 端口 | **现场读**（`devspace_tunnel.ps1 -List` 或 `.ssh/config`）；该端口**每个环境一个、重建即变** |
| 用户名 | `developer` |
| 私钥路径 | **现场读** `C:\Users\<user>\.atomgitdevenv\.ssh\config` 的 `IdentityFile`（用 `-F <config>` 就不用手敲） |
| CANN 路径 | `~/Ascend/cann-9.0.0/set_env.sh` |

**调用形式（占位符）**：

```bash
ssh -F "C:\Users\<user>\.atomgitdevenv\.ssh\config" <HOST_ALIAS> "<命令>"
```

### 2.1 ⚠️ 隧道行为（已实测，务必照做）

| 现象 | 说明 | 做法 |
|---|---|---|
| `Connection refused` | 转发服务无监听（抖动 **或** 环境本体没了） | 跑 `devspace_tunnel.ps1 -Role <角色>` 自举；仍不通看下一行的判读，**不要重试轰炸** |
| `no longer exists` / 换不出 connect_url | 服务端**认不得这个环境** —— 两种成因，处置完全不同 | 跑 `devspace_tunnel.ps1 -Env <短名> -Diag`，看它交叉读扩展日志给的判定：**带 `status`=N** ⇒ 环境在列表里只是没开机 → 控制台点**启动**；**不带 status** ⇒ 当前登录账号列表里查不到 → **切回它所属账号并点一次『连接』**重签转发凭据 |
| `Connection closed by 127.0.0.1:<PORT>` | 转发服务/容器侧掉线 | 同上：先脚本自举一次，仍不通就报告用户 |
| `Connection timed out during banner exchange` | 隧道断了 | 停手报告，请用户在 VS Code 终端（不经隧道）确认隧道状态 |
| 端口在监听但 ssh 不通 | 容器内 sshd 挂了/被压满 | 让用户在 IDE 终端里看负载，脚本 `-Diag` 看日志 |
| `Test-NetConnection <PORT>` 探不到 | 该端口是转发服务监听，探测手段不适用 | ⛔ 不要用它判断连通性 |

- ⭐ **`.ssh/config` 的 Host 别名自带 `Match !exec` 自举**：连别名就会拉起转发进程（`forward-bootstrap\vscode\<envId>.cmd` → `Code.exe` 当 node 跑 `forward-bootstrap.js --config <envId>.json`）。→ `devspace_tunnel.ps1` 就是把"自举 + 等端口 + ssh 验证 + 日志判读"固化下来。
- ⚠️ **但转发凭据不是永久有效的**：json 里的 `connect_code` 是**签发时那次 IDE 连接**的凭据。实测（2026-09-20）`e6z6k` 的条目 15:51 签发时能连，16:06 桌面 VS Code 走了一次 logout→login（换成 `tpm0u` 的账号），16:17 起服务端对这个 devEnvId 报"环境不存在"——**同一时间控制台里另一个账号显示该环境运行中**。所以"no longer exists"有两种真因：**环境真被回收** 或 **当前登录账号已不是它的主人**。
- 🔎 **判据（脚本 `-Diag` 已自动做）**：扩展日志 `logs/vscode/latest.log` 里 `bootstrapRefresh.skip.envNotRunning` 这一行——**带 `status":N`** = 环境在列表里只是没开机 → 点**启动**；**不带 status** = 列表里查不到这个 devEnvId → **切回它所属账号并点一次『连接』**，扩展会重写 Host + 重签 `connect_code`。
- 🔬 **互斥根因（2026-09-20 逆向扩展源码 + 现场配置查清，解释"为什么两个账号不能同时连"）**：
  1. **端口/别名不是冲突源**：`.ssh/config` 里**每个环境一个 Host 块、各自独立 `localPort`**（实测 4 个块并存，`02aeb` 的 `Port` 与 `tpm0u`/`e6z6k` 互不相同），`forward-bootstrap/vscode/<envId>.json|.cmd` 也**按 envId 分文件**；
  2. `<envId>.json` 里**没有任何凭据字段**（字段仅 `devEnvId / localPort / remotePort / serverAddr / enableSSL / extensionVersion / runtimePath / hubScriptPath / hubLockFilePath / source / updatedAt / version`）⇒ **连接凭据是运行时向 hub 现签的**，不是签发后落盘；
  3. hub 目录 = `path.join(os.homedir(), ".atomgitdevenv", ".hub", "vscode")`（扩展 `extension.js` 写死 `homedir()`，grep `ATOMGIT_*` 环境变量开关 **0 命中**），锁文件 `1.1.2.LOCK` 内容是 **`pid|pid` 两个进程号**，**路径里没有账号维度**；
  4. 扩展登录态存在 VS Code **`globalState`**（随 user-data-dir 走）。
  ⇒ **一个 Windows 用户下只有一个 hub 单例，它按"当前登录的那个账号"签凭据**。切账号后 `bootstrapRefresh` 会用新账号去刷旧环境 ⇒ 报 `envNotRunning` 且不带 `status`（就是上面 `e6z6k` 那次事故的真因）。
  ⚠️ **推论（未实测）**：只开两个 VS Code Profile（分开 `--user-data-dir`）**不足以并行**——登录态能分开，但 hub 锁与 `forward-bootstrap` 仍在同一个 homedir，第二个实例会 attach 同一个 hub 而非另起。⇒ 要并行必须隔离 **homedir**，见 §2.3。
- ⛔ **不要删 `~/.atomgitdevenv`、不要改 VS Code 安装位置或扩展版本**：`.cmd`/`.json` 里写死了 `runtimePath` 与 `hubScriptPath`（含版本号），挪走或升级后旧配置指空，需回 IDE 重新生成。
- ⚠️ **`devspace_tunnel.ps1` 必须存成 UTF-8 with BOM**：PS 5.1 无 BOM 时中文注释的字节会被读乱，**连 `param` 块都解析不了**（实测 `表达式或语句中包含意外的标记 ')'`）。改完这个文件先跑 `-List` 确认还能解析。

### 2.2 比赛平台专用入口（另有其法）

`cannjudge-submit` 技能的认证走 `密钥.txt` 的密文 + `public.pem`，**与上面这条 SSH 隧道是两套东西**，不要混用。详见 `算子开发工作流.md` §5。

### 2.3 ⭐ 第二账号真机 = 走通道机（VMware VM）的隔离通道（2026-09-20 选定方案①）

§2.1 的根因决定了：**同机并行两个账号唯一干净的隔离粒度就是 `os.homedir()`**。落地做法 = 在**通道机（VMware Ubuntu VM）**里装第二个 VS Code、登录**第二个账号**、由它连**第二台真机**；Windows 侧继续用第一个账号。两边 homedir 天然隔离，各有各的 hub。装机进度见下面的现状表。

**驱动方式（关键）**：VM 内的隧道只有 VM 自己能签凭据，所以从 Windows 驱动第二台真机要**二跳**，且私钥**不出 VM**：

```bash
ssh -A fszqsn@192.168.101.128 \
  "ssh -F ~/.atomgitdevenv/.ssh/config <VM里的环境别名> '<远端命令>'"
```

> ⛔ **不要**改用"Windows `-L` 穿透 VM 端口"那条路——那要求把第二账号的私钥拷到 Windows，等于把方案①的隔离削掉一半。

**VM 现状（2026-09-20 21:15 实测）**：

| 项 | 值 | 备注 |
|---|---|---|
| 连通 | ✅ `fszqsn@192.168.101.128:22` 免密可达 | 用现有 `~/.ssh/id_ed25519`，hostname `fszqsn-virtual-machine` |
| 系统 / 资源 | Ubuntu 22.04.5 / **4 vCPU** / 15G 内存 / 58G 空闲 | ⚠️ **不跑仿真**（2026-09-22 起仿真流程停用，直连真机），别把算力活派给它 |
| 出网 | ✅ `update.code.visualstudio.com` 200、`atomgit.com` 200 | 可直接装 VS Code 与扩展 |
| ⚠️ **无 `curl`** | 只有 `wget` | VM 内凡脚本用 curl 处一律换 `wget -qO-` |
| ⚠️ **sudo 要密码** | `sudo -n true` → `a password is required` | ⇒ Agent 装不了任何 `.deb`/apt 包；**一律走"免 root 装到家目录"**（VS Code 就是这么装的） |
| VS Code | ✅ **1.138.0** 已装 `~/opt/VSCode-linux-x64`，启动器 `~/.local/bin/code`（软链） | 官方 tarball `https://update.code.visualstudio.com/latest/linux-x64/stable`，**不需要 root**。桌面 GUI 由用户在 VM 里自己开（GNOME 在 tty2 已登录） |
| devspace 扩展 | ✅ **`atomgit-cloudenv.cannlabdevspace@1.1.2`** 已装进 VM 的 `~/.vscode/extensions/` | 从 Windows 同目录 tar 管道拷（36 文件、无 `.exe/.node/.dll` ⇒ 跨平台可直接搬）；`package.json` md5 双侧一致 `f3d7e5ba…`；`code --list-extensions` 已认出 |
| Qoder CN | ✅ **CLI v1.1.59** 已装：`~/.qoder-cn/bin/qoderclicn/qoderclicn-1.1.59`，入口 `~/.local/bin/qoderclicn` | 装法 `wget -qO- https://qoder.com.cn/install \| bash`（⚠️ 必须 `bash`，脚本第 3 行 `set -o pipefail` 在 Ubuntu 的 `sh`=dash 下会挂）。**桌面 IDE 另有 Linux `.deb`，但要 sudo ⇒ 用户自己装**；CLI 已够"让 VM 里的 Qoder 帮我跑" |
| Qoder CN **IDE** | ✅ **1.31.1**（`qoder-cn-ide_1.31.1-1789825954`）已装 `~/opt/qoder-ide`，入口 `~/.local/bin/qoder-ide`；21:47 已在 VM 桌面起窗 | **免 root 装法**（绕过 sudo）：官方直链 `https://qoder-ide-cn.oss-cn-hangzhou.aliyuncs.com/qoder/release/lastest/qoder-cn-ide_amd64.deb`（⚠️ 路径里 `lastest` 是官方拼写，别纠正）→ `dpkg-deb -x` 解到 `~/opt` → 软链。deb 179M / md5 `e01c48f4…`、解包 806M、`ldd` 依赖 0 缺失。**代价：没有应用图标、不自动更新**（要更新就重下再解，或改走 `sudo apt install`）。⛔ 别软链成 `code`（已被独立 VS Code 占）也别叫 `qodercn`（和 CLI 撞名）|
| `~/.atomgitdevenv` | 仍**干净**（未生成） | 第二账号登录/连接后才出现 —— 这是**唯一剩下的手工步骤** |
| 工作区 | ✅ **`~/ops_comp/ascend-operator-competition`**（439 文件 / 7.1M，12 个关键文件 md5 逐一对齐，HEAD `997ba8a`） | ⛔ **不含** `ops-transformer-master/`（235M，需按各 `codeN.md §10` 的 📦 注记自行 clone） |
| 凭据 | ✅ `public.pem` / `encrypt_password.py` / **`密钥.txt`**（`chmod 600`，md5 `d824be9e…` 双侧一致） | `密钥.txt` **可以带过去**：`encrypt_password.py` 用的是**服务器公钥**做 RSA-PKCS1v1.5，密文解出来就是同一个 CANNJudge 密码，**不与本机绑定**；在 VM 里重跑加密只会得到另一串随机填充的密文，不必重设 |
| git 角色 | ⛔ **Windows 是主干**：VM 的 `origin` push URL 已置为 `DISABLED_WINDOWS_IS_TRUNK_no_push_from_vm`（fetch 正常，可 pull） | 要解：`git remote set-url --push origin git@github.com:marak999459/ascend-operator-competition.git` |

**待办**：

> ⚠️ **2026-09-20 21:31 VM 侧连真机的实测卡点（下次接手先看这条）**：AtomGit **登录成功**（`login.gitCodeOAuth.session.settled outcome=success`）⇒ 扩展也已经认到实例，但紧接着
> `POST https://dev.gitcode.com/api/v1/proxy/vscode/DevContainerService/open-api-inner/v1/devenv/instances/git-ssh-key` 返回 **400** ⇒ **SSH 凭据没发下来** ⇒ `~/.atomgitdevenv/.ssh/config` 至今不生成（里面本该有 `Host devenvc_xxx`）、`.hub/vscode/` 下也没有 `<envId>.json`、VM 上没有任何转发端口。
> 判据（一条命令看全）：VM 里 `ls ~/.atomgitdevenv/.ssh/config ~/.atomgitdevenv/.hub/vscode/*.json` —— **两个都不存在就是还没通**。⛔ 这跟 IDE/扩展无关（换 Qoder IDE 也是同一个服务端接口），别再从客户端排查；按顺序试：面板里『重置SSH凭据』→『刷新』→再连接；仍 400 就去查该账号在 gitcode 上有没有配 SSH 公钥、以及环境是否处在可连状态。
> 另：21:23 那次 `activate.untrustedWorkspace.deferInit` = 工作区没信任 ⇒ 扩展会**延迟写 SSH 配置 / 起端口转发**（扩展自己的说明：`untrustedWorkspacesDesc`），21:31 重载窗口信任后已消失 —— 这是独立坑，别再和上面的 400 混为一谈。

- [x] VM 内装 VS Code —— 已按"免 root tarball"完成（上面表里那行），不改 PATH 之外的任何东西
- [x] 装 devspace 扩展（版本对齐 1.1.2）
- [x] 把 `密钥.txt` 带进 VM（通用 ⇒ 直接拷，权限 600）
- [ ] **用户 GUI**：在 VM 的 VS Code 里**登录第二个账号**并『连接』那台 NPU ⇒ VM 内生成自己的 `~/.atomgitdevenv/`（这一步只有用户能做，且必须开着 VM 桌面）
- [ ] **用户**：VM 里首次跑 `qoderclicn` 登录 Qoder 账号（`~/.qoder-cn/.auth` 是本机凭据，Agent 未跨机搬）
- [ ] 拿到别名后把这台补进 `devspace_tunnel.ps1` 的 `$EnvTable`（标清是**经 VM 二跳**、以及 `use='free'` 还是 `'reserved'`）
- [ ] ⚠️ 若要**从第二台真机提交比赛平台**：`cannjudge-submit` 会话要在 VM 侧另建一次（§2.2），凭据不随环境走
- [ ] 文档同步：Windows 侧 `codeN.md`/`连接信息.md` 改动后要把 VM 那份**重新 tar 管道推一次并逐文件 md5 复验**（主干单向 Windows→VM；VM 侧不产提交）

---

## 3. 通道机（VMware VM：第二账号通道 + 第二工位）

> ⚠️ **口径变更（2026-09-22）**：**仿真流程全部停用**（用户决定一直有真机），这台 VMware VM 只承担两件事 —— ① **第二账号的 devspace 登录位**（homedir 隔离，见 §2.3）② **第二工位 Qoder**。下面那行 CANN 路径是**历史遗留**（当年仿真用的），别再往这里派任务。

| 项 | 取值来源 |
|---|---|
| 用途 | ~~本地仿真机逻辑验证~~ → **第二账号通道 + Qoder 第二工位**（详见 §2.3 现状表） |
| 主机 / 用户名 | **现场读取**（VMware 设置或虚拟机内 `ip addr`）；2026-09-20 实测 `fszqsn@192.168.101.128` |
| CANN 路径 | `~/Ascend/cann/cann-9.0.0/set_env.sh`（⚠️ **不是** `/usr/local/Ascend/...`，那份写在旧文档里） |

> ⚠️ **每个环境的环境变量必须分别确认**，混用会直接报 `libascend_hal.so: cannot open shared object file`。

---

## 4. 通用上机注意事项

| 坑 | 现象 | 做法 |
|---|---|---|
| `scp` 卡死 | 长时间无输出后超时（隧道本身正常，`ssh` 秒回） | 改用 `cmd /c type` 管道传输，**传完必须 `md5sum` 复验** |
| PowerShell 管道改行尾 | 远端文件比本地**多 2 字节**（LF → CRLF），MD5 对不上 | 同上；字节敏感的场合尤其注意 |
| 跑测试前忘记 source | `libascend_hal.so: cannot open` | 每次跑测试前先 `source <对应 CANN 路径>/set_env.sh` |
